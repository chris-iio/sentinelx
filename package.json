{
  "private": true,
  "scripts": {
    "test": "vitest",
    "workflow:gpt-routing": "node --no-warnings=MODULE_TYPELESS_PACKAGE_JSON --experimental-strip-types scripts/audit-gpt-routing.ts"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^26.1.0",
    "typescript": "^6.0.2",
    "vitest": "^3.1.0"
  }
}
