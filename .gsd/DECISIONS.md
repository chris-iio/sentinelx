# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D027 | M003/S01/T01 | architecture | How to wrap _do_lookup() with per-provider semaphore without duplicating the 40-line lookup+retry body | Extract inner body to _do_lookup_body() helper; _do_lookup() calls it from inside or outside `with sem:` depending on whether the provider has a semaphore | The alternative (inline `if sem: with sem: [full body] else: [full body duplicate]`) would require maintaining two identical 40-line blocks. Extracting _do_lookup_body() keeps the semaphore gate as a clean 4-line if/else while the cache, retry, and store logic lives in one place. Trade-off: one extra private method, but significantly cleaner call graph for T02's backoff modifications. | Yes | agent |
| D028 | M003/S02/T01 | architecture | Whether to fire enrichment for IOCType.EMAIL IOCs | Exclude EMAIL from provider_counts in routes.py; no enrichment adapters for email | No threat intelligence providers in the current adapter set support email address lookups. Including email in the provider_counts dict would produce "email": 0 in the JSON payload, misleading the frontend into showing 0-provider enrichment progress bars for email IOCs. Exclusion keeps the enrichment flow clean until an email-capable adapter is added. | Yes — revisit when an email reputation adapter (e.g., EmailRep, Spamhaus) is integrated | agent |
| D001 | M001/S03 planning | architecture | When to inject section headers (VIS-03) and no-data summary rows (GRP-02) into the enrichment details container | Post-enrichment injection via markEnrichmentComplete() — not per-result | sortDetailRows() re-orders .provider-detail-row elements on each result arrival. Section headers (.provider-section-header) are NOT .provider-detail-row — they would become mispositioned if injected per-result then the rows sort around them. Post-enrichment injection into the final sorted DOM order avoids this timing issue entirely. The no-data summary row count is also accurate only after all results arrive. | Yes | agent |
| D002 | M001/S03/T02 | pattern | How to implement no-data row collapse/expand toggle | CSS-driven toggle: .no-data-expanded class on container controls child visibility; aria-expanded on trigger tracks state; keyboard Enter/Space wired for a11y | CSS-only visibility toggle avoids per-row JavaScript manipulation. Single class toggle on the container is efficient and consistent with the existing chevron-toggle pattern. Keyboard handlers required since element has role="button". | Yes | agent |
| D003 | M001/S03/T01 | architecture | Whether to remove .consensus-badge CSS when DOM creation was removed | Kept .consensus-badge CSS as dead CSS for safe rollback; only removed DOM creation in row-factory.ts | Consensus badge CSS may still be referenced by older cached pages. Leaving dead CSS is zero-cost and allows instant rollback if micro-bar has rendering issues. | Yes | agent |
| D004 | M001/S04 planning | architecture | Where to define the three-section structure (Reputation, Infrastructure Context, No Data) for enrichment details | Server-rendered in _enrichment_slot.html Jinja template with static .provider-section-header elements; JS routes rows to section containers at render time | S03 proved the section concept works via post-enrichment JS injection. Promoting the structure into the template gives the sections a stable DOM presence before any JS runs, eliminates the timing dependency on post-enrichment injection for headers, and makes the section structure visible in server-rendered HTML (better for debugging). The JS simplifies from "create headers + route rows" to just "route rows." Empty sections hide via CSS :has() selector (96%+ browser support). | Yes | agent |
| D005 | S04/T01 | architecture | Where should enrichment section structure (Reputation/Context/No Data) live? | Server-rendered in Jinja template (_enrichment_slot.html), not JS-injected post-enrichment | Section headers are structural, not behavioral — they belong in HTML. JS only routes rows into existing containers; CSS :has() handles empty-section hiding. Simpler, faster, and eliminates a class of timing bugs from post-enrichment injection. | Yes | agent |
| D006 | S04/T01 | architecture | How should empty enrichment sections be hidden? | CSS :has() selector on .enrichment-section containers | CSS :has(.provider-detail-row) eliminates need for JS visibility management. Sections auto-hide when empty, auto-show when rows are routed in. Supported in all modern evergreen browsers (Chrome 105+, Firefox 121+, Safari 15.4+). | Yes | agent |
| D007 | M001/S05 | architecture | Which cached_at timestamp to display when multiple providers are cached | Show oldest cached_at across all providers (worst-case data age) rather than newest | Analysts need to know the stalest data in the result set. Showing the newest would hide that some providers have much older cached data, undermining the trust signal. | Yes | agent |
| D008 | M001/S05/T01 | architecture | How to handle IP Context and ASN Intel both populating the context line, possibly out of order | IP Context replaces ASN Intel span when IP Context arrives (regardless of arrival order), using data-context-provider attribute for dedup | IP Context provides more comprehensive geo data (country, city, ASN) than ASN Intel (ASN + prefix only). When both fire, showing only IP Context avoids contradictory or redundant partial ASN-only data. Replacement via data-context-provider dedup attribute avoids duplicate or contradictory info. | Yes | agent |
| D009 | M001/S05/T02 | architecture | Whether error results should carry cachedAt field | Error results explicitly get cachedAt: undefined — only "result" type responses can be cached | Error responses from providers are not cached by the backend. Attempting to read cached_at on an error-typed result would require handling a discriminated union branch that never carries that field. Explicit undefined is clearer than conditional access. | Yes | agent |
| D010 | M002 discussion | architecture | Results page layout structure | Single-column full-width IOC rows replacing 2-column card grid | User wants lightweight info-first tool. Single column gives each IOC full width for data presentation — no cramped hashes, natural top-to-bottom triage scan. | No | agent |
| D011 | M002 discussion | pattern | Design language for results page | Quiet precision — verdict-only color, muted typographic hierarchy for everything else | Eliminates competing visual signals. Analyst's eye lands on verdict severity without parsing a wall of colored badges. Linear/Vercel energy rather than dark-ops density. | No | agent |
| D012 | M002 discussion | architecture | How to access full provider breakdown | Inline expand below IOC row — no page navigation | Keeps analyst in context for the 80% triage case. Detail page still exists for deep dives (relationship graph, annotations). | No | agent |
| D013 | M002 discussion | architecture | Information visible without interaction | Verdict + real-world context (geo/ASN/DNS) + key provider numbers (detection ratios, report counts) | User explicitly chose "both combined" — dense but clean at-a-glance surface. Core design challenge of the milestone. | No | agent |
| D014 | M002 discussion | architecture | Verdict dashboard approach | Compressed inline summary bar instead of 5 large KPI boxes | User said keep but simplify. Current KPI boxes push results below fold. Compact dashboard gives same info while keeping IOCs visible. | Yes — if analysts prefer prominent counts | agent |
| D015 | M002/S01 planning | architecture | Whether to rename contract selectors (.ioc-card → .ioc-row, partial filenames) in S01 | Preserve all existing selector names and partial filenames — mutate internals in place | Renaming .ioc-card to .ioc-row cascades into cards.ts, filter.ts, ui.ts, E2E selectors, and CSS contracts. Research confirms codebase is safer if S01 mutates existing partials in place. Roadmap language suggesting new names is aspirational, not functional. | Yes — can rename in S05 when E2E selectors are updated | agent |
| D016 | M002/S02/T01 | pattern | How to restore full opacity on enrichment slot when content has loaded | BEM modifier override: .enrichment-slot base sets opacity:0.85 as pre-load signal; .enrichment-slot.enrichment-slot--loaded overrides to opacity:1 with 0.2s ease transition | The --loaded class is already wired by enrichment.ts — the CSS just needed to respond to it. Base+modifier pattern keeps the pre-load dimming signal while giving a clean loaded state. The modifier class is also the correct diagnostic signal (count of 0 means TS pipeline issue, not CSS). | Yes | agent |
| D017 | M002/S02/T02 | pattern | Process safeguard for verifying prior task code changes landed | Always verify prior task's key_files via git show <hash> --stat before building on them | T01's git commit only saved .gsd docs — the actual input.css edits were never written to disk. T02 discovered this via git show and applied the fixes. A simple stat check would have caught this immediately and saved debugging time. | Yes | agent |
| D018 | M002/S03/T01 | pattern | How to wire click/keyboard handlers on dynamically-created summary rows in wireExpandToggles() | Event delegation on stable .page-results ancestor via event.target.closest(".ioc-summary-row") | wireExpandToggles() is called once from init() before any .ioc-summary-row elements exist (they're created during polling). Direct querySelectorAll at init time binds 0 handlers. Event delegation on the pre-existing .page-results ancestor captures bubbled events from all rows created at any point during polling. The old .chevron-toggle approach worked only because those buttons were server-rendered before init(). | Yes | agent |
| D019 | M002/S03/T01 | pattern | How to preserve the chevron icon element across incremental enrichment updates in updateSummaryRow() | Save chevronWrapper reference before summaryRow.textContent = "" and re-append it after rebuilding all other children | updateSummaryRow() uses summaryRow.textContent = "" as an immutable-rebuild pattern before re-appending verdict badge, attribution, micro-bar etc. Any child element injected only once (chevronWrapper, injected by getOrCreateSummaryRow()) is destroyed by this clear. Without the save+restore, the chevron disappears on the first incremental enrichment update. General rule: any persistent child in a clear-and-rebuild container must be explicitly saved before the clear and re-appended after. | Yes | agent |
| D020 | M002/S03/T02 | pattern | Where to trigger "View full detail →" link injection and how to prevent duplicate injection | Call injectDetailLink() from markEnrichmentComplete() with querySelector idempotency guard, iterating .enrichment-slot--loaded elements | Injecting on each handleProviderResult() call would re-inject the link on every incremental result. markEnrichmentComplete() fires once when all providers are done, ensuring the link appears at the right time with a stable ioc-type/ioc-value context. The querySelector(.detail-link-footer) idempotency guard is simpler than a boolean flag and works correctly even if the function is somehow called twice. Iterating .enrichment-slot--loaded (not all .enrichment-slot) skips slots that received no data. | Yes | agent |
| D021 | M002/S04/T03 | pattern | Casing of brand name in HTML <title> | All-lowercase "sentinelx" in <title> — tests must match template, not vice versa | The base.html template intentionally renders "sentinelx" (all-lowercase) as a brand choice. Two E2E tests in test_homepage.py and test_settings.py were asserting "SentinelX" (title-case), causing false failures. Tests were updated to match the template. Changing the template would alter the brand presentation. | Yes | agent |
| D022 | M002/S05/T01 | pattern | How to assert CSS class presence in Playwright when other classes may co-exist on the element | Use regex class-match pattern (r".*is-open.*") in to_have_class() rather than exact-match string | Playwright's to_have_class with a plain string checks for an exact full-class-list match on some versions; regex avoids fragility when other classes are co-present on the element (e.g., .ioc-summary-row.is-open alongside layout classes). | Yes | agent |
| D023 | M002/S05/T02 | pattern | When to register Playwright route mocks relative to navigation in E2E tests | Register Playwright page.route() BEFORE the page action that triggers the fetch | Registering after submit races against enrichment.ts first poll tick (750ms) and may miss it, leaving enrichment.ts with no response and the UI in an unloaded state. setup_enrichment_route_mock(page) must always precede idx.goto() and extract_iocs() in any helper that simulates online enrichment mode. | Yes | agent |
| D024 | M003 discussion | architecture | How to fix zero-auth provider starvation without true rate limiting | Per-provider concurrency semaphore: VT gets Semaphore(4), all other providers get Semaphore(8) or higher; global max_workers raised to ~20 | True token-bucket rate limiting (4 req/min) is complex and the actual bug is concurrency starvation, not over-calling. A semaphore of 4 for VT preserves the accidental compliance with VT's free tier limit while allowing zero-auth providers to run freely. The correct fix for VT rate limits would be a token bucket, but that's out of scope — the semaphore is a pragmatic improvement. | Yes — revisit if VT 429s persist after this fix | agent |
| D025 | M003 discussion | pattern | Retry behavior for 429 vs. other errors | 429 errors use exponential backoff with jitter before retry; all other EnrichmentErrors retry immediately (current behavior) | Immediate retry on 429 burns quota — two consecutive failed calls to VT waste 2 of 4/min. Non-429 errors (network timeout, 5xx) benefit from immediate retry since they're transient infrastructure failures, not quota exhaustion. Backoff formula: base_delay * (2^attempt) + random jitter, capped at 30s. | Yes | agent |
| D026 | M003 discussion | scope | Email IOC enrichment | Display-only for now — extract and show in EMAIL group, no enrichment adapters | OTX supports email lookups but wiring a new adapter for one provider in an "efficiency" milestone adds risk and scope. The value of email extraction is in surfacing the IOC type; enrichment can be added in a future milestone when more providers support it. | Yes — add enrichment adapters in a future milestone | agent |
| D029 | M003/S03 | pattern | How to display multiple provider results on the detail page | Stacked provider cards (all visible) replacing CSS-only radio tabs | The CSS-only radio tab pattern requires an inline <style> block with Jinja-generated per-tab :checked selectors, which is fragile and doesn't use design tokens. The detail page typically has 3-7 providers — all fit on screen as stacked cards. Stacked cards follow the M002 pattern of progressive disclosure (summary row → expanded details → detail page) without adding interaction complexity at the deepest level. Each card is self-contained with provider name, verdict badge, and fields. | Yes — could restore tabs if provider count grows large | agent |
| D030 | M003/S04 | pattern | OTX supported_types assertion count when IOCType.EMAIL is added | Keep assertion at 8 (not 9) — OTX explicitly excludes EMAIL with a named frozenset; update docstring to explain why | OTXAdapter.supported_types uses an explicit frozenset (not frozenset(IOCType)), deliberately excluding EMAIL because OTX has no email lookup endpoint. The S04 plan incorrectly assumed frozenset(IOCType) was used. When adding new IOC types, each adapter's supported_types must be audited individually — not assumed to auto-include new members. | Yes | agent |
| D031 | M004 discussion | architecture | Rate limiter storage backend for Flask-Limiter | Filesystem backend (`storage_uri="file://..."`) rather than Redis | Redis adds an external service dependency for a local dev tool. Filesystem backend survives restarts, is consistent across workers in practice (single-process deployment), and requires no new infrastructure. If SentinelX ever goes multi-process/distributed, revisit. | Yes — if multi-worker deployment needed | agent |
| D032 | M004 discussion | architecture | GeoIP HTTPS replacement for ip-api.com free tier | ipinfo.io free tier (`https://ipinfo.io/{ip}/json`, no auth, HTTPS, 50k req/month) | ip-api.com free tier is HTTP-only; Pro tier requires payment. ipinfo.io free tier provides equivalent fields (country, city, org/ASN), has generous limits for a local tool, and uses HTTPS by default. Response format is slightly different — the adapter must be updated to map `org` (e.g. "AS15169 Google LLC") to ASN + org name. | Yes — if ip-api Pro is added later | agent |
| D033 | M004 discussion | pattern | Polling cursor implementation | Client-side `since` counter sent as `?since=<int>` query param; server returns `results[since:]` and `next_since`; client increments cursor; removes `rendered` dedup map | Simplest correct implementation. Server-side cursor storage would require session state. Client cursor resets on page reload, which is correct — a fresh session should render all results. The `rendered` dedup map becomes dead code and is removed. | Yes | agent |
| D034 | M004/S01/T01 | architecture | How to fix semaphore-held-during-sleep without removing _do_lookup_inner() | Extract _single_attempt(); _do_lookup() does explicit sem.acquire()/release() with try/finally; sleep runs outside semaphore; _do_lookup_inner() kept as passthrough shim | The semaphore must not be held during time.sleep() to avoid stalling all concurrent slots. Extracting _single_attempt() isolates one HTTP attempt; the explicit acquire/release pattern makes the scope explicit. _do_lookup_inner() kept to avoid hidden-caller risk — no external callers exist, but the shim costs nothing and avoids a grep-required audit. | Yes — shim can be removed when S02 confirms no external callers | agent |
| D035 | M004/S01/T02 | pattern | Exception handler ordering for network errors in requests-based adapters | SSLError before ConnectionError before Exception; SSLError ordering is a hard correctness constraint, not style | SSLError inherits from ConnectionError in requests. Any swap of these handlers silently reroutes SSL errors to the wrong message. The full chain (Timeout → HTTPError → SSLError → ConnectionError → Exception) is the canonical order for all requests adapters. | No — ordering must never be reversed | agent |
| D036 | M004/S02/T03 | pattern | How ipinfo.io private IP responses (HTTP 404) are handled in the ip_api adapter | Check resp.status_code == 404 before raise_for_status() — short-circuit to EnrichmentResult(verdict="no_data", raw_stats={}) for private/reserved IPs | ip-api.com returned HTTP 200 with body.status=="fail" for private IPs; ipinfo.io returns HTTP 404. The 404 must be caught before raise_for_status() to avoid the HTTPError exception path — semantically, a private IP is "no data", not an error. This preserves the existing contract where private IPs return a clean no_data result rather than an EnrichmentError. | Yes | agent |
| D037 | M004/S04 | architecture | Rate limiter storage backend for R025 requirement | Keep `memory://` with explanatory comment — the `limits` library (used by Flask-Limiter) has no filesystem backend; only Redis, Memcached, and MongoDB are supported as persistent alternatives. Adding any of these would introduce infrastructure dependencies inappropriate for a single-process local dev tool. | R025 requires "persistent storage backend (filesystem or Redis), not memory://". However, the `limits` library does not support filesystem storage (confirmed via `storage_from_string("file://...")` → ConfigurationError). The only persistent options are Redis, Memcached, and MongoDB — all require running external services. D031 originally chose "filesystem backend" but this is not implementable. For a local dev tool running as a single process, memory:// is functionally correct and resets are acceptable. The comment documents the constraint for future revisiting. | Yes — if Redis is added for caching or other features, rate limiter can piggyback on it | agent |
| D038 | M004/S04/T03 | architecture | Rate limiter storage backend for Flask-Limiter | Keep memory:// (in-memory) storage — do not add Redis/Memcached | Flask-Limiter's `limits` library only supports Memory, Redis, Memcached, and MongoDB backends — there is no filesystem backend. SentinelX is a single-process local dev tool; adding Redis or Memcached would introduce infrastructure dependencies inappropriate for the use case. Rate limit counters reset on restart, which is acceptable for a local tool. | Yes — if SentinelX moves to multi-process deployment, revisit with Redis backend | agent |
| D039 | M005 | arch | Where to place safe_request() helper | In app/enrichment/http_safety.py alongside existing validate_endpoint(), read_limited(), TIMEOUT | http_safety.py already centralizes all HTTP safety controls. safe_request() composes these existing primitives into a single call. Keeps the module focused on HTTP safety concerns. Adapters already import from this module. | No | collaborative |
| D040 | M005 | pattern | safe_request() API shape for adapter consolidation | safe_request() accepts method, pre-raise hooks, optional data/headers; each adapter calls safe_request() and parses the returned dict | 12 adapters vary in GET vs POST, 404/429 pre-checks, and per-request headers. A single function with method parameter, optional pre_raise_hooks callback, optional data (for POST), and optional headers (for per-request overrides) covers all variations without becoming a Swiss Army knife. Returns dict (parsed body) or EnrichmentError. | Yes — if new adapter patterns emerge | collaborative |
| D041 | M005 | pattern | Test strategy for safe_request() migration | Keep existing adapter tests mocking adapter._session; add new tests for safe_request() itself | Existing adapter tests mock adapter._session directly and provide full regression coverage. safe_request() calls self._session internally, so these mocks still work. Refactoring 12 test files to mock safe_request() instead adds churn for no additional signal. New dedicated tests for safe_request() cover the extracted helper's own behavior. | Yes — if adapter test patterns drift | collaborative |
| D042 | M006 discussion | architecture | Where to store analysis history data | SQLite alongside existing CacheStore (WAL-mode DB) | Zero new infrastructure. Reuses the same SQLite WAL-mode pattern already proven in CacheStore. Gets proper querying for free. Fits the self-hosted, no-external-dependencies ethos. | Yes — if storage size becomes a problem | collaborative |
| D043 | M006 discussion | library | WHOIS data source for domain enrichment | python-whois (direct WHOIS protocol, no API key, no rate limits) | Fits the self-hosted zero-infrastructure ethos. No API key needed, no rate limits, works offline. Tradeoff: data quality varies by TLD registrar, but good enough for 80% of domains. Hosted APIs (WhoisXML) would require API keys and introduce external dependency. | Yes — if data quality proves insufficient for key TLDs | collaborative |
| D044 | M006 discussion | architecture | How analysts access past analysis history | Recent analyses list on the home page below the input form — not a separate /history page | Keeps the tool lightweight. User's primary negative constraint was "complexity / heaviness." A separate history page adds navigation, feels like a platform feature. A list on the home page is immediate, contextual, and doesn't add UI chrome. | Yes — if history volume outgrows a simple list | collaborative |
| D045 | M006 discussion | architecture | Analysis history storage depth | Store full enrichment results alongside input text and IOC list — reload shows complete results page without re-querying providers | Re-enriching on reload would burn API quota and be slow. The whole point of history is instant access to past work. Storing full results means each analysis adds more data to the DB, but for a local tool this is acceptable. | Yes — could add a purge/retention policy later | collaborative |
| D046 | M006 discussion | scope | Whether to add a REST API for programmatic IOC submission | Defer to a future milestone | User explicitly chose "Not for M006" when asked. REST API is a valid future feature but not needed for the current scope of making the tool more useful for analysts working in the browser. | Yes — candidate for next milestone | human |
| D047 | M007 planning | architecture | Whether to reattempt safe_request() consolidation from M005 | Yes — safe_request() does not exist in the codebase despite M005 claiming completion. The work must be redone. | Investigation confirmed: http_safety.py has only validate_endpoint() and read_limited(). All 12 HTTP adapters still inline the full boilerplate. R026/R027 marked validated incorrectly — reset to active for M007. | Yes | collaborative |
| D048 | M010/S02 | architecture | Where Recent Analyses lives after removal from home page | Dedicated /history route in history.py, new history.html template, nav link in base.html | User chose dedicated page over results page or removal. Gives history proper visibility and room to grow. Reuses existing HistoryStore.list_recent() and existing /history/<id> detail route. | Yes | human |
| D049 | M011/S02 | pattern | Per-adapter test dedup vs contract tests | Remove duplicates entirely and trust the contract suite. Per-adapter files keep only verdict logic and response parsing tests. | Contract tests (test_adapter_contract.py) parametrically cover protocol conformance for all 15 adapters. Granular per-field tests that re-assert detection_count=0 / total_engines=0 / scan_date=None are pure duplication. | No | human |
