# M017 Optimization Audit — SentinelX

- Mode: `baseline`
- Generated at: `2026-05-22 06:42:53 UTC`
- Repo root: `/home/chris/projects/sentinelx`
- Output path: `.gsd/milestones/M017/M017-AUDIT.md`

## Workflow contract

- A finding must be backed by **measurement when practical**. If direct measurement is awkward or too invasive, the finding must cite **explicit code-path reasoning** instead of taste-based cleanup language.
- Every finding must land in exactly one ranked bucket: `do now`, `do next`, `later`, or `leave alone`.
- Every finding must call out the continuity guardrails it could endanger and the verification lanes that must be rerun before claiming the optimization is safe.
- `leave alone` is a valid outcome when current architecture is already intentional and the evidence does not justify churn.

## Command surface

| Entry point | Command | Purpose |
| --- | --- | --- |
| CLI help | `python3 tools/optimization_audit.py --help` | Show the supported modes, capture options, and output controls. |
| Template scaffold | `python3 tools/optimization_audit.py --milestone-id M017 --mode template --output .gsd/milestones/M017/M017-AUDIT-TEMPLATE.md` | Create a reusable milestone-local ranked artifact template. |
| Working baseline artifact | `python3 tools/optimization_audit.py --milestone-id M017 --mode baseline --output .gsd/milestones/M017/M017-AUDIT.md` | Create/update the current audit document used by later optimization slices. |
| Convenience targets | `make audit-m017-template` / `make audit-m017` | Repo-native wrappers around the same workflow for contributors. |

## Verification lanes

| Lane | Command | Use when |
| --- | --- | --- |
| verify-fast | `make verify-fast` | Default rerun lane for security scanning, backend/frontend logic, build/test plumbing, and any finding that does not change mocked-online browser behavior. |
| verify-deep | `make verify-deep` | Required whenever a change touches live enrichment orchestration, polling/status flow, results-page DOM/state, or mocked-online browser seams. |
| verify | `make verify` | Full pre-handoff lane when downstream slices need the unambiguous repo-wide proof command. |

## Verified rerun checklist

| Step | Proof surface | Command | Required when | Expected durable evidence |
| --- | --- | --- | --- | --- |
| 1 | M017 workflow runner + identity-grounded ranked artifact refresh | `python3 tools/optimization_audit.py --milestone-id M017 --mode baseline --output .gsd/milestones/M017/M017-AUDIT.md` | Every M017 optimization slice before handoff. | Updated M017 audit artifact citing `docs/project-map.md`, R085/R087, D078-D080, S01 seam priorities, and current ranked buckets. |
| 2 | Fast local regression lane | `make verify-fast` | Every shipped optimization, including keep-decisions that changed code or build/test plumbing. | Fresh command capture or task summary evidence proving unit/integration/frontend/build checks stayed green. |
| 3 | Deterministic mocked-online browser proof | `make verify-deep` | Any change touching live enrichment orchestration, polling/status flow, shared result application, or analyst-visible DOM/state. | Fresh evidence proving the analyst-visible mocked-online browser seam still passes end-to-end. |
| 4 | S04 frontend/render outcome confirmation | Compare `do now` frontend/render rows against T01/T02 focused proof and mocked-online browser evidence | Before S05 final assembly. | Clear shipped-or-rejected decision explaining why the secondary optimization outcome is durable and evidence-backed. |

## Continuity guardrails

| Requirement | Continuity guardrail |
| --- | --- |
| R008 | Preserve enrichment polling, export, filtering, detail links, copy buttons, and progress continuity. |
| R009 | Preserve CSP, CSRF, SSRF allowlist, host validation, and DOM-safety constraints. |
| R010 | Preserve or improve polling/render efficiency. |
| R014 | Preserve per-provider concurrency behavior unless evidence proves a better approach. |
| R015 | Preserve 429 backoff behavior unless evidence proves a better approach. |
| R018 | Preserve semaphore/backoff and snapshot correctness unless evidence proves otherwise. |
| R019 | Preserve cursor-based polling efficiency unless evidence proves otherwise. |
| R020 | Preserve persistent HTTP session behavior where still justified. |
| R022 | Preserve WAL-mode cache/history store behavior unless evidence supports change. |
| R040 | Keep strong verification continuity while refactoring and optimizing. |

## Measurement captures

| Capture | Command | Exit | Duration (ms) | Summary |
| --- | --- | ---: | ---: | --- |
| runtime-provider-diagnostics | `internal benchmark: EnrichmentOrchestrator synthetic runtime/provider diagnostics` | 0 | 5 | provider mix CacheAlpha:2d/0e, RateLimitBeta:2d/1e; dispatch=4; attempts=5; cache-hit ratio 1/5 (20%); retries=1 (429=1); errors=1; latency total=2.25s max=1.00s. |
| status-snapshot-scaling | `internal benchmark: EnrichmentOrchestrator.get_status() snapshot scaling` | 0 | 92 | 400 polls at 5000 retained results: `get_status()` 90.09ms vs `get_incremental_status(since=4990)` 2.71ms (33.2x faster) while returning 10 tail rows with next_since=5000. |
| cache-store-tempdb | `internal benchmark: CacheStore temp WAL put/get loop` | 0 | 20 | Temp WAL cache DB: 250 puts in 6.74ms, 250 TTL reads in 1.18ms, 250 hits, 250 retained rows. |
| cache-stats-query-count | `internal benchmark: CacheStore.stats aggregate query count` | 0 | 12 | CacheStore.stats() executed 1 SELECT for total_entries=1 and oldest_present=True: SELECT COUNT(*), MIN(cached_at) FROM enrichment_cache. |
| history-store-tempdb | `internal benchmark: HistoryStore temp WAL save/list/load loop` | 0 | 20 | Temp WAL history DB: 180 saves in 6.73ms, list_recent(20) in 0.05ms, single load in 0.02ms, latest total_count=1, recent rows=20. |
| pipeline-duplicate-candidates | `internal benchmark: run_pipeline normalized duplicate candidate gate` | 0 | 73 | 7 raw URL variants normalize to 1 IOC value(s); classify calls=1; output values=http://evil.com. |

## Seam checklist

### runtime/provider

- Continuity focus: Orchestrator concurrency, cache interaction, retry/backoff behavior, and provider dispatch cost.
- Audit prompt 1: What work is measured here, and what hot-path reasoning is still required?
- Audit prompt 2: Which guardrails and rerun lanes must stay attached if we change this seam?

### request/status

- Continuity focus: Flask route/helper status flow, next_since continuity, and history-save diagnostics.
- Audit prompt 1: What request-path work is actually hot versus only structurally central?
- Audit prompt 2: If a finding changes analyst-visible status behavior, which proof lane catches it?

### persistence

- Continuity focus: SQLite WAL cache/history store access, locking, query shape, and post-enrichment durability.
- Audit prompt 1: Is there measured contention, or should this seam remain a leave-alone decision?
- Audit prompt 2: What evidence would justify revisiting long-lived WAL-backed connections?

### frontend/render

- Continuity focus: Polling cadence, shared live/history result application, and DOM/render churn.
- Audit prompt 1: What analyst-visible work is actually happening per poll or per render flush?
- Audit prompt 2: Does the finding preserve live/history parity and deterministic mocked-online proof?

## Ranked finding schema

Use the same table shape in every bucket. Required fields per row:

- **Finding** — one concrete optimization or keep-decision.
- **Seam** — `runtime/provider`, `request/status`, `persistence`, or `frontend/render`.
- **Evidence kind** — `measurement` or `code-path reasoning`.
- **Evidence summary** — cite the measurement, command capture, or the exact path reasoning that justifies the rank.
- **Continuity guardrails** — list the requirement IDs that must remain protected.
- **Rerun lanes** — at minimum one of `make verify-fast`, `make verify-deep`, or `make verify`.
- **Continuity notes** — state what behavior must remain true after the future change ships, or why the seam should stay untouched.

## M017 identity-grounded contract

- Project map grounding: `docs/project-map.md` is present and anchors this audit to SentinelX as a local analyst IOC triage workbench: paste investigation text, extract IOCs, optionally enrich them, then review verdict-first results with history, details, filters, copy/export, and diagnostics.
- Analyst identity: SentinelX is optimized as a fast local analyst IOC triage workbench, not as generic subsystem cleanup.
- Decisions: D078 requires `docs/project-map.md` and `.gsd/PROJECT.md` before target selection; D079 ranks work by the analyst IOC triage loop; D080 allows aggressive/moderate cross-seam optimization only with proof.
- Requirements: R085 requires product-identity-grounded optimization decisions; R087 requires measurement when practical or explicit code-path reasoning plus regression proof.
- S01 seam inventory priorities: (1) enrichment fan-out/status snapshot cost across `app/enrichment` and `app/routes`, (2) browser result rendering churn, (3) SQLite cache/history access shape, (4) IOC pipeline duplicate candidate handling in `app/pipeline`, (5) provider registration/config diagnostics clarity.
- S03 shipped proof: the do-now M017 optimization is the enrichment fan-out/status snapshot path, with measurement from `status-snapshot-scaling` and code-path proof in `app/enrichment/orchestrator.py::get_incremental_status()` plus `app/routes/enrichment_jobs.py::_get_enrichment_status()`.
- Current pipeline proof: the duplicate IOC candidate follow-up now ships in `app/pipeline/extractor.py::run_pipeline()` with `pipeline-duplicate-candidates` measurement and focused `tests/test_pipeline.py` regression proof.

## Baseline stance

- Do now: S03 shipped the enrichment fan-out/status snapshot optimization, the highest-ranked S01 seam in SentinelX's analyst IOC triage loop, with `status-snapshot-scaling` measurement and explicit route/orchestrator code-path proof.
- Do now: incremental status snapshots now return empty out-of-range cursor tails before walking retained results.
- Do now: polling result serialization now reuses one cached-marker map lookup per payload and skips per-result cache-key lookup work when the marker map is empty.
- Do now: S04 shipped the secondary frontend/render optimization by keeping shared result application on the severity-change gate, so provider-only/no-op flushes skip global dashboard recount/reorder while severity-changing flushes still update counts and order.
- Do now: live polling progress updates now reuse init-time progress element handles instead of re-querying the same IDs per status payload.
- Do now: summary-row cached timestamp selection now uses a single-pass oldest lookup instead of filter/map/sort allocation.
- Do now: inline context snippet formatting now builds ASN and DNS text directly instead of using temporary arrays and joins.
- Do now: no-data summary insertion now scans section children once instead of querying and allocating a NodeList.
- Do now: summary attribution now uses a single-pass best-candidate scan instead of filter/copy/sort allocation.
- Do now: summary worst-verdict computation now combines known-good override and severity tracking in one scan.
- Do now: exported worst-entry lookup now caches severity and short-circuits once malicious is found.
- Do now: result-application severity detection now compares dirty IOC verdict changes during flush instead of taking whole-grid before/after snapshots.
- Do now: provider-count metadata parsing now caches parsed DOM JSON by raw attribute value and reparses only when that value changes.
- Do now: provider detail-row sorting now caches row severity in an indexed NodeList pass before sorting instead of recomputing it in comparator calls or allocating through Array.from/map.
- Do now: export dropdown actions now use one delegated dropdown listener instead of per-action button listeners.
- Do now: IOC card sorting now caches card severity in an indexed NodeList pass before sorting instead of recomputing it in comparator calls or allocating through Array.from/map.
- Do now: card verdict label updates now share one classList-based helper instead of duplicated className split/filter/join logic.
- Do now: filter applications now reuse init-time static card/control node lists with indexed NodeList loops instead of re-querying them or using callback iteration on every filter change.
- Do now: card stagger initialization now applies capped CSS indexes with indexed NodeList iteration instead of callback iteration.
- Do now: copy buttons now use one delegated document click handler instead of attaching a listener per button at init.
- Do now: form initialization now reuses one element lookup bundle across submit, auto-grow, and mode-toggle helpers.
- Do now: settings accordion updates now reuse init-time header references with indexed loops instead of querying headers or callback-iterating records on every expansion.
- Do now: settings initialization now reuses one settings-section query with indexed NodeList loops for accordion and key-toggle setup.
- Do now: dashboard verdict-count updates now scan count elements once with indexed NodeList loops and use a precomputed verdict set instead of querying once per bucket or scanning the verdict tuple per count element.
- Do now: frontend export text construction now accumulates CSV and copy-all IOC text directly instead of collecting row/value strings in secondary arrays before joining.
- Do now: frontend CSV exports now use a literal static header instead of joining a columns array at module load.
- Do now: IOC detail graph setup now splits nodes and indexes providers in single-pass loops instead of extra array helper passes.
- Do now: browser-route provider-count metadata and coverage counts now use registry direct count paths instead of allocating provider lists for count-only metadata.
- Do now: Online fanout admission diagnostics now use cached direct provider counts instead of allocating provider lists per IOC type.
- Do now: browser-route enrichable progress totals now reuse admission fanout counts instead of repeating same-type registry scans after launch.
- Do now: enrichment dispatch execution now delegates ThreadPoolExecutor/future plumbing to a job-execution helper instead of carrying it in the orchestrator method body.
- Do now: enrichment single-attempt cache/adapter/latency flow now lives in a dedicated attempt-execution helper instead of the orchestrator method body.
- Do now: enrichment diagnostics state repair and provider bucket selection now live in a diagnostic-state helper instead of the orchestrator method body.
- Do now: online routes now reuse the configured-provider list across admission, coverage metadata, and orchestrator launch, and skip provider setup entirely for zero-IOC submissions.
- Do now: API health registry detail now uses direct registered/configured count paths instead of allocating provider lists for count-only metadata.
- Do now: health payload construction and validation now reuse precomputed check order/key sets instead of sorting or allocating static validation sets on every request.
- Do now: shared health/diagnostic contract frozensets now use tuple inputs instead of temporary set literals at module import.
- Do now: API analyze responses now serialize and group each IOC in one pass only after online admission checks pass.
- Do now: browser analyze validation, Online branching, and render decisions now live in one result helper while still skipping template grouping for missing-provider redirects.
- Do now: history reload now rebuilds and groups persisted IOC models in one pass, skips empty-history grouping, and returns the empty replay JSON literal without invoking the JSON encoder.
- Do now: IOC detail routes now use a precomputed valid-type set instead of rebuilding it per request.
- Do now: orchestration diagnostic export coercion now applies top-level, nested dict, and list caps with bounded key/list iteration instead of materializing bounded copies or allocating mapping items views before filtering.
- Do now: orchestration diagnostic export list coercion now accumulates primitive list values directly for up to four entries instead of using a list-comprehension frame, and nested diagnostic child scalar assignment delegates to `_set_export_child_scalar()`.
- Do now: diagnostic source sanitization now applies mapping and sequence caps with bounded iteration instead of materializing full containers before slicing.
- Do now: recent-history diagnostic payloads now use bounded iteration over returned rows and helper-owned row append mutation instead of slicing a second list after the store-level limit.
- Do now: diagnostic safe-error summary normalization now uses a compiled whitespace regex instead of split/join word-list allocation.
- Do now: diagnostic source-record field validation and payload shaping now live in a dedicated contract helper instead of the dataclass methods.
- Do now: diagnostic source descriptor validation now lives in a dedicated preparation module instead of carrying duplicate/path validation state in the assembler.
- Do now: diagnostic archive path validation now scans path segments once instead of allocating split-list parts and rescanning them.
- Do now: diagnostic payload traversal now lives in a dedicated payload-redaction helper instead of the redaction facade.
- Do now: diagnostic provider-label normalization now reuses a compiled cleanup regex instead of module-level `re.sub()` calls.
- Do now: diagnostic text credential pattern redaction now lives in a dedicated rule engine instead of keeping regex replacement callbacks in the recursive redaction module.
- Do now: diagnostic configured-secret candidate ordering now skips list sorting for zero or one candidate while preserving longest-secret-first ordering for overlapping multi-secret redaction.
- Do now: IOC normalization now skips defang regex substitutions for already-clean values with no known defang sentinel.
- Do now: the IOC pipeline now skips repeated classification for raw variants that normalize to the same canonical value, preserving first-match output semantics with focused regression proof.
- Do now: IP classification now parses candidates once instead of probing IPv6 and IPv4 separately.
- Do now: domain classification now reuses one lowercase value for blacklist lookup and IOC output.
- Do now: raw IOC extraction now appends first-seen candidates directly while tracking a seen-raw set instead of returning dict values.
- Do now: SSH auth.log parsing now streams lines, parses BSD timestamps directly, and caches repeated source classification instead of materializing full uploads, calling `strptime()`, or reparsing duplicate sources.
- Do now: `CacheStore.stats()` now uses one aggregate SQLite query for total entries and oldest timestamp, backed by query-count proof.
- Do now: nonpositive cache TTL reads now return before SQLite because such entries cannot be fresh.
- Do now: empty cache read/write payloads now use JSON literals instead of invoking the encoder or decoder for empty result payloads.
- Do now: cache detail reads now use direct row projection for up to four provider rows before falling back to the accumulator loop.
- Do now: cache/history SQLite PRAGMA setup now lives in one shared helper, preserving WAL behavior while removing duplicated store initialization code.
- Do now: recent-history summaries now project the 120-character input preview in SQLite while full history reload preserves the saved input.
- Do now: empty history save/load payloads now use JSON literals instead of invoking the encoder or decoder for empty IOC/result lists.
- Do now: history top-verdict computation now short-circuits once a malicious verdict is found, preserving severity semantics with focused proof.
- Do now: registry scans now avoid provider values views and list-comprehension frames for configured-provider filters, and provider-count lookups count matching providers with a direct counter loop instead of allocating an intermediate provider list, values view, or sum-generator path.
- Do now: static adapter membership frozensets now use tuple inputs instead of temporary set literals at module import.
- Do now: DNS lookup now uses table-driven extractors plus shared raw_stats and per-record resolution helpers, and result construction uses one informational provider envelope helper.
- Do now: DNS TXT extraction now decodes single-chunk records directly and only joins multi-chunk TXT records when needed.
- Do now: Team Cymru ASN TXT decoding now skips join iteration for common single-chunk answers while raw_stats shaping is delegated to one helper.
- Do now: VirusTotal now uses the shared HTTP lookup pipeline, shared 404/429 status helpers, and delegated analysis-map, engine-count, verdict, timestamp, top-detection, and raw_stats mechanics while engine totals still compute in one scan.
- Do now: ThreatMiner lookup paths now share result-body gates and raw_stats helpers while capped passive-DNS/sample extraction remains bounded.
- Do now: EmailRep malicious verdict selection now reuses the ordered risk-flag scan, parsing delegates verdict/count/raw_stats mechanics, and result construction uses one provider envelope helper.
- Do now: AbuseIPDB now uses the base adapter's generic 429 rate-limit policy flag and result construction uses one provider envelope helper for parsed score-threshold responses.
- Do now: crt.sh now uses the shared HTTP lookup pipeline for JSON-list responses, delegates raw_stats shaping while still using one body scan, and result construction uses one informational provider envelope helper.
- Do now: crt.sh subdomain selection now skips sorting empty, single-subdomain, and short multi-subdomain sets up to four entries while preserving sorted multi-subdomain output and oversized caps.
- Do now: Shodan now uses the base adapter's generic 404 no-data policy flag while result construction uses one provider envelope helper for 404 and parsed InternetDB responses.
- Do now: ThreatFox best-record selection now short-circuits when a perfect confidence record is seen instead of callback-scanning every record, and ThreatFox result construction now uses one provider envelope helper.
- Do now: WHOIS name-server normalization now reuses already-materialized lists instead of copying them, and WHOIS result construction now uses one informational provider envelope helper.
- Do now: HTTP adapters now cache allowed-host membership as a frozenset at construction instead of rescanning the caller's allowlist for every SSRF validation.
- Do now: route-mapped HTTP adapters now derive supported IOC types from endpoint maps instead of repeating equivalent frozenset literals, Hashlookup/OTX use the base adapter's generic 404 no-data policy flag, Hashlookup/OTX/URLhaus parsing delegates raw_stats/verdict mechanics, and result construction uses provider envelope helpers.
- Do now: MalwareBazaar parsing now delegates raw malware metadata shaping, and result construction uses one provider envelope helper for found and not-found responses.
- Do now: GreyNoise result construction now uses one provider envelope helper for 404 and parsed Community API responses.
- Do now: ConfigStore writes now keep the just-written parser cached so immediate read-after-write paths do not reparse disk.
- Do now: ConfigStore value normalization, TTL parsing, and provider-section accumulation now live in config value helpers.
- Do now: provider registry setup now walks one catalog-owned registration plan while preserving all 16 providers and key semantics.
- Do now: security scanner JSON finding serialization now shares one append helper for the long path while short result paths stay direct.
- Do now: runtime-state boundary JSON serialization now shares append helpers for long classification and audit-finding paths while short paths stay direct.
- Do now: runtime-state repair JSON action serialization now shares one append helper for the long path while short paths stay direct.
- Later: broader SQLite cache/history access shape still needs contention evidence before promotion.
- Leave alone: provider registration/config diagnostics clarity should not distract this optimization pass unless readiness diagnostics become the actual blocker.
- Evidence standard: every shipped optimization needs before/after measurement when practical, or explicit code-path reasoning plus regression proof; S03 satisfies that standard for status polling and S04 satisfies it for the frontend/render follow-up; artifacts must not expose API keys, tokens, or analyst-sensitive IOC data.

## Ranked findings

### do now

| Finding | Seam | Evidence kind | Evidence summary | Continuity guardrails | Rerun lanes | Continuity notes |
| --- | --- | --- | --- | --- | --- | --- |
| Keep S03's shipped enrichment status polling optimization on the tail-only snapshot path for SentinelX's analyst IOC triage loop. | enrichment fan-out/status snapshot cost | measurement + code-path reasoning | `docs/project-map.md` ranks enrichment fan-out/status snapshot cost as the #1 optimization priority for the local analyst IOC triage workflow. The `status-snapshot-scaling` capture measures the old retained-list snapshot against the shipped tail accessor: `get_status()` scales with retained results while `get_incremental_status(since=4990)` returns only the tail and preserves `next_since`. Code-path proof lives in `app/enrichment/orchestrator.py::get_incremental_status()` and `app/routes/enrichment_jobs.py::_get_enrichment_status()`, where the polling route calls the incremental accessor and serializes only returned tail rows plus aligned `cached_markers`. `app/enrichment/status_snapshots.py` now owns full and incremental status snapshot construction, so `app/enrichment/orchestrator.py` can focus on dispatch, retry, cache, and diagnostics mutation. `app/enrichment/job_state.py::register_live_job()` now owns live job registration, stale terminal cleanup, and retention enforcement, `app/enrichment/job_state.py::resolved_job_record()` owns live-versus-terminal record resolution for status and diagnostics accessors, `app/enrichment/job_state.py::mark_live_job_failed()` and `mark_live_job_complete()` own id-based terminal lifecycle mutation, `app/enrichment/job_state.py::record_live_lookup_result()` owns id-based lookup result progress mutation, and `app/enrichment/job_state.py` also owns initial job records, completion/failure mutation, live/terminal LRU eviction, eviction tombstones, and cached-marker snapshot construction, leaving `app/enrichment/orchestrator.py` focused on dispatch, retry, cache, and diagnostics mutation rather than status-dict shape. Full `get_status()` snapshots still return a caller-isolated result list, but now copy retained results with direct accumulation instead of routing through the `list()` constructor; focused proof lives in `tests/test_orchestrator.py::TestGetStatusListSnapshot::test_get_status_snapshot_copies_results_directly`. The incremental snapshot helper now uses `itertools.islice()` plus helper-owned tail append mutation through `append_result_tail_item()` for non-negative and negative cursors to avoid creating an intermediate sliced results list or constructor-copying the returned tail while preserving Python negative-slice compatibility; focused proof lives in `tests/test_orchestrator.py::TestIncrementalStatusSnapshot::test_get_incremental_status_nonnegative_since_does_not_slice_results`, `test_get_incremental_status_copies_tail_without_list_constructor`, and `test_get_incremental_status_preserves_negative_since_behavior`. Out-of-range cursors now return an empty tail before walking retained results; focused proof lives in `tests/test_orchestrator.py::TestIncrementalStatusSnapshot::test_get_incremental_status_returns_empty_tail_beyond_retained_length`. Scalar status fields are now copied directly by known public key instead of scanning every job item and filtering internals; focused proof lives in `tests/test_orchestrator.py::TestIncrementalStatusSnapshot::test_get_incremental_status_builds_scalar_fields_without_items_scan`. `app/enrichment/status_snapshots.py::aligned_cached_markers_snapshot()` now aligns cached markers with direct empty/single/pair/three/four-result paths before the generic tail loop, and marker key construction reuses `app/enrichment/cache_payloads.py::cache_marker_key()` instead of repeating the `ioc|provider` key format; focused proof lives in `tests/test_orchestrator.py::TestIncrementalStatusSnapshot::test_aligned_cached_markers_skips_iteration_for_short_tails` and `test_incremental_status_reuses_cache_marker_key_helper`. The public `cached_markers` snapshot now delegates to `app/enrichment/job_state.py::cached_markers_snapshot()`, fallback marker-copy mutation delegates to `append_cached_marker_snapshot_entry()`, and cache-hit marker mutation delegates to `app/enrichment/job_state.py::record_cached_marker()`, preserving caller isolation while copying markers with direct key accumulation instead of `dict(self._cached_markers)` and keeping mutable marker-map writes with the job-state helpers; focused proof lives in `tests/test_orchestrator.py::TestCachedMarkersLock::test_cached_markers_snapshot_copies_directly`, `test_cached_markers_snapshot_short_paths_skip_fallback_loop`, and `test_orchestrator_delegates_single_attempt_execution`. `app/enrichment/cache_payloads.py` now owns cache-hit hydration, marker keys, and cache-store payload shaping. `app/enrichment/attempt_execution.py::run_single_attempt()` now owns single-attempt cache lookup, adapter invocation, successful result cache storage, error recording, and latency timing; `EnrichmentOrchestrator._do_lookup()` binds that helper directly for retry execution instead of retaining `_single_attempt()` as a compatibility delegator, while cache-marker mutation stays lock-scoped in `EnrichmentOrchestrator._record_cached_marker()`. Focused proof lives in `tests/test_orchestrator.py::TestCachedMarkersLock::test_orchestrator_delegates_cache_payload_helpers` and `test_orchestrator_delegates_single_attempt_execution`. `app/enrichment/dispatch_plan.py` now owns adapter/IOC dispatch-pair construction and keyed-provider semaphore construction, so `app/enrichment/orchestrator.py` no longer carries nested dispatch setup or API-key semaphore setup loops inside the class body. Focused proof lives in `tests/test_orchestrator.py::TestMultiAdapterDispatch::test_orchestrator_delegates_dispatch_setup_helpers`, which keeps those helpers out of `EnrichmentOrchestrator.__init__()` and `enrich_all()` while guarding dispatch-pair construction against list-comprehension frames. `app/enrichment/job_execution.py::run_dispatch_pairs()` now owns ThreadPoolExecutor/future completion plumbing for dispatch pairs, leaving `EnrichmentOrchestrator.enrich_all()` focused on job-state lifecycle, dispatch setup, delegated execution, and terminal mutation. `run_dispatch_pairs()` also uses direct empty/single/pair/three/four-dispatch-pair submission paths through `submit_dispatch_pair()` before falling back to the generic submission loop, keeping execution aligned with dispatch-pair construction and diagnostics. Focused proof lives in `tests/test_orchestrator.py::TestMultiAdapterDispatch::test_orchestrator_delegates_thread_pool_dispatch_execution`, which keeps executor setup and `as_completed()` out of `enrich_all()` while preserving locked result/done updates through `app/enrichment/job_state.py::record_live_lookup_result()`, and `test_dispatch_execution_skips_pair_iteration_for_four_or_fewer_pairs`, which proves short dispatch execution indexes pairs directly without real thread timing. Top-level `app/enrichment` modules now import sibling enrichment helpers through relative imports instead of round-tripping through the `app.enrichment` package facade, with `tests/test_orchestrator.py::test_top_level_enrichment_modules_use_relative_sibling_imports` guarding that boundary while adapter modules remain out of scope for this slice. `app/enrichment/diagnostics.py` now owns cache, retry, latency, and error counter mutation helpers. `app/enrichment/dispatch_plan.py::build_dispatch_pairs()` now routes every adapter-support check through `append_supported_dispatch_pairs()` and uses direct empty/single/pair/three/four-IOC paths before the generic fan-out loop, preserving dispatch order while avoiding the outer IOC iterator for typical small Online submissions. `app/enrichment/diagnostics.py::build_dispatch_diagnostics()` now uses direct empty/single/pair/three/four-dispatch-pair paths and delegates provider bucket mutation to `record_dispatch_pair()`, while the long path passes dispatch-pair tuples through directly instead of destructuring and rebuilding them. `app/enrichment/diagnostic_state.py::apply_job_diagnostics_update()` now owns live job diagnostics lookup, malformed-state repair, provider bucket selection, and invoking a supplied counter helper, leaving `app/enrichment/orchestrator.py::_update_job_diagnostics()` as a lock/error-boundary wrapper. `app/routes/enrichment_status.py` now owns polling result serialization, status payload normalization, terminal payload shape, cursor coercion, live/terminal response selection, and not-found terminal reason membership, leaving `app/routes/enrichment_jobs.py` focused on Flask request handling, registry eviction, and background save flow. `app/routes/enrichment_status.py::evicted_terminal_status()` now owns the fixed evicted-job reason and error text used by route-level registry eviction, so `_register_orchestrator()` passes a status builder instead of rebuilding tombstone policy with a route-local lambda. `app/routes/enrichment_status.py::unknown_terminal_status()` now owns unknown-job reason/error tombstone policy for both missing orchestrators and missing incremental status records instead of rebuilding the same terminal payload in both response branches. `app/routes/enrichment_status.py::terminal_status_response()` now owns terminal/unknown response cursor alignment so `enrichment_status_response()` delegates missing-live-orchestrator responses instead of mutating terminal payloads inline. `app/routes/enrichment_status.py::live_status_response()` now owns live snapshot result serialization, payload normalization, and status-code selection so `enrichment_status_response()` only chooses between missing-live, missing-status, and live-status branches. `app/routes/enrichment_status.py::serialized_status_results()` now owns cached-marker extraction and result-tail serialization for live status snapshots, keeping marker-map lookup out of `live_status_response()`. `app/routes/enrichment_jobs.py::_get_enrichment_status()` now applies polling JSON responses through the shared `app/routes/json_results.py::apply_json_result()` helper instead of formatting `jsonify(result.payload), status` inline. `app/routes/enrichment_status.py::EnrichmentStatusResponse` now exposes the shared `status` field directly instead of retaining a route-local `status_code` compatibility field. `app/routes/enrichment_jobs.py` imports `enrichment_status` as the owner module and calls it directly instead of retaining a private status-payload alias. `app/routes/enrichment_jobs.py::__all__` now advertises only `get_orchestration_diagnostics_snapshot()` so route-private job state, executor, setup, and polling helpers are not exposed as wildcard-import public API. `app/routes/enrichment_jobs.py`, `app/routes/enrichment_history.py`, and `app/routes/api_health.py` import sibling route helpers through relative imports instead of round-tripping through the `app.routes` package facade. `app/routes/query_values.py::status_cursor_from_query()` now owns polling query cursor extraction before shared cursor coercion, so `_get_enrichment_status()` no longer reads individual query args directly. `app/routes/enrichment_job_registry.py::register_orchestrator_state()` now owns route-level live orchestrator registration, duplicate terminal cleanup, and bounded eviction tombstone pruning, so `enrichment_jobs.py::_register_orchestrator()` stays a locked state wrapper and `_setup_orchestrator()` no longer carries the OrderedDict mutation loops. `app/routes/enrichment_job_registry.py::registered_job_state()` now owns the locked live/terminal route-state lookup shared by status polling and orchestration diagnostics, so `app/routes/enrichment_jobs.py` no longer repeats `_orchestrators.get()`/`_terminal_jobs.get()` blocks in each route-facing accessor. `app/routes/enrichment_jobs.py::_build_enrichment_orchestrator()` now owns cache TTL lookup, configured-adapter selection, and `EnrichmentOrchestrator` construction through an explicit config-store factory, so `_setup_orchestrator()` stays focused on job id creation, registration, executor submission, and returning route-facing handles. `app/routes/enrichment_jobs.py::_resolve_orchestrator_runtime_dependencies()` now owns the explicit-registry/cache-or-current-app fallback boundary, and `_setup_orchestrator()` delegates to it before orchestrator construction, so accepted Online requests reuse already-known runtime dependencies without mixing Flask fallback resolution into lifecycle wiring. `app/routes/enrichment_jobs.py::_submit_enrichment_job()` now owns executor submission and background task argument ordering, so `_setup_orchestrator()` stays focused on lifecycle wiring instead of embedding thread-pool dispatch mechanics. Polling serialization now reads `status.get('cached_markers')` once per payload in `serialized_status_results()` and reuses the marker map for every tail result; long result tails share `app/routes/enrichment_status.py::append_serialized_result()` for serialize-and-append mutation while short-result fast paths up to four entries stay direct. Focused proof lives in `tests/test_routes.py::test_enrichment_status_reads_cached_markers_once_per_payload`, `test_serialize_results_shared_direct_accumulation`, `test_append_serialized_result_owns_long_path_mutation`, and `test_serialize_results_delegates_long_path_append`. That same status proof also guards `_get_enrichment_status()` against list-comprehension frames while serializing the returned tail. `_build_status_payload()` now delegates to `app/routes/enrichment_status.py::build_status_payload()`, which only falls back to `len(status['results'])` when `next_since` is missing, instead of evaluating that length while an explicit cursor is already present; focused proof lives in `tests/test_routes.py::test_status_payload_uses_explicit_next_since_without_measuring_results`. `_serialize_result()` delegates to `app/routes/enrichment_status.py::serialize_result()`, which skips per-result cache-key construction entirely when the cached-marker map is empty and reuses `app/enrichment/cache_payloads.py::cache_marker_key()` when a marker lookup is needed; `app/routes/enrichment_status.py::enrichment_status_response()` owns live-job, terminal tombstone, unknown-job, payload serialization, and status-code selection; focused proof lives in `tests/test_routes.py::test_serialize_result_skips_empty_cached_marker_map`, `test_serialize_result_reuses_cache_marker_key_helper`, `tests/test_routes.py::test_enrichment_jobs_delegates_status_payload_helpers` keeps the pure status helpers and response decision out of the Flask route module, `test_enrichment_status_owns_unknown_terminal_tombstone_shape` keeps unknown-job tombstone policy in one status helper, `test_enrichment_status_terminal_response_owns_cursor_alignment` keeps terminal cursor alignment out of the main resolver branch, `test_enrichment_status_live_response_owns_result_serialization` keeps live result serialization and status-code selection out of the main resolver branch while proving `serialized_status_results()` owns cached-marker extraction, `test_enrichment_status_route_delegates_response_decision` proves the route only supplies request/registry inputs and applies JSON/status output through a shared registry-state lookup, `test_registered_job_state_reads_live_and_terminal_under_lock` keeps the live/terminal lookup mechanics in the registry helper, and `test_enrichment_jobs_delegates_status_query_cursor` keeps direct query-arg reads out of the polling route body. Background history-save serialization now accumulates result and IOC payloads with direct loops instead of callback-style bulk mapping, and long IOC serialization shares `app/routes/ioc_payloads.py::_append_serialized_ioc()` while short IOC lists stay direct. `app/routes/enrichment_history.py::save_enrichment_history()` now owns the save-analysis payload construction, and `app/routes/enrichment_history.py::save_enrichment_status_history()` owns completed-status save/skip branching plus attempt/outcome diagnostics so `_run_enrichment_and_save()` stays focused on enrichment execution, status lookup, and the bounded failure/logging boundary without retaining route-local history-save helper bodies; focused proof lives in `tests/test_history_routes.py::TestEnrichmentSaveWrapper::test_save_serializes_results_and_iocs_with_direct_loops`, `test_serialize_iocs_delegates_long_path_append`, `test_history_save_status_helper_delegates_serialization_helper`, `test_run_enrichment_delegates_status_save_decision`, and `test_history_save_status_helper_records_skipped_without_persistence`. `app/enrichment/history_diagnostics.py` now owns history-save diagnostics state, coercion, attempt/outcome updates, and constructor-free mapping snapshots, while `app/routes/enrichment_diagnostics.py` stays focused on terminal snapshot copying and orchestration diagnostic export coercion without retaining a private mapping-copy alias. History-save diagnostic default, live, and terminal snapshots are now copied through one enrichment-owned direct helper instead of `dict(...)` or route-module state wrappers, with direct mapping-copy paths up to four keys; focused proof lives in `tests/test_history_routes.py::TestEnrichmentSaveWrapper::test_history_save_diagnostics_falls_back_to_safe_defaults`, which preserves malformed-state coercion while guarding the coercion/accessor functions against constructor-copying diagnostics and proving the route diagnostics module no longer owns the live history-save state, plus `tests/test_routes.py::test_route_snapshot_helpers_share_mapping_copy_contract`, which proves terminal tombstones use the same owner helper without a route-local `_copy_mapping()` wrapper. History-save timestamp presence and orchestration status string coercion now reuse shared whitespace helpers so timestamp checks avoid stripped-string allocation and status strings strip at most once when a normalized value is kept; the fixed history/status field groups and recordable outcome sets now live as owner-module constants instead of rebuilt tuple/set literals inside each coercion call; focused proof lives in `test_history_save_diagnostics_presence_checks_avoid_timestamp_strip`, `test_history_save_diagnostics_error_summary_strips_once`, and `tests/test_routes.py::test_orchestration_status_string_coercion_strips_once`. Polling terminal-status code selection now reuses the tuple-backed `_STATUS_NOT_FOUND_REASONS` membership table instead of allocating the `{'unknown', 'evicted'}` set on every status response; focused proof lives in `tests/test_routes.py::test_enrichment_status_not_found_reasons_use_static_membership_set`. Live orchestrator retention now runs through `app/enrichment/job_state.py::register_live_job()` and `app/enrichment/job_state.py::evict_oldest_jobs()` with tombstones routed through `app/enrichment/job_state.py::eviction_tombstone()` inside the enrichment core, so the old orchestrator-local `_evict_if_needed()` wrapper is gone. route-level live job registration delegates to `_register_orchestrator()` and then `app/routes/enrichment_job_registry.py::register_orchestrator_state()`, route-level evicted tombstone construction delegates to `app/routes/enrichment_status.py::evicted_terminal_status()`, orchestrator construction delegates to `_build_enrichment_orchestrator()` with runtime dependency fallback isolated in `_resolve_orchestrator_runtime_dependencies()`, background dispatch delegates to `_submit_enrichment_job()`, and diagnostic terminal tombstone snapshots copy terminal job state with direct key accumulation instead of `dict(_terminal_jobs.get(...))`; focused proof lives in `tests/test_orchestrator.py::TestLRUEviction::test_orchestrator_delegates_job_state_construction`, `tests/test_routes.py::test_setup_orchestrator_delegates_live_job_registration`, `test_enrichment_jobs_public_exports_exclude_route_private_state`, `test_route_job_registry_owns_live_orchestrator_retention`, `test_enrichment_status_owns_evicted_terminal_tombstone_shape`, `test_setup_orchestrator_delegates_orchestrator_construction`, `test_setup_orchestrator_accepts_explicit_runtime_dependencies`, `test_setup_orchestrator_delegates_background_submission`, and `tests/test_routes.py::test_orchestration_diagnostics_evicted_job_copies_terminal_snapshot_directly`. Malformed diagnostics provider maps are also coerced by scanning provider keys directly instead of allocating an `items()` view; focused proof lives in `tests/test_orchestrator.py::TestJobDiagnostics::test_get_diagnostics_falls_back_to_safe_defaults_for_malformed_state`. Focused update-helper proof lives in `tests/test_orchestrator.py::TestJobDiagnostics::test_orchestrator_delegates_diagnostic_update_helpers`, which keeps nested cache/retry/latency/error counter mutation closures out of the orchestrator while preserving aggregate and provider-scoped counters, and `test_orchestrator_delegates_diagnostic_state_repair`, which keeps malformed diagnostics repair and provider bucket construction out of the orchestrator wrapper. | R085, R087, R008, R010, R018, R019, D078, D079, D080 | `python3 tools/optimization_audit.py --milestone-id M017 --mode baseline`; `make verify-fast`; add `make verify-deep` for browser-visible polling changes | S03 shipped this path with measurement and code-path proof; preserve `total`, `done`, `complete`, `status`, `terminal`, `terminal_reason`, `error`, full-status result isolation, cached-marker snapshot isolation, shared cache-marker key formatting, `next_since`, failure tombstones, history-save diagnostics, diagnostic provider defaults/merging, dispatch-pair semantics, keyed-provider semaphore semantics, and redacted diagnostics without falling back to full result-list snapshots on polling, eagerly measuring retained results when an explicit cursor exists, constructor-copying cached markers or incremental result tails, allocating diagnostic provider items views, rebuilding live job-state dictionaries in the dispatch path, embedding dispatch-pair/semaphore setup loops in the orchestrator class, embedding semaphore acquire/release mechanics in the orchestrator class, embedding thread-pool dispatch mechanics in setup wiring or the orchestrator method body, rebuilding single-attempt cache/adapter/latency flow in the orchestrator method body, rebuilding diagnostics state repair/provider bucket construction in the orchestrator method body, rediscovering registry/cache/config-store dependencies during accepted Online setup, mixing registry/cache current-app fallback resolution into setup lifecycle wiring, rebuilding polling payload helper bodies or route-owned live/terminal status branching in the Flask route module, rebuilding route-local evicted tombstone policy lambdas, repeating locked live/terminal registry lookups in route-facing accessors, duplicating unknown-job tombstone construction across status response branches, mutating terminal polling cursors in the main response resolver branch, serializing live polling results in the main response resolver branch, reading cached-marker maps in the live response normalization helper, reading polling query args directly in the status route body, constructor-copying terminal tombstones, constructor-copying history-save diagnostic defaults/snapshots, keeping history-save diagnostic state in route modules, retaining route-local mapping-copy wrappers, retaining history-save persistence helper bodies in the job lifecycle route module, repeatedly stripping diagnostic status strings, rebuilding diagnostic/status field tuples, rebuilding orchestrator-local diagnostics counter mutation closures, or rebuilding terminal-not-found reason sets per response. |
| Keep S04's shipped frontend/render optimization on the shared result-application severity-change gate. | browser polling and result application | code-path reasoning + focused regression proof | S04 shipped the remaining high-confidence frontend/render follow-up by removing the duplicate broad `flush()` implementation in `app/static/src/ts/modules/result-application.ts` that always called `updateDashboardCounts()` and `sortCardsBySeverity()`. The active shared coordinator path now compares each dirty IOC's previous and computed verdict while flushing, so it only runs global dashboard recount/reorder calls when severity-affecting state changes. Focused proof lives in `app/static/src/ts/modules/result-application.test.ts`: provider-only/no-op deltas preserve summaries, provider rows, copy/detail affordances, and skip global recount/reorder, while severity-changing deltas still update counts and order. T02 verification also reran the full frontend suite plus mocked-online browser checks for results and EmailRep continuity. | R085, R086, R087, R088, R008, R009, R010, R019, D078, D079, D080 | `npm test -- --run app/static/src/ts/modules/result-application.test.ts`; `npm test -- --run`; `python3 -m pytest -q tests/e2e/test_results_page.py tests/e2e/test_emailrep_online.py`; add `make verify-deep` for broader browser-visible polling changes | S04 is no longer an unresolved target: preserve the severity-change gate, live/history shared coordinator parity, filtering/sorting/copy/export/detail links, textContent-safe DOM construction, visible enrichment progress/results state, browser-accessible history/detail pages, and deterministic mocked-online browser proof without exposing secrets. |
| Keep the shared result-application coordinator on cached per-IOC DOM handles before chasing broader flush-wide render work. | frontend/render | code-path reasoning + focused regression proof | `app/static/src/ts/modules/result-application.ts` now caches each IOC's card, summary row, context line, verdict label, enrichment slot, details panel, section handles, reputation-row count, no-data summary injection state, detail-link injection state, spinner wrapper, pending indicator, copy button, and provider-count total inside `createResultApplicationCoordinator()`. That lets `apply()`, `flush()`, and `finalize()` reuse stable DOM references instead of repeating `findCardForIoc()` and `.querySelector('.ioc-summary-row')`/`.querySelector('.ioc-context-line')`/`.querySelector('.verdict-label')`/`.querySelector('.enrichment-details')`/`.querySelector('.spinner-wrapper')`/`.querySelector('.enrichment-waiting-text')`/`.querySelector('.no-data-summary-row')` work. Focused proof lives in `app/static/src/ts/modules/result-application.test.ts::caches stable IOC handles and provider counts across repeated apply, flush, and finalize calls`, which verifies repeated same-IOC results do not repeat summary-row, context-line, verdict-label, spinner, or pending-indicator lookups, finalization does not add another details-panel lookup, and finalization skips the old no-data summary guard query; `skips reputation detail-row sorting while an IOC has only one reputation row`; `app/static/src/ts/modules/row-factory.test.ts::reuses cached summary row and details handles when provided`; and `CTX-01: reuses a cached context line when provided`. `finalize walks cached IOC values without allocating a Map values iterator` also patches `Map.prototype.values` to fail while preserving detail-link injection, avoiding a `Map.values()` iterator, and proving repeated finalization does not repeat `.detail-link-footer` lookup. | R008, R009, R010, R019, R040 | `npx vitest run app/static/src/ts/modules/result-application.test.ts app/static/src/ts/modules/row-factory.test.ts`; `make verify-fast` | The shipped coordinator-local cache retired repeated card/summary-row/context-line/verdict-label/slot/details/spinner/pending-indicator/no-data-summary lookups. Preserve live/history parity, textContent-only DOM construction, expand toggles, export/copy/detail-link wiring, severity-change gating, and dynamic summary/no-data behavior while avoiding repeated coordinator-local DOM rediscovery. |
| Keep live polling progress updates on cached progress element handles. | browser polling and result application | DOM lookup proof + focused regression proof | `app/static/src/ts/modules/enrichment.ts::init()` now captures the progress fill/text elements once for a live results page and passes those handles into `updateProgressBar()` on each poll, instead of re-querying the same IDs for every status payload. Focused proof lives in `app/static/src/ts/modules/enrichment.test.ts`, where `document.getElementById` is spied during a running poll and the progress fill/text IDs are each read only once while the progress text still updates. | R008, R010, R019, R040 | `npx vitest run app/static/src/ts/modules/enrichment.test.ts`; `make verify-fast`; add `make verify-deep` for broader browser-visible polling changes | Preserve live-owner gating, polling cadence, progress text/fill behavior, retry/terminal failure handling, export enablement, and shared result application while avoiding repeated progress-node DOM lookups per poll. |
| Keep summary-row expand toggles on cached details-panel lookups. | frontend/render | code-path reasoning + focused regression proof | `app/static/src/ts/modules/enrichment.ts::wireExpandToggles()` now keeps a per-wired-root `WeakMap` from `.ioc-summary-row` to its `.enrichment-details` panel, so repeated click/keyboard toggles reuse the details handle instead of rediscovering the slot and querying the panel every time. Focused proof lives in `app/static/src/ts/modules/enrichment.test.ts::caches details panel lookups across repeated summary-row toggles`, which toggles the same summary row twice and verifies the slot performs only one `.enrichment-details` lookup while preserving aria-expanded and open/closed state. | R008, R009, R010, R019, R040 | `npx vitest run app/static/src/ts/modules/enrichment.test.ts`; `make verify-fast` | Preserve delegated click/keyboard expand behavior, dynamic summary-row support for live/history rendering, aria-expanded updates, and details-panel open state while avoiding repeated per-toggle DOM rediscovery. |
| Keep live warning banners on a cached warning element handle. | frontend/render | DOM lookup proof + focused regression proof | `app/static/src/ts/modules/enrichment.ts::init()` now captures `#enrich-warning`, `#enrich-progress`, `#export-btn`, and `#export-dropdown` once for the live results page, reuses the already-cached progress text handle, and passes those handles into provider warning, terminal-warning, completion, terminal-failure, and `initExportButton()` rendering. Focused proof lives in `app/static/src/ts/modules/enrichment.test.ts::reuses the warning banner handle across repeated provider warnings`, which returns two warning-producing provider errors in one complete payload, verifies `document.getElementById('enrich-warning')` and `document.getElementById('enrich-progress')` are each called once, verifies `#export-btn` and `#export-dropdown` are each looked up once during init-time export wiring, and verifies completion does not add another `export-btn` lookup while the final warning remains visible. | R008, R009, R010, R019, R040 | `npx vitest run app/static/src/ts/modules/enrichment.test.ts`; `make verify-fast` | Preserve rate-limit/auth warning copy, terminal failure banners, live-owner polling behavior, and export/progress completion while avoiding repeated warning/progress/export DOM lookups per payload. |
| Keep summary-row cached timestamp selection on a single-pass oldest lookup. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/row-factory.ts::oldestCachedAt()` now finds the oldest cached timestamp in one pass instead of filtering, mapping, and sorting cached entries. Focused proof lives in `app/static/src/ts/modules/row-factory.test.ts`, where the cached timestamp helper returns the oldest value while `Array.prototype.sort` is patched to fail. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/row-factory.test.ts`; `make verify-fast` | Preserve staleness badge semantics, textContent-only DOM construction, and summary-row rendering while avoiding unnecessary cached-entry array work. |
| Keep inline context snippet formatting on direct string construction. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/row-factory.ts::formatAsnContext()` and `formatDnsAContext()` now build ASN and DNS inline context strings directly instead of using temporary arrays with `push()`, `slice()`, `filter()`, and `join()`. `CONTEXT_PROVIDERS` now derives from one readonly context-provider name list instead of carrying the provider list inline at the exported set construction site. Focused proof lives in `app/static/src/ts/modules/row-factory.test.ts`, where context formatting still preserves ASN/prefix and first-three-string DNS behavior while `Array.prototype.slice`, `Array.prototype.filter`, and `Array.prototype.join` are patched to fail, and the context-provider set source is guarded against inline exported set construction. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/row-factory.test.ts app/static/src/ts/modules/result-application.test.ts`; `make verify-fast` | Preserve inline IOC card context text, IP Context priority over ASN Intel, DNS A-record first-three-string semantics, context-provider membership, context-provider ordering, and textContent-safe DOM updates while avoiding temporary context arrays and duplicated/inline context-provider list ownership. |
| Keep no-data summary insertion on direct child scanning. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/row-factory.ts::injectSectionHeadersAndNoDataSummary()` now scans direct no-data section children once, counting no-data rows and remembering the first insertion anchor instead of allocating a `querySelectorAll()` NodeList. Focused proof lives in `app/static/src/ts/modules/row-factory.test.ts`, where the no-data section's `querySelectorAll()` is patched to fail while summary count text and insertion still work. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/row-factory.test.ts app/static/src/ts/modules/result-application.test.ts app/static/src/ts/modules/history.test.ts`; `make verify-fast` | Preserve no-data summary text, insertion before the first no-data row, click/keyboard expansion, accessibility attributes, and live/history rendering while avoiding per-slot NodeList allocation. |
| Keep summary attribution provider selection on a single-pass best-candidate scan. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/verdict-compute.ts::computeAttribution()` now chooses the highest-detail provider with a single scan instead of filtering candidates and sorting a copied array. Focused proof lives in `app/static/src/ts/modules/verdict-compute.test.ts`, where attribution still honors total-engine priority and severity tie-breaks while `Array.prototype.sort` is patched to fail. | R008, R010, R040 | `npx vitest run app/static/src/ts/modules/verdict-compute.test.ts app/static/src/ts/modules/row-factory.test.ts`; `make verify-fast` | Preserve summary attribution text, no-data/error exclusion, total-engine priority, and severity tie-break behavior while avoiding candidate array allocation and sorting. |
| Keep summary worst-verdict computation on a single-pass scan. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/verdict-compute.ts::computeWorstVerdict()` now handles `known_good` override and severity tracking in one pass instead of doing an upfront `some()` scan before `findWorstEntry()`. Focused proof lives in `app/static/src/ts/modules/verdict-compute.test.ts`, where worst-verdict selection still preserves known-good override and malicious severity while `Array.prototype.some` is patched to fail. | R008, R010, R040 | `npx vitest run app/static/src/ts/modules/verdict-compute.test.ts app/static/src/ts/modules/row-factory.test.ts`; `make verify-fast` | Preserve known-good override, no-data fallback, and severity ordering while avoiding the extra pre-scan on every summary verdict computation. |
| Keep exported worst-entry lookup on cached severity and malicious short-circuiting. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/verdict-compute.ts::findWorstEntry()` now caches the current worst severity instead of recomputing it each iteration, and returns immediately once `malicious` is found. Focused proof lives in `app/static/src/ts/modules/verdict-compute.test.ts`, where a later row throws if inspected after a malicious verdict. | R008, R010, R040 | `npx vitest run app/static/src/ts/modules/verdict-compute.test.ts app/static/src/ts/modules/row-factory.test.ts`; `make verify-fast` | Preserve exported worst-entry behavior, first-entry tie behavior, and severity ordering while avoiding redundant severity lookups after the maximum verdict is known. |
| Keep result-application severity detection on per-dirty IOC comparison. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/result-application.ts::flush()` now accumulates a severity-changed flag from `flushIoc()` instead of building before/after arrays from whole-grid `.ioc-card` queries. Focused proof lives in `app/static/src/ts/modules/result-application.test.ts`, where a severity-changing flush still triggers dashboard recount/reorder while `Document.prototype.querySelectorAll` records no `.ioc-card` snapshot scan from the coordinator. | R008, R009, R010, R019, R040 | `npx vitest run app/static/src/ts/modules/result-application.test.ts`; `make verify-fast` | Preserve the severity-change gate, live/history shared coordinator parity, and dashboard recount/reorder behavior while avoiding two whole-grid verdict snapshots on every dirty flush. |
| Keep provider-count metadata parsing cached by raw DOM value. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/types/ioc.ts::getProviderCounts()` now caches parsed `data-provider-counts` metadata by raw attribute value, so repeated coordinator setup for the same results surface does not re-run `JSON.parse()`. `verdictSeverityIndex()` still uses the shared prebuilt severity map, but that map is now populated with an indexed loop instead of allocating `VERDICT_SEVERITY.map(...)` entries at module load. Focused proof lives in `app/static/src/ts/types/ioc.test.ts`, where repeated reads parse once, changed raw metadata reparses, malformed metadata fallback is cached for the same raw value, and severity ordering is guarded against array-map lookup construction. | R008, R010, R040 | `npx vitest run app/static/src/ts/types/ioc.test.ts app/static/src/ts/modules/result-application.test.ts`; `make verify-fast` | Preserve provider-count fallback values, dynamic DOM metadata overrides, malformed JSON fallback behavior, result-application pending-count semantics, and verdict severity ordering while avoiding repeated provider-count JSON parsing for unchanged metadata and module-load severity entry mapping. |
| Keep provider detail-row sorting on cached severity keys. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/shared-rendering.ts::sortDetailRows()` now decorates each provider row with its severity once before sorting instead of reading `data-verdict` and recomputing severity inside every comparator call. The decoration pass now walks the row NodeList with an indexed loop instead of allocating through `Array.from(...).map(...)`. Focused proof lives in `app/static/src/ts/modules/shared-rendering.test.ts`, where four rows sort into severity order with exactly four `data-verdict` reads, and row sorting still works when `Array.from` is patched to fail. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/shared-rendering.test.ts app/static/src/ts/modules/result-application.test.ts app/static/src/ts/modules/history.test.ts`; `make verify-fast` | Preserve provider detail-row severity order and live/history shared rendering behavior while avoiding comparator-amplified DOM attribute reads and extra row decoration array passes. |
| Keep export dropdown actions on one delegated listener. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/shared-rendering.ts::initExportButton()` now installs one delegated click handler on the export dropdown instead of querying `[data-export]` buttons and attaching one listener per action. Focused proof lives in `app/static/src/ts/modules/shared-rendering.test.ts`, where the dropdown's `querySelectorAll()` is patched to fail while nested JSON, CSV, and copy-all clicks still call the expected export helpers. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/shared-rendering.test.ts app/static/src/ts/modules/result-application.test.ts app/static/src/ts/modules/history.test.ts`; `make verify-fast` | Preserve live/history export wiring, dropdown close behavior, nested button clicks, JSON/CSV downloads, and copy-all IOC behavior while avoiding per-action listener attachment. |
| Keep IOC card sorting on cached severity keys. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/cards.ts::sortCardsBySeverity()` now decorates each IOC card with its severity once before sorting instead of reading `data-verdict` and recomputing severity inside every comparator call. The decoration pass now walks the card NodeList with an indexed loop instead of allocating through `Array.from(...).map(...)`, and the final append pass also uses an indexed loop instead of array callback iteration. Focused proof lives in `app/static/src/ts/modules/cards.test.ts`, where four cards sort into severity order with exactly four `data-verdict` reads after the debounced sort fires, card sorting still works when `Array.from` is patched to fail, and the module source is guarded against `.forEach(` regressions. | R008, R010, R040 | `npx vitest run app/static/src/ts/modules/cards.test.ts app/static/src/ts/modules/result-application.test.ts`; `make verify-fast` | Preserve dashboard card ordering and the debounced sort behavior while avoiding comparator-amplified DOM attribute reads and extra card decoration array passes. |
| Keep card verdict label updates on the shared classList helper. | browser polling and result application | refactor proof + focused regression proof | `app/static/src/ts/modules/cards.ts::applyCardVerdict()` now centralizes card `data-verdict`, label class, and label text updates for both direct card updates and shared result application. The helper uses `classList.remove()`/`classList.add()` for known verdict classes instead of rebuilding `className` with `split().filter().join()`. Focused proof lives in `app/static/src/ts/modules/cards.test.ts`, where verdict-label updates preserve unrelated classes and assert the classList calls. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/cards.test.ts app/static/src/ts/modules/result-application.test.ts`; `make verify-fast` | Preserve card data-verdict updates, visible verdict labels, unrelated CSS classes, and shared live/history result-application behavior while removing duplicated string-based class rebuilding. |
| Keep filter applications on cached static node lists. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/filter.ts::init()` now captures the static IOC cards, verdict buttons, and type pills once instead of re-querying those node lists on every verdict/type/search filter application. Filter setup and application now walk those NodeLists with indexed loops instead of `NodeList.prototype.forEach()` callbacks. Focused proof lives in `app/static/src/ts/modules/filter.test.ts`, where verdict click, type click, dashboard-badge click, and debounced search filtering preserve visible card and active-control behavior without additional `querySelectorAll` calls after init, and with `NodeList.prototype.forEach` patched to fail. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/filter.test.ts app/static/src/ts/modules/enrichment.test.ts app/static/src/ts/modules/history.test.ts`; `make verify-fast` | Preserve verdict, type, search, and dashboard filter behavior while avoiding repeated static selector scans and callback iteration during every filter application. |
| Keep card stagger initialization on indexed NodeList iteration. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/ui.ts::initCardStagger()` now applies capped `--card-index` values with an indexed `NodeList` loop instead of allocating a `forEach()` callback during page-load UI setup. Focused proof lives in `app/static/src/ts/modules/ui.test.ts`, where stagger indexes still cap at 15 while `NodeList.prototype.forEach` is patched to fail, and scroll-aware filter bar behavior remains covered. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/ui.test.ts app/static/src/ts/modules/main.test.ts`; `make verify-fast` | Preserve card stagger CSS variable values, the 15-index cap, scroll-aware filter-bar class toggling, and main page-load initialization while avoiding callback iteration for static card setup. |
| Keep copy-button handling on one delegated listener. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/clipboard.ts::init()` now installs one document-level delegated click handler instead of attaching a click listener to every `.copy-btn` at initialization. Focused proof lives in `app/static/src/ts/modules/clipboard.test.ts`, where one listener handles an existing nested click target plus a later-added copy button, and repeated `init()` calls do not duplicate clipboard writes. | R008, R009, R010, R040, R085 | `npx vitest run app/static/src/ts/modules/clipboard.test.ts app/static/src/ts/modules/export.test.ts app/static/src/ts/modules/result-application.test.ts`; `make verify-fast` | Preserve copy text, enrichment suffixes, nested button clicks, copied feedback, export clipboard reuse, and dynamic result/history buttons while avoiding per-button listener attachment. |
| Keep form initialization on shared element lookups. | browser polling and result application | refactor proof + focused regression proof | `app/static/src/ts/modules/form.ts::init()` now gathers the index form's textarea, submit button, clear button, and mode-toggle nodes once and passes them into submit, auto-grow, and mode-toggle helpers instead of letting each helper repeat document lookups. Focused proof lives in `app/static/src/ts/modules/form.test.ts`, where form mode, submit enablement, and missing-markup behavior remain covered while `document.querySelector()` calls for `#ioc-text`, `#submit-btn`, and `#mode-input` are each asserted once. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/form.test.ts app/static/src/ts/modules/main.test.ts`; `make verify-fast` | Preserve submit enablement, clear behavior, textarea auto-grow, paste feedback, mode toggle state synchronization, and missing-markup fail-fast behavior while avoiding repeated static form selector lookups. |
| Keep settings accordion updates on cached header references. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/settings.ts::initAccordion()` now caches each settings section's accordion header during initialization and reuses those references when expanding a section. Accordion record setup and expansion now use indexed loops instead of array `forEach()` callbacks. Focused proof lives in `app/static/src/ts/modules/settings.test.ts`, where accordion clicks preserve one-open-section behavior without any post-init `querySelector()` calls from the click path, API key visibility toggling remains covered, and the module source is guarded against `.forEach(` regressions. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/settings.test.ts app/static/src/ts/modules/main.test.ts`; `make verify-fast` | Preserve settings accordion aria-expanded state, one-open-provider behavior, and API key show/hide toggles while avoiding repeated section-header selector work and callback iteration on every accordion expansion. |
| Keep settings initialization on one section query. | browser polling and result application | refactor proof + focused regression proof | `app/static/src/ts/modules/settings.ts::init()` now queries `.settings-section` once and passes that NodeList to accordion and API key toggle setup instead of querying provider sections and all sections separately. Settings section setup now walks that NodeList with indexed loops instead of iterator/callback-based passes. Focused proof lives in `app/static/src/ts/modules/settings.test.ts`, where accordion and API key toggles still work while `document.querySelectorAll()` observes exactly one `.settings-section` query, no `.settings-section[data-provider]` query, and the module source is guarded against `.forEach(` regressions. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/settings.test.ts app/static/src/ts/modules/main.test.ts`; `make verify-fast` | Preserve settings accordion behavior, provider-only accordion wiring, API key show/hide toggles, and main init dispatch while avoiding duplicated settings-section selector scans and callback iteration. |
| Keep dashboard verdict-count updates on one count-element query. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/cards.ts::updateDashboardCounts()` now scans `[data-verdict-count]` elements once instead of issuing one dashboard `querySelector()` per verdict bucket. It also checks valid dashboard verdict names through a precomputed set instead of scanning the static verdict tuple for every count element. The card and count-element NodeLists are walked with indexed loops instead of `NodeList.prototype.forEach()` callbacks on each recount. Focused proof lives in `app/static/src/ts/modules/cards.test.ts`, where dashboard counts update correctly while the dashboard performs one count-element query, no per-verdict `querySelector()` calls, no `Array.prototype.includes()` membership scans, and `NodeList.prototype.forEach` is patched to fail. | R008, R010, R040 | `npx vitest run app/static/src/ts/modules/cards.test.ts app/static/src/ts/modules/result-application.test.ts`; `make verify-fast` | Preserve dashboard count semantics for malicious, suspicious, clean, known_good, and no_data while reducing repeated selector, membership-scan, and callback-iteration work during recounts. |
| Keep frontend export text construction on direct string accumulation. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/export.ts::buildCSV()` now builds the CSV output directly instead of pushing every row into an intermediate array and joining it after the loop. The static CSV header is now a literal string instead of a module-load columns-array join, and CSV raw-stat array fields are also formatted without `Array.prototype.join()`. `buildIocListText()` and `buildIocListTextFromResults()` also build copy-all IOC text directly while sharing one deduplicated append helper, and `copyAllIOCs()` now accepts the accumulated export results from `initExportButton()` so the live/history dropdown copy action does not rescan `.ioc-card` DOM. Focused proof lives in `app/static/src/ts/modules/export.test.ts`, where the source is guarded against `CSV_COLUMNS.join`, duplicated copy-text append branches, CSV formatting and IOC copy text still preserve behavior while `Array.prototype.push` and `Array.prototype.join` are patched to fail, and result-backed IOC copy text is guarded against `document.querySelectorAll()` scans. | R008, R010, R040 | `npx vitest run app/static/src/ts/modules/export.test.ts app/static/src/ts/modules/history.test.ts app/static/src/ts/modules/enrichment.test.ts`; `make verify-fast` | Preserve CSV column order, result-only export behavior, raw-stat extraction, quote escaping, copy-all deduplication, and live/history export wiring while avoiding module-load header joins, row-array, array-field join, IOC-value array allocations, duplicated copy-text append branches, and copy-action card DOM scans proportional to exported results. |
| Keep IOC detail graph setup on single-pass node indexing. | browser polling and result application | code-path reasoning + focused regression proof | `app/static/src/ts/modules/graph.ts::renderRelationshipGraph()` now splits IOC/provider nodes in one pass and builds the provider index map directly instead of using `filter()`, `find()`, and `providerNodes.map()` setup passes. Provider node drawing also uses indexed iteration instead of callback iteration, literal empty graph-node payloads now return the empty state before calling `JSON.parse()`, and literal empty graph-edge payloads skip edge JSON parsing while preserving SVG rendering. Focused proof lives in `app/static/src/ts/modules/graph.test.ts`, where the renderer still creates the SVG graph and empty state while `Array.prototype.filter`, `Array.prototype.find`, `Array.prototype.map`, and `Array.prototype.forEach` are patched to fail, where an empty-node graph payload patches `JSON.parse` to fail while preserving the empty state, and where an empty-edge graph payload makes `JSON.parse('[]')` fail while still rendering a graph with zero edge lines. | R008, R009, R010, R040 | `npx vitest run app/static/src/ts/modules/graph.test.ts`; `make verify-fast` | Preserve IOC detail relationship graph rendering, text-node-safe labels, malformed-data empty state, empty graph-node payload empty state, empty graph-edge payload rendering, and provider edge coloring while avoiding extra graph-node array passes, callback iteration during provider drawing, and empty graph-node/edge JSON parsing. |
| Keep browser-route provider-count metadata on the direct count path. | request/status | code-path reasoning + focused regression proof | `app/routes/provider_metadata.py::provider_counts_json()` now builds result-page provider-count metadata with a direct accumulator and `registry.provider_count_for_type()` instead of allocating a dict-comprehension frame or provider lists with `providers_for_type()` for every IOC type. `app/enrichment/registry.py::ProviderRegistry.registered_count()` returns the registered provider count directly, and `app/routes/provider_metadata.py::provider_coverage()` now uses that count with the caller's configured-provider list instead of copying all registered providers for coverage metadata. `app/routes/provider_metadata.py::enrichable_count()` keeps the progress-total helper available while `app/routes/analysis.py` stays focused on Flask request handling, Online admission, and template rendering without re-exporting provider metadata compatibility aliases. Focused proof lives in `tests/test_routes.py::test_provider_counts_metadata_uses_direct_count_path`, which fails if provider-count metadata falls back to `providers_for_type()` or a dict-comprehension frame, `tests/test_routes.py::test_provider_coverage_reuses_configured_provider_list`, which fails if coverage allocates all providers or rereads configured providers, `tests/test_routes.py::test_analysis_route_delegates_provider_metadata_helpers`, which keeps provider metadata helper bodies and stale compatibility aliases out of the route module, and `tests/test_provider_registry.py::TestRegistryRegister::test_registered_count_does_not_allocate_provider_list`, which fails if the direct count path delegates to `all()`. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_routes.py::test_provider_counts_metadata_uses_direct_count_path tests/test_routes.py::test_provider_coverage_reuses_configured_provider_list tests/test_routes.py::test_analyze_online_with_api_key_returns_job_id tests/test_routes.py::test_enrichable_count_multi_provider tests/test_routes.py::test_enrichable_count_domain_two_providers tests/test_provider_registry.py`; `make verify-fast` | Preserve result-page provider-count JSON, enrichable progress counts, provider coverage metadata, and online admission behavior while avoiding count-only provider list allocation, registered-provider list copies for coverage counts, and dict-comprehension frame creation on the fixed IOC-type metadata walk. |
| Keep Online fanout admission diagnostics on the direct count path. | request/status | code-path reasoning + focused regression proof | `app/routes/online.py::_online_fanout_diagnostics()` now caches `registry.provider_count_for_type()` per IOC type while computing dispatch limits instead of allocating provider lists with `providers_for_type()`. `app/routes/online.py::_online_admission()` centralizes configured-provider lookup, Online admission limit reads, and fan-out diagnostics while accepting route-supplied registry and limit inputs, so HTML and JSON analyze routes do not duplicate the same admission decision or hide Flask-global registry reads inside the admission helper. `app/routes/analysis.py::analyze()` and `app/routes/api.py::api_analyze()` now pass `current_app.config` into `_online_limits_from_config()` explicitly, so route-level Online limit wiring does not rely on the helper rediscovering Flask config during normal browser/API requests. `_resolve_online_limit_config()` now owns the remaining direct-call current-app fallback boundary, leaving `_online_limits_from_config()` focused on parsing the two Online admission limits. `app/routes/online.py::_log_online_limit_rejection()` now owns the secret-free Online limit warning format and diagnostic field reads, so HTML and JSON routes no longer duplicate admission-guard logging mechanics. `app/routes/analysis_workflow.py::start_online_analysis()` now shares the Online start decision between HTML and JSON routes while receiving registry and online-limit inputs from the route-facing workflow, so each route formats its own rejection/success response without duplicating admission and launch control flow. Focused proof lives in `tests/test_routes.py::test_online_fanout_diagnostics_uses_direct_count_path`, which verifies duplicate IOC types reuse one count lookup and fail if diagnostics call `providers_for_type()`, `tests/test_routes.py::test_online_admission_centralizes_provider_and_limit_decision` and `test_online_admission_short_circuits_when_no_providers`, which pin the shared admission boundary and prove `_online_admission()` no longer reaches into `current_app.registry`, `test_online_limits_config_resolution_is_named_boundary`, which keeps the current-app config fallback out of the Online limit parser, `test_online_limit_rejection_logging_is_shared`, which keeps Online limit warning field reads out of the HTML/API route bodies, `tests/test_routes.py::test_browser_analyze_uses_shared_intake_workflow` and `tests/test_api.py::TestApiAnalyzeValidation::test_api_analyze_uses_shared_intake_workflow`, which guard route-supplied registry/config/limit forwarding through the shared workflow boundary, plus `tests/test_routes.py::test_analyze_online_uses_shared_limit_config_helper` and `tests/test_api.py::TestApiAnalyzeOnline::test_online_uses_shared_limit_config_helper`, which prove both route surfaces call the shared limit helper with `current_app.config` while preserving rejection behavior. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_routes.py::test_online_fanout_diagnostics_uses_direct_count_path tests/test_routes.py::test_online_admission_centralizes_provider_and_limit_decision tests/test_routes.py::test_online_admission_short_circuits_when_no_providers tests/test_routes.py::test_analyze_online_rejects_ioc_limit_before_launch tests/test_routes.py::test_analyze_online_uses_shared_limit_config_helper tests/test_routes.py::test_analyze_online_rejects_dispatch_limit_before_launch tests/test_api.py::TestApiAnalyzeOnline::test_online_with_provider tests/test_api.py::TestApiAnalyzeOnline::test_online_rejects_ioc_limit_before_launch tests/test_api.py::TestApiAnalyzeOnline::test_online_uses_shared_limit_config_helper tests/test_api.py::TestApiAnalyzeOnline::test_online_rejects_dispatch_limit_before_launch tests/test_provider_registry.py`; `make verify-fast` | Preserve Online IOC and dispatch limit diagnostics, secret-free admission responses, HTML/API limit parity, and background-work rejection behavior while avoiding count-only provider list allocation, duplicated online-limit config parsing, duplicated configured-provider admission reads, hidden Flask-global reads in the admission helper, hidden route-level Flask config rediscovery for normal Online requests, parser-owned Flask config fallback resolution, and duplicated route-local admission-guard warning formatting. |
| Keep browser-route enrichable progress totals on cached provider counts by IOC type. | request/status | code-path reasoning + focused regression proof | `app/routes/provider_metadata.py::enrichable_count()` now caches `registry.provider_count_for_type()` results by IOC type while summing progress totals, so repeated IOC types do not repeat the same registry count after admission succeeds. The browser online route now reuses `_online_fanout_diagnostics()`' already-computed `dispatch_count` for the template `enrichable_count`, avoiding a second progress-total pass after admission. Focused proof lives in `tests/test_routes.py::test_enrichable_count_caches_provider_counts_by_ioc_type`, which verifies two IPv4 IOCs and one domain still produce the per-IOC fanout total while calling the registry count path once per type and never allocating provider lists, `tests/test_routes.py::test_analysis_route_delegates_provider_metadata_helpers`, which keeps that pure helper out of `app/routes/analysis.py`, and `test_analyze_online_reuses_fanout_dispatch_count_for_progress_total`, which verifies the route progress total comes from the admission fanout. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_routes.py::test_enrichable_count_caches_provider_counts_by_ioc_type tests/test_routes.py::test_analyze_online_reuses_fanout_dispatch_count_for_progress_total tests/test_routes.py::test_analyze_online_with_api_key_returns_job_id tests/test_routes.py::test_enrichable_count_multi_provider tests/test_routes.py::test_enrichable_count_domain_two_providers`; `make verify-fast` | Preserve browser online progress totals, per-IOC fanout semantics, background enrichment launch behavior, provider metadata, and online admission guard behavior while avoiding repeated same-type provider-count scans and the post-admission recount. |
| Keep online route configured-provider reads single-use across admission, coverage, and launch. | request/status | code-path reasoning + focused regression proof | `app/routes/analysis_workflow.py::start_online_analysis()` now stores the configured-provider list read for the online admission gate and passes it through `_setup_orchestrator()`, while the browser route reuses that same admission list for `_provider_coverage()` instead of calling `registry.configured()` again. The browser and API online routes also skip provider configuration, admission diagnostics, and background launch setup when extraction returns zero IOCs. Focused proof lives in `tests/test_routes.py::test_provider_coverage_reuses_configured_provider_list`, `test_analyze_online_creates_all_three_adapters`, and `tests/test_api.py::TestApiAnalyzeOnline::test_online_with_provider`, which preserve launch behavior while proving configured providers are read once on the accepted route path; `tests/test_routes.py::test_analyze_online_no_iocs_skips_enrichment_setup` and `tests/test_api.py::TestApiAnalyzeOffline::test_online_no_iocs_skips_enrichment_setup` prove zero-IOC online requests do not touch provider setup. | R008, R010, R014, R040, R085, R087 | `python3 -m pytest -q tests/test_routes.py::test_provider_coverage_reuses_configured_provider_list tests/test_routes.py::test_analyze_online_creates_all_three_adapters tests/test_routes.py::test_analyze_online_no_iocs_skips_enrichment_setup tests/test_api.py::TestApiAnalyzeOnline::test_online_with_provider tests/test_api.py::TestApiAnalyzeOffline::test_online_no_iocs_skips_enrichment_setup`; `make verify-fast` | Preserve no-provider rejection, Online admission limits, browser provider coverage metadata, API/browser background launch behavior, adapter list identity, and no-results responses while avoiding repeated configured-provider list allocation per accepted online request and zero-work online setup. |
| Keep API health registry detail on direct registry count paths. | request/status | code-path reasoning + focused regression proof | `app/enrichment/registry.py::ProviderRegistry.configured_count()` now counts configured providers directly, and `app/routes/api_health.py::registry_health_detail()` combines `registry.configured_count()` with `registry.registered_count()` instead of allocating registered or configured provider lists for `/api/health` count-only metadata. `app/routes/api_health.py::build_api_health_checks()` now owns cache/history/registry dependency probing and secret-free degraded check rows from explicit cache/history/registry/logger dependencies instead of a whole Flask app object. `app/routes/api_health.py::api_health_result()` now owns health contract payload/status construction with the shared `app/routes/json_results.py::JsonResult`, and `api_health_route_response()` applies it through `app/routes/json_results.py::apply_json_result()` without a route-specific result or response-applier alias, leaving `app/routes/api.py::api_health()` as explicit current-app dependency wiring plus one route-response helper call. Focused proof lives in `tests/test_api.py::TestApiHealth::test_health_touches_only_aggregate_provider_configuration`, which preserves the health detail text while failing if the route asks for registered or configured provider lists, `tests/test_api.py::TestApiHealth::test_api_health_route_delegates_dependency_probe_helpers`, which keeps dependency probing, health payload construction, result construction, and JSON application out of the route body while proving explicit cache/history/registry/logger forwarding and no whole-app health helper input, `test_api_health_result_owns_contract_payload_and_json_application`, which preserves health payload/status formatting and route-response application in the helper module and fails if route-specific health result or response-applier aliases return, plus `tests/test_provider_registry.py::TestRegistryConfigured::test_configured_count_does_not_allocate_provider_list`, which fails if the direct configured count delegates to `configured()`. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_api.py::TestApiHealth tests/test_provider_registry.py::TestRegistryConfigured::test_configured_count_does_not_allocate_provider_list`; `make verify-fast` | Preserve `/api/health` schema, secret-free degraded dependency handling, registry detail text, and health payload ordering while avoiding provider-list allocations for count-only registry health metadata, route-owned health dependency probing, whole-app health helper inputs, route-owned health payload construction, route-owned health result construction, duplicated health/analyze JSON result containers, duplicated health/analyze JSON response application, and route-owned health JSON response application. |
| Keep health payload ordering and validation key sets precomputed. | request/status | code-path reasoning + focused regression proof | `app/health_contract.py::HEALTH_CHECK_ORDER` now defines the stable cache/history/registry check order as a literal tuple, and `build_health_payload()` iterates that tuple instead of sorting `HEALTH_CHECKS` on every health request or paying import-time sorted-list allocation. `HEALTH_PAYLOAD_KEYS`, `HEALTH_CHECK_VALUE_KEYS`, and `HEALTH_STATUSES` also let build/validation paths reuse precomputed frozensets instead of allocating validation or allowed-status sets per call. Validation now reuses `HEALTH_CHECK_ORDER` for per-check validation instead of creating a `checks.values()` view after the exact key set is already known. `app/text_utils.py::has_non_whitespace()` gives health and analyze routes a shared direct scanner for string presence checks, and `build_health_payload()` uses it instead of allocating stripped detail strings for every non-empty dependency detail. Focused proof lives in `tests/test_api.py::TestApiHealth::test_health_contract_constants_stay_secret_free`, which guards the literal order and tuple-backed status-set shape, plus `test_health_payload_uses_precomputed_check_order`, `test_health_payload_validation_uses_precomputed_key_sets`, `test_health_payload_uses_precomputed_status_set`, and `test_health_payload_detail_presence_skips_strip_allocation`, which patch `builtins.sorted` and `builtins.set`, make `checks.values()` fail, guard against status set-literal regressions, and make detail `.strip()` fail while preserving the secret-free health payload schema and whitespace-only fallback. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_api.py::TestApiHealth`; `make verify-fast` | Preserve `/api/health` schema, check names, deterministic check order, degraded status handling, whitespace-only detail fallback, and secret-free error details while avoiding import-time/request-time sorting, validation set allocation, allowed-status set allocation, values-view scans, and stripped-string allocation on health payload paths. |
| Keep shared contract frozensets on tuple inputs. | request/status | code-path reasoning + focused regression proof | Shared static membership tables in `app/health_contract.py`, `app/diagnostics/contract.py`, `app/diagnostics/assembler.py`, and `app/diagnostics/redaction.py` now call `frozenset((...))` instead of `frozenset({...})`, avoiding temporary set-literal allocation during module import while preserving immutable membership checks. Diagnostic source-record status groups and archive dot-segment validation now also reuse static frozensets instead of rebuilding tiny membership sets during record normalization or path validation. Focused proof lives in `tests/test_api.py::TestApiHealth::test_health_contract_constants_stay_secret_free`, `tests/test_diagnostic_export_contract.py::test_contract_static_frozensets_avoid_temporary_set_literals` and `test_source_record_status_groups_are_static_frozensets`, `tests/test_diagnostic_export_assembler.py::test_archive_entry_order_uses_explicit_extension` and `test_archive_path_dot_segments_use_static_membership_set`, and `tests/test_diagnostic_redaction.py::test_redaction_static_frozensets_avoid_temporary_set_literals`, which guard those modules against inline or whitespace-separated `frozenset({` regressions and per-call static-membership set construction while the existing schema, archive, and redaction tests preserve behavior. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_api.py::TestApiHealth tests/test_diagnostic_export_contract.py tests/test_diagnostic_export_assembler.py tests/test_diagnostic_redaction.py`; `make verify-fast` | Preserve health schema validation, diagnostic source status/category validation, source-record omitted/error normalization, forbidden archive path checks, and redaction header-key membership while avoiding temporary set-literal allocation for shared static membership tables and per-call diagnostic status/path membership checks. |
| Keep API analyze IOC serialization and grouping on one pass. | request/status | code-path reasoning + focused regression proof | `app/routes/api.py::api_analyze()` now serializes each IOC once while building the top-level `iocs` array and grouped summary in the same loop, avoiding a separate `group_by_type()` pass and identity-map lookup pass. `app/routes/ioc_payloads.py::_append_serialized_ioc_payload()` now owns the paired mutation of the flat serialized IOC list and grouped summary, so mixed three-row and long response paths do not repeat serialize/append/group mechanics while preserving tiny-batch shortcuts. `app/routes/ioc_payloads.py::api_analysis_response_payload()` now owns the public API analyze response envelope, including empty-result payloads and optional online `job_id`/`status_url` fields, so `api_analyze()` no longer assembles those response dictionaries by hand. `app/routes/ioc_payloads.py` also owns API analyze error payload text for invalid JSON, missing text, invalid modes, and missing Online providers, so `api_analyze()` no longer embeds public error-message literals in its branching logic. `app/routes/api_analysis.py::api_analyze_result()` now owns API analyze validation branching, Online rejection/launch branching, limit-rejection logging, and payload/status selection with the shared `app/routes/json_results.py::JsonResult` and no route-specific result alias. `app/routes/api_analysis.py::api_analyze_route_response()` now decodes request JSON, constructs the result, and applies it through `app/routes/json_results.py::apply_json_result()` without a route-specific response-applier alias, leaving `api_analyze()` as dependency wiring plus one route-response helper call. The route now defers that serialization until after online missing-provider and fanout-limit rejection paths, so rejected online requests do not build unused IOC response payloads. `app/routes/api_analysis.py::api_analyze_result()` also uses `app/routes/analysis_workflow.py::build_analysis_intake()` with the shared direct non-whitespace scanner for request text validation instead of duplicating validation/extraction control flow or allocating a stripped copy just to reject empty input. `app/routes/json_values.py::json_mapping_payload()` now owns silent JSON decoding and object-payload validation, so `api_analyze_route_response()` treats malformed JSON and non-object JSON arrays/scalars as the same public 400 response before request field extraction. `app/routes/analysis_workflow.py::analysis_request_values()` now owns raw analyze request field extraction and default mode handling through `app/routes/analysis_modes.py::DEFAULT_ANALYSIS_MODE`, so API analysis no longer reads individual JSON fields before validation. `app/routes/analysis_modes.py` owns the offline/online literals and public supported-mode label, so validation, Online branching, and API invalid-mode text no longer repeat those mode strings outside the shared mode boundary. Focused proof lives in `tests/test_api.py::TestApiAnalyzeOffline::test_groups_serialized_iocs_in_one_pass`, which monkeypatches `_serialize_ioc()` and proves each IOC is serialized once in response order while preserving grouped output, `test_serialized_ioc_payload_append_owns_flat_and_grouped_mutation`, `test_serialized_ioc_response_payload_delegates_mixed_group_append`, `test_api_analysis_response_payload_owns_public_shape`, which keeps empty/result/status-url response shape out of the Flask route body, `test_api_error_payloads_own_public_shape`, which keeps public API error text out of the Flask route body, `tests/test_api.py::TestApiAnalyzeOnline::test_online_no_providers_skips_ioc_serialization`, which proves the missing-provider rejection returns before IOC serialization, `tests/test_api.py::TestApiAnalyzeValidation::test_api_analyze_route_delegates_response_result_helper`, which keeps JSON decoding, validation, Online branching, payload/status selection, and result application out of the Flask route body, `test_api_analyze_route_response_uses_shared_json_application`, which preserves JSON payload/status formatting through the shared JSON result helper and fails if route-specific result or response-applier aliases return, plus `test_api_analyze_uses_shared_text_presence_check` and `test_api_analyze_uses_shared_intake_workflow`, which prove API analyze validation and JSON field extraction are wired through the shared intake helpers. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_api.py::TestApiAnalyzeOffline::test_groups_serialized_iocs_in_one_pass tests/test_api.py::TestApiAnalyzeOnline::test_online_no_providers_skips_ioc_serialization tests/test_api.py::TestApiAnalyzeOffline::test_extracts_ipv4 tests/test_api.py::TestApiAnalyzeOffline::test_returns_grouped`; `make verify-fast` | Preserve API analyze JSON shape, default offline mode, invalid-mode diagnostics, non-string text rejection, non-object JSON rejection, top-level IOC order, grouped response semantics, tiny-batch response shortcuts, online job/status-url fields, online rejection payloads, whitespace-only text rejection, and secret-free fanout-limit responses while avoiding duplicate IOC serialization, repeated mixed/long response serialize-and-group append mechanics, route-owned API analyze response envelope construction, route-owned API analyze validation/Online branching, duplicated health/analyze JSON result containers, duplicated health/analyze JSON response application, route-owned API analyze JSON decoding, route-owned API analyze result construction, route-owned API analyze JSON response application, route-owned public API error text, unused rejected-request payload work, route-local JSON body decoding, route-local JSON field extraction, duplicated mode literals, and stripped-string allocation for request text presence checks. |
| Keep browser analyze render decisions behind one result helper. | request/status | code-path reasoning + focused regression proof | `app/routes/analysis_results.py::browser_analyze_result()` now owns browser analyze validation branching, empty/invalid intake rendering decisions, Online start/rejection branching, limit-rejection logging, and results template context selection. `app/routes/analysis_results.py::apply_browser_analyze_result()` now owns Flask redirect-vs-render application for those result objects, delegates browser flash/redirect plumbing to `app/routes/browser_responses.py::apply_flash_redirect()`, and applies browser renders through the shared `TemplateResult` response boundary. `browser_analyze_route_response()` owns browser analyze result construction and response application, leaving `app/routes/analysis.py::analyze()` as dependency wiring plus one route-response helper call. `browser_analyze_result()` builds grouped template IOC data through `_ioc_template_context()` after the online missing-provider redirect path, so rejected online requests do not build unused template grouping data. `app/routes/analysis_results.py::online_result_template_extras()` now owns accepted and limit-rejected Online result template extras, including provider metadata, progress totals, and limit diagnostics, so `analyze()` no longer assembles those template-context dictionaries by hand. `app/routes/analysis_results.py::recent_analyses_context()` now owns fail-open recent-history lookup and sanitized warning logging for the intake page, and the route module no longer keeps compatibility wrappers or helper re-export aliases. `app/routes/analysis_results.py::index_page_result()` now wraps that context in the shared `TemplateResult` response boundary, and `index_page_route_response()` applies it through the shared template response helper, leaving `index()` as dependency wiring plus one route-response helper call. `browser_analyze_result()` also uses `app/routes/analysis_workflow.py::build_analysis_intake()` with the shared direct non-whitespace scanner for form text validation instead of duplicating validation/extraction control flow or allocating a stripped copy just to reject empty input. `app/routes/analysis_workflow.py::analysis_request_values()` now owns raw analyze request field extraction and default mode handling through `app/routes/analysis_modes.py::DEFAULT_ANALYSIS_MODE`, so `analyze()` no longer reads individual form fields before validation. `app/routes/analysis_modes.py` owns the offline/online literals and public supported-mode label, so validation, Online branching, and API invalid-mode text no longer repeat those mode strings outside the shared mode boundary. Focused proof lives in `tests/test_routes.py::test_analyze_groups_template_iocs_without_group_by_type`, which preserves rendered offline results while failing if the old grouping helper is reintroduced, `tests/test_routes.py::test_analyze_online_without_api_key_skips_template_grouping`, which proves the missing-provider redirect returns before template grouping, `tests/test_routes.py::test_browser_analyze_route_delegates_render_result_helper`, which keeps validation, Online branching, template/status selection, result construction, and result application out of the Flask route body, `test_apply_browser_analyze_result_owns_flask_response_application`, which preserves flash, redirect, URL, render, and status behavior while keeping it in result helper modules, `test_apply_flash_redirect_owns_browser_flash_redirect_response_application`, which keeps shared browser flash/redirect call ordering in one helper, `tests/test_routes.py::test_online_result_template_extras_own_browser_shape`, which keeps Online result-context dictionaries out of the Flask route body while preserving accepted and limit-rejected template shape, `test_recent_analyses_context_lives_in_result_helper`, which keeps recent-history lookup, fail-open warning mechanics, index result construction, index response application, and the old route wrapper out of the route module, `test_analysis_route_delegates_provider_metadata_helpers`, which keeps stale provider/IOC/template helper aliases out of the route module, and `tests/test_routes.py::test_analyze_uses_shared_text_presence_check` plus `test_browser_analyze_uses_shared_intake_workflow`, which prove browser analyze validation and form field extraction are wired through the shared intake helpers. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_routes.py::test_analyze_groups_template_iocs_without_group_by_type tests/test_routes.py::test_analyze_online_without_api_key_skips_template_grouping tests/test_routes.py::test_analyze_groups_by_type tests/test_routes.py::test_analyze_with_valid_input`; `make verify-fast` | Preserve browser analyze template grouping, raw submitted text, default offline mode, top-level result count, no-results behavior, whitespace-only text rejection, fail-open recent-analysis context, index page rendering, online admission flow, missing-provider redirect behavior, accepted Online progress metadata, limit diagnostics, and rendered IOC ordering while avoiding route-owned browser analyze validation/render branching, route-owned browser analyze result construction, route-owned browser analyze response application, route-owned index result construction, route-owned index response application, duplicated browser flash/redirect plumbing, unused rejected-request grouping, route-owned Online result template-context construction, route-local recent-history lookup/logging mechanics, route-local form field extraction, duplicated mode literals, and stripped-string allocation for request text presence checks. |
| Keep history reload IOC reconstruction, grouping, and empty replay serialization lean. | request/status | code-path reasoning + focused regression proof | `app/routes/ioc_payloads.py::_group_history_iocs()` now rebuilds persisted IOC models and groups them by type in one loop instead of first allocating a full IOC list and then rescanning it with `group_by_type()`. `app/routes/ioc_payloads.py::_append_history_ioc_row()` owns the paired persisted-row rebuild and grouped append for mixed three-row and long history replay paths, so those paths no longer repeat `_ioc_from_history_row()` plus `append_ioc_by_type()` mechanics while preserving short-row fast paths. `app/routes/history.py::history_detail()` now checks `total_count` before rebuilding persisted IOC template data, so empty history reloads skip the grouping helper entirely. `app/routes/history_replay.py::history_list_context()` now owns the history list template shape and bounded recent-history query limit, `history_list_result()` wraps that context in the same `TemplateResult` response boundary used by history detail replay, and `history_list_route_response()` applies that decision so the Flask route no longer owns intermediate result construction or response application. `app/routes/history_replay.py::load_history_replay_context()` now owns persisted analysis loading, missing-record normalization, and replay context construction. `app/routes/history_replay.py::history_detail_result()` now owns the history detail render-or-404 decision, returning the shared `app/routes/template_results.py::TemplateResult`, and `history_detail_route_response()` applies it through `app/routes/template_results.py::apply_template_result()`, leaving `history_detail()` as current-store dependency wiring plus one route-response helper call. `app/routes/history_replay.py::history_replay_context()` now owns history replay template shape, including the fixed history job id, empty provider metadata, result owner, IOC template context, enrichable count, and replay result serialization. `app/routes/history_replay.py::history_results_json()` returns the shared literal empty replay payload for empty result lists instead of invoking the JSON encoder on the no-results history path, and history replay uses a shared empty provider-counts JSON literal for the static metadata attribute. `app/routes/history.py` now imports only route-owned render/list/detail dependencies and no longer re-exports history replay JSON or IOC grouping helpers for compatibility; shared JSON sentinels stay with their owner modules instead of route-local aliases. `app/static/src/ts/modules/history.ts::init()` also recognizes that literal empty replay payload and marks history replay complete without calling `JSON.parse()`, while parsed history replay captures `#enrich-progress`, `#enrich-progress-text`, `#export-btn`, and `#export-dropdown` once and reuses those handles for completion and `initExportButton()` wiring. Focused proof lives in `tests/test_history_routes.py::TestHistoryDetailRoute::test_history_groups_iocs_while_rebuilding_models`, which preserves the rendered history detail page while failing if the old second-pass grouping helper is reintroduced, `test_history_ioc_row_append_owns_rebuild_and_group_mutation`, `test_group_history_iocs_delegates_rebuild_group_append`, `tests/test_history_routes.py::TestHistoryDetailRoute::test_empty_history_skips_ioc_grouping`, which fails if empty history reloads rebuild unused IOC template data, `tests/test_history_routes.py::TestHistoryDetailRoute::test_empty_history_results_skip_json_dumps`, which fails if empty replay serialization calls `json.dumps()`, `test_history_route_delegates_replay_context_helpers`, which keeps replay template shape, missing-record 404 decision, and abort/render application in history replay helpers instead of the Flask route body, `test_template_result_helper_owns_abort_or_render_application`, which preserves shared abort/render behavior for result objects, `tests/test_history_routes.py::TestHistoryListRoute::test_history_list_delegates_template_context_helper`, which keeps list-page query limits, template shape, and route response application out of the route body, and `app/static/src/ts/modules/history.test.ts`, where empty replay completion keeps export disabled while `JSON.parse` is patched to fail and parsed replay verifies each history completion/export ID is looked up once. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_history_routes.py::TestHistoryDetailRoute::test_history_groups_iocs_while_rebuilding_models tests/test_history_routes.py::TestHistoryDetailRoute::test_empty_history_skips_ioc_grouping tests/test_history_routes.py::TestHistoryDetailRoute::test_empty_history_results_skip_json_dumps tests/test_history_routes.py::TestHistoryDetailRoute::test_history_returns_200_with_seeded_data tests/test_history_routes.py::TestHistoryDetailRoute::test_history_shows_correct_ioc_count`; `npx vitest run app/static/src/ts/modules/history.test.ts`; `make verify-fast` | Preserve history list/detail rendering, persisted IOC type/value/raw_match semantics, grouped result-page template input, short-row grouping fast paths, no-results history rendering, missing-record 404 behavior, history replay data, empty replay payload shape, empty provider-count metadata shape, empty replay completion state, disabled empty replay export, parsed replay export enablement, and history-owner DOM attributes while avoiding an extra IOC list scan, repeated persisted-row rebuild/group append mechanics, empty-history IOC grouping, route-owned history list/replay template shape, route-owned history list/detail result construction, route-owned history list/detail response application, route-owned history detail render-or-404 branching, route-owned history detail abort/render application, route-local history record loading, route-local history list query limits, unnecessary empty-result JSON encoding, ad hoc empty provider-count JSON literals, empty replay JSON parsing, and repeated completion/export ID lookups on history reload. |
| Keep IOC detail valid-type checks on a precomputed set. | request/status | refactor proof + focused regression proof | `app/routes/detail_graph.py` now precomputes `VALID_IOC_TYPES` once at module load through a direct helper loop instead of rebuilding `{t.value for t in IOCType}` on every detail-page request, and `app/routes/detail.py` no longer retains compatibility aliases for detail graph helpers. Empty-cache detail pages now pass literal empty graph payloads instead of allocating an IOC graph node that the frontend will discard into its empty state. `app/routes/detail_graph.py` now owns IOC/provider graph node and edge payload construction, leaving `app/routes/detail.py` focused on type validation, cache lookup, and template rendering. Provider graph node/edge payload construction now runs through one `_provider_graph_payload()` helper, and long graph paths share `_append_provider_graph_payload()` for the paired list mutation instead of repeating provider/verdict extraction plus node/edge append mechanics. `app/routes/detail_graph.py::detail_template_context()` now owns the full IOC detail template view model, including provider results and graph payload fields, so `detail.py::ioc_detail()` no longer assembles graph/template keyword arguments itself. `app/routes/detail_graph.py::load_detail_template_context()` now owns valid-type rejection, cache result lookup, and context construction. `app/routes/detail_graph.py::detail_page_result()` now owns the IOC detail render-or-404 decision, returning the shared `app/routes/template_results.py::TemplateResult`, and `detail_page_route_response()` applies that decision through `app/routes/template_results.py::apply_template_result()`, leaving `detail.py::ioc_detail()` as current-cache dependency wiring plus one route-response helper call. Focused proof lives in `tests/test_ioc_detail_routes.py::TestIocDetailRoute::test_valid_ioc_types_are_precomputed`, which guards the helper against generator/set-comprehension frames and keeps the old valid-type alias out of the route module, `test_detail_route_delegates_graph_payload_helpers`, which keeps graph payload helper bodies and stale graph aliases out of the route module, `test_detail_route_delegates_template_context_helper`, which keeps detail template shape, cache lookup, valid-type rejection, the detail 404 decision, and shared abort/render application in detail graph helpers instead of the Flask route body, `test_append_provider_graph_payload_owns_long_path_mutation`, which preserves default provider/verdict fallback behavior while proving paired node/edge mutation has one owner, `test_graph_data_skips_iteration_for_empty_single_pair_three_or_four_results`, which preserves short-result no-iteration fast paths while checking the graph builder delegates shared payload construction and long-path append mechanics, and `test_detail_page_empty_cache`, which preserves the empty page while checking the graph payloads stay `[]`, with existing 200/404 route checks preserving valid and invalid type behavior. | R008, R010, R040 | `python3 -m pytest -q tests/test_ioc_detail_routes.py::TestIocDetailRoute::test_valid_ioc_types_are_precomputed tests/test_ioc_detail_routes.py::TestIocDetailRoute::test_detail_page_empty_cache tests/test_ioc_detail_routes.py::TestIocDetailRoute::test_detail_invalid_type tests/test_ioc_detail_routes.py::TestIocDetailRoute::test_detail_page_200`; `make verify-fast` | Preserve IOC detail 200/404 routing semantics, populated provider graph payloads, graph fallback literals, provider result template input, short-result no-iteration paths, and empty-cache detail messaging while avoiding repeated valid-type set construction, import-time valid-type generator/comprehension frames, route-owned cache lookup/type rejection, route-owned detail render-or-404 branching, route-owned detail result construction, route-owned detail response application, route-owned detail abort/render application, route-owned graph helper bodies, route-owned detail template shape, repeated provider graph node/edge construction, repeated graph append mutation, and discarded empty-cache graph node allocation. |
| Keep orchestration diagnostic export coercion on bounded iteration. | request/status | code-path reasoning + focused regression proof | `app/routes/enrichment_diagnostics.py::_coerce_orchestration_diagnostics_for_export()` now uses `itertools.islice()` over top-level and nested diagnostic dict keys, indexes accepted entries directly, and keeps list value caps on bounded iteration instead of materializing bounded copies or allocating dict items views before filtering. List values are also accumulated with a direct loop instead of a list-comprehension frame, while `_set_export_child_scalar()` owns nested diagnostic child scalar filtering, key truncation, and value truncation. Focused proof lives in `tests/test_routes.py::test_orchestration_diagnostics_export_coercion_uses_bounded_iteration`, where dict subclasses raise if coercion reads past the documented 40-entry export caps or calls `.items()`, `test_orchestration_diagnostics_export_coercion_does_not_slice_lists`, where a list subclass raises if list coercion slices before applying the 25-entry cap, and `test_orchestration_diagnostics_export_coercion_accumulates_lists_directly`, which guards the list path against `<listcomp>` bytecode and proves nested child scalar assignment delegates to `_set_export_child_scalar()`. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_routes.py::test_orchestration_diagnostics_export_coercion_uses_bounded_iteration tests/test_routes.py::test_orchestration_diagnostics_export_coercion_does_not_slice_lists tests/test_diagnostic_export_route.py tests/test_diagnostic_export_sources.py`; `make verify-fast` | Preserve diagnostic export truncation, primitive-only nested/list values, unsupported-value repr fallback, and secret-safe archive assembly while avoiding full diagnostic container materialization, orchestration diagnostic mapping items-view allocation before caps are applied, and list-comprehension frames for diagnostic list values. |
| Keep diagnostic source sanitization on bounded mapping and sequence iteration. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/runtime_payloads.py` now owns runtime payload construction for config-secret inventory, cache stats, recent history, health, and orchestration diagnostics, and `app/diagnostics/sources.py` imports that sibling module directly instead of through the package facade or parallel symbol imports, leaving `sources.py` focused on building `DiagnosticSource` descriptors with public default/type aliases assigned from the runtime payload owner and runtime type aliases kept out of `sources.py::__all__`. `app/diagnostics/runtime_payloads.py` now calls `jsonish` owner helpers without re-exporting JSON-safe cap aliases, and `app/diagnostics/sources.py` no longer imports unused JSON-safe cap aliases after payload shaping moved to the runtime payload owner. `app/diagnostics/policy.py` now imports the default source-byte cap from `app/diagnostics/source_record_fields.py`, the owner of source-record bounds, instead of reaching it through the contract wrapper. `app/routes/diagnostic_export.py::diagnostic_export_response()` now owns successful diagnostic export source construction, bundle assembly, filename selection, and ZIP response header decisions without materializing Flask `Response` objects directly, leaving `app/routes/diagnostics.py::diagnostics_export()` focused on request dependency wiring. `app/routes/diagnostic_export.py::diagnostic_export_route_response()` now owns timestamp capture through an injectable clock, ConfigStore construction through an explicit factory, bounded failure logging, and delegation to `apply_diagnostic_export_http_response()`, while `diagnostics_export()` passes the cache/history stores explicitly instead of handing the helper the whole Flask app. `app/routes/diagnostic_export.py::diagnostic_export_failure_response()` now owns the bounded analyst-facing failure response body, status, and plain-text mimetype as a response decision, so only the shared diagnostic export response applier materializes Flask `Response` objects. `app/diagnostics/runtime_payloads.py::safe_job_diagnostics_payload()` now owns orchestration diagnostic type validation, safe mapping coercion, and default application, while `apply_job_diagnostics_defaults()` owns `job_id`/`found` default mutation without using `setdefault()` and `job_diagnostics_payload()` stays focused on accessor lookup and missing-job payloads. `app/diagnostics/runtime_payloads.py::resolved_health_checks()` now owns runtime health-check callable/static/default selection, and `health_checks_mapping()` owns non-mapping validation, keeping `health_payload()` focused on final health-contract payload construction. `app/diagnostics/sources.py::_runtime_source()` now owns repeated runtime `DiagnosticSource` descriptor defaults, keeping `build_default_diagnostic_sources()` focused on dependency selection, omission decisions, and collect-callable wiring. `app/diagnostics/sources.py::_optional_runtime_source()` now owns config/cache/history missing-dependency omission versus present-dependency runtime source construction, while `_dependency_collector()` owns dependency capture/invocation for optional sources, keeping repeated optional source branches out of the default source factory. `app/diagnostics/sources.py::_config_secret_source()`, `_cache_stats_source()`, and `_recent_history_source()` now own the fixed optional runtime descriptor shapes for config, cache, and history so `build_default_diagnostic_sources()` keeps only source ordering and dependency selection. `app/diagnostics/sources.py::_orchestration_source()` now owns orchestration diagnostic job/accessor omission branching and collect-source construction, while `_omitted_orchestration_source()` owns the fixed omitted orchestration descriptor shape, keeping the default source factory focused on source ordering. `app/diagnostics/sources.py::_default_source_context()` now owns generated-at fallback and bounded history-limit normalization for default diagnostic sources. `app/diagnostics/sources.py::_health_source()` now owns health-check source descriptor construction, while `_health_payload_collector()` owns dependency capture and health payload invocation for config/cache/history/check providers. `app/diagnostics/sources.py::_history_save_source()` now owns the fixed history-save diagnostics source descriptor. `app/diagnostics/sources.py::_default_runtime_sources()` now owns the ordered default diagnostic source tuple, keeping `build_default_diagnostic_sources()` focused on context normalization and dependency handoff. `app/diagnostics/jsonish.py::_safe_jsonish()` now dispatches container-specific sanitization to `_safe_jsonish_mapping()`, `_safe_jsonish_sequence()`, `_safe_jsonish_set()`, and `_safe_jsonish_default()` while `_safe_mapping()` passes mappings through directly instead of copying them first. `app/diagnostics/jsonish.py::_safe_jsonish_sequence()` now handles empty, single, pair, three-item, and four-item runtime diagnostic sequences by direct index access before sequence and set long paths share capped item accumulation through `_append_safe_jsonish_item()`. The recursive mapping, sequence, and set normalization paths still use `itertools.islice()` for caps, accumulate into dicts/lists with direct loops instead of comprehension frames, and mapping caps iterate keys directly instead of allocating `items()` views. Focused proof lives in `tests/test_diagnostic_export_sources.py::test_sources_delegate_runtime_payload_builders`, which keeps runtime payload shaping and repeated runtime source descriptor construction out of the descriptor factory while proving `sources.py` imports the runtime payload sibling directly rather than via the package facade or parallel symbol imports, its public default/type aliases still point at the runtime payload owner, and runtime type aliases are not wildcard-public source exports, `test_diagnostic_sanitization_policy_centralizes_source_bounds`, which keeps policy source-byte bounds routed through the source-record field owner instead of the contract wrapper, `test_default_runtime_sources_owns_source_ordering`, which preserves default source order and history-limit propagation in one tuple builder, `test_runtime_source_helper_owns_descriptor_defaults`, which preserves runtime source path, collect callable, and max-byte defaults in one helper, `test_optional_runtime_source_helper_owns_missing_or_present_dependency`, which preserves omitted and collectable optional source behavior for injected dependencies while proving dependency capture is delegated, `test_dependency_collector_owns_dependency_capture`, which keeps dependency invocation in one helper, `test_optional_runtime_descriptor_helpers_own_fixed_source_shapes`, which preserves config/cache/history optional descriptor IDs, paths, omission, and collect behavior in named helpers, `test_orchestration_source_helper_owns_request_branching`, which preserves missing-job, missing-accessor, and present-job orchestration source behavior while proving omitted branches delegate fixed descriptor shape, `test_omitted_orchestration_source_helper_owns_fixed_descriptor`, which keeps the fixed omitted orchestration descriptor fields in one helper, `test_default_source_context_owns_history_limit_and_timestamp`, which preserves generated-at fallback and bounded history-limit normalization outside the default source factory, `test_health_source_helper_owns_dependency_capture`, which preserves health source descriptor shape while proving dependency capture is delegated, `test_health_payload_collector_owns_dependency_capture`, which preserves captured health dependency invocation in one helper, `test_history_save_source_helper_owns_fixed_descriptor`, which preserves the fixed history-save diagnostics descriptor outside the default source factory, `tests/test_diagnostic_export_route.py::test_diagnostic_export_route_delegates_response_helper`, which keeps timestamp capture, successful ZIP response decisions, bounded failure response decisions, and Flask response application in their dedicated helpers while proving the route passes cache/history stores explicitly and the helper no longer dereferences app stores, `test_diagnostic_export_route_response_accepts_explicit_clock`, which proves route-helper timestamp capture is injectable without monkeypatching `utc_now`, `test_job_diagnostics_payload_adds_defaults_without_setdefault`, which proves orchestration diagnostic default insertion delegates to the safe job payload helper without `setdefault()`, `test_safe_job_diagnostics_payload_owns_mapping_coercion_and_defaults`, which keeps mapping coercion and default application in the helper, `test_job_diagnostics_defaults_helper_owns_payload_mutation`, which preserves explicit job fields while proving one helper owns default mutation, `test_health_checks_mapping_owns_resolution_and_validation`, which preserves static, callable, and default health-check sources while proving `health_payload()` delegates resolution/validation, `test_resolved_health_checks_owns_source_selection`, which keeps callable/static/default source selection in one helper, `test_safe_mapping_uses_bounded_iteration_for_nested_mappings`, where mapping subclasses raise if sanitization reads past the documented 50-entry caps or calls `.items()`, `test_safe_jsonish_uses_direct_recursive_loops`, which preserves nested JSON-safe output while guarding all jsonish helper bodies against `<dictcomp>` and `<listcomp>` bytecode, `test_safe_jsonish_skips_iteration_for_exact_empty_single_pair_three_or_four_containers`, which makes short runtime diagnostic sequences fail on iteration or slicing while preserving output for up to four values, and `test_safe_jsonish_helpers_own_container_and_default_coercion`, which proves the dispatcher names each owner helper while preserving container and fallback coercion. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_export_sources.py tests/test_diagnostic_export_bundle_integration.py tests/test_diagnostic_export_route.py`; `make verify-fast` | Preserve diagnostic source shape, public diagnostic source default compatibility, source-record byte-cap semantics, route-level bounded failure responses, download filename/header shape, orchestration diagnostic default fields, config/cache/history optional omission semantics, health-check static/callable/default resolution semantics, history-save diagnostics source shape, metadata generated-at and history-limit semantics, recursive depth limits, string/list/dict/set caps, redaction compatibility, and secret-free archive assembly while avoiding runtime payload helper bodies in the source descriptor factory, package-facade self-imports for internal runtime payload helpers, parallel runtime-payload symbol imports in the source descriptor factory, wildcard-public runtime type aliases from the source descriptor factory, unused JSON-safe cap aliases in source descriptor and runtime payload factories, policy-owned source byte caps routed through the contract wrapper, repeated runtime `DiagnosticSource` descriptor construction in the default source factory, repeated optional dependency omitted/runtime branching in the default source factory, repeated fixed config/cache/history descriptor literals in the source-order builder, public default source factories that manually append ordered sources, inline optional dependency capture closures, repeated fixed omitted orchestration descriptor literals, inline orchestration diagnostic job/accessor branching in the default source factory, mixed orchestration diagnostic lookup/coercion/defaulting, inline health source dependency-capture wiring in descriptor construction, inline fixed history-save descriptor construction in the default source factory, inline metadata timestamp/history-limit normalization in the default source factory, whole-app diagnostic helper inputs, route-local diagnostic timestamp wrappers, hidden diagnostic-export clock dependencies in tests/callers, successful diagnostic ZIP response assembly in the Flask route body, bounded diagnostic failure response construction in the Flask route body, route-local orchestration diagnostic default mutation, mixed runtime health-check source selection and type validation, full mapping and sequence materialization before caps are applied, mapping items-view allocation, mixed container traversal ownership in the jsonish dispatcher, and recursive comprehension-frame allocation during sanitization. |
| Keep recent-history diagnostic payloads on bounded iteration. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/runtime_payloads.py::recent_history_payload()` now delegates returned-row coercion to `recent_history_items()`, keeping the public payload builder focused on the history-store contract and envelope fields. `recent_history_items()` uses `itertools.islice()` over the returned history rows instead of slicing a second list after `HistoryStore.list_recent(limit=...)` has already bounded the store query, delegates long-path row coercion and mutation to `append_recent_history_item()` instead of allocating a list-comprehension frame around `_safe_jsonish()`, and owns the empty/single-row fast paths. `app/diagnostics/sources.py` calls runtime payload builders directly instead of retaining private compatibility aliases. Focused proof lives in `tests/test_diagnostic_export_sources.py::test_recent_history_payload_uses_bounded_iteration_not_slice`, where a list subclass raises if the helper slices returned rows while preserving the 10-item export cap and proving the payload builder delegates to `recent_history_items()`, `test_recent_history_payload_accumulates_without_list_comprehension_frame`, which guards both the payload builder and row helper against `<listcomp>` bytecode, and `test_recent_history_items_owns_safe_row_coercion`, which proves safe row coercion, append mutation, and bounded iteration live in the helper pair. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_export_sources.py tests/test_diagnostic_export_bundle_integration.py tests/test_diagnostic_export_route.py`; `make verify-fast` | Preserve recent-history limit propagation, returned_count/items payload shape, safe JSON coercion, and secret-free diagnostic archive behavior while avoiding public payload-builder ownership of row coercion, redundant slice allocation, and comprehension-frame allocation on already bounded history rows. |
| Keep diagnostic manifest duplicate-source validation single-pass. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/contract.py::DiagnosticManifest.__post_init__()` now validates duplicate source IDs while normalizing iterable source inputs instead of first materializing one source-id list and one set, and accepted iterable-source accumulation delegates to `app/diagnostics/contract.py::append_manifest_source()`. Focused proof lives in `tests/test_diagnostic_export_contract.py::test_manifest_duplicate_source_validation_stops_at_first_duplicate`, where a generator raises if manifest construction reads past the first duplicate source. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_export_contract.py tests/test_diagnostic_export_assembler.py tests/test_diagnostic_export_bundle_integration.py`; `make verify-fast` | Preserve diagnostic manifest deterministic serialization, duplicate rejection, source tuple normalization, and aggregate counts while avoiding extra duplicate-validation containers and reads past an early duplicate. |
| Keep diagnostic safe-error summary whitespace normalization on compiled regex. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/source_record_fields.py::_normalize_error_summary()` now collapses whitespace with a module-level compiled regex instead of `strip().split()` plus `join()`, avoiding a temporary word list on every diagnostic error-source record. `app/diagnostics/source_record_fields.py` now owns source-record status/category constants, field normalization, redaction-label normalization, and JSON-safe source-record payload shaping, leaving `app/diagnostics/contract.py::DiagnosticSourceRecord` as a dataclass wrapper around those rules without re-exporting private field-helper functions or owner-only status/category vocabulary. Diagnostic source-record required/optional text normalization now reuses `app/text_utils.py::stripped_bounded_text()` instead of carrying a duplicate local strip/bound helper. `app/diagnostics/source_record_fields.py::_normalize_redaction_label()` now owns per-label text validation so `_normalize_redaction_labels()` stays focused on cardinality fast paths, dedupe, and stable ordering instead of carrying a nested normalizer closure. Focused proof lives in `tests/test_diagnostic_export_contract.py::test_safe_error_summary_normalizes_whitespace_without_split_list`, where a string subclass raises if normalization calls `split()` while preserving single-line summary output, `test_source_record_text_normalization_uses_shared_helper`, which proves source-record text fields are routed through the shared stripped-text helper, and `test_source_record_delegates_field_normalization_and_payload_builder`, which keeps field validation and source-record payload construction out of the dataclass methods. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_export_contract.py tests/test_diagnostic_export_assembler.py tests/test_diagnostic_export_bundle_integration.py`; `make verify-fast` | Preserve diagnostic source error defaults, bounded safe_error_summary truncation, source-record text validation, single-line whitespace normalization, source-record status/category validation, redaction-label normalization, JSON-safe source-record payloads, public package default source byte/error-summary compatibility, and secret-free manifest output while avoiding split-list allocation for error summaries, dataclass-owned source-record field validation, dataclass-owned source-record payload shaping, contract-facade re-exports of owner-only status/category/error-bound vocabulary, nested redaction-label normalizer closures, and duplicate local stripped-text helper logic. |
| Keep diagnostic manifest aggregate serialization single-pass. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/contract.py::DiagnosticManifest` now caches deterministic `sorted_sources` at construction time, avoiding repeated sorting, and `DiagnosticManifest.to_dict()` delegates status counts, redaction totals, and serialized source dictionary construction to `app/diagnostics/manifest_payloads.py::manifest_payload()`, which walks that cached order once instead of sorting on every serialization or rescanning with multiple `sum()` calls plus a separate source-list pass. `app/diagnostics/manifest_payloads.py::SourceCountsAccumulator` now owns source status and redaction aggregate counting, `source_counts_payload()` uses it for bundle summaries, and `manifest_payload()` uses it while serializing source records, so manifest serialization keeps one source loop without duplicating the status-count branch. `app/diagnostics/manifest_payloads.py` now imports source status constants from `app/diagnostics/source_record_fields.py` instead of duplicating equivalent manifest-local status literals beside the accumulator. `source_counts_payload()` and `manifest_payload()` now also skip fallback iteration for empty, single-source, pair, three-source, and four-source manifests by using direct indexed accumulator paths before the long source loop. `app/diagnostics/manifest_payloads.py::append_serialized_source()` now owns the per-source `to_dict()` append, and `app/diagnostics/manifest_payloads.py::_add_and_append_source()` owns paired count-and-append mutation for short and long manifest payload paths while `manifest_payload()` remains the one-pass count-and-serialize coordinator. `DiagnosticSourceRecord.to_dict()` now serializes already-normalized redaction labels with direct accumulation instead of constructor-copying the tuple with `list(...)`. `_normalize_redaction_labels()` also deduplicates labels with direct set accumulation instead of a set-comprehension frame before producing the stable sorted tuple, delegates per-label text coercion to `_normalize_redaction_label()`, and skips sorting entirely for zero or one normalized label. `DiagnosticManifest.__post_init__()` now also skips sorted-source construction for zero or one source while preserving sorted order for multi-source manifests. Focused proof lives in `tests/test_diagnostic_export_contract.py::test_manifest_reuses_construction_time_sorted_sources`, where `builtins.sorted` is patched after manifest construction while source order and serialization remain deterministic, `test_manifest_construction_skips_sort_for_zero_or_one_source`, where `builtins.sorted` is patched during empty/single-source manifest construction, `test_manifest_serialization_computes_counts_in_one_source_pass`, where `builtins.sum` is patched to fail while aggregate counts and deterministic source ordering are preserved, `test_manifest_to_dict_delegates_payload_builder`, which keeps aggregate manifest payload construction out of the dataclass method while proving serialized source construction shares the accumulator, delegates source-record append mutation, reuses the source-record status vocabulary, and does not take a separate count-only source pass, `test_manifest_payload_skips_iteration_for_empty_single_pair_three_or_four_sources`, which makes short manifest source tuples fail on iteration or slicing while preserving counts and source serialization through four sources, `test_manifest_serialized_source_append_owns_record_serialization`, which preserves source-record payload shape while proving one helper owns `to_dict()` append mutation, `test_source_record_serializes_redaction_labels_without_list_constructor`, which guards source-record label serialization against `list(...)` constructor calls, `test_redaction_label_normalization_uses_direct_accumulation`, which preserves label dedupe/order while guarding against `<setcomp>` bytecode, and `test_redaction_label_normalization_skips_sort_for_zero_or_one_label`, where `builtins.sorted` is patched for empty/single-label normalization. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_export_contract.py tests/test_diagnostic_export_assembler.py tests/test_diagnostic_export_bundle_integration.py`; `make verify-fast` | Preserve diagnostic manifest schema, deterministic source ordering, aggregate counts, redaction totals, source-record status vocabulary, redaction-label text coercion/dedupe/order, and JSON-safe source records while avoiding dataclass-owned aggregate manifest payload construction, duplicated source-count branches across manifest and bundle summaries, repeated source-record append mutation, separate count-only source passes during manifest serialization, duplicate manifest-local source status literals, fallback iteration for short manifest source tuples, repeated sorting and repeated scans during manifest serialization, unnecessary empty/single-item sorting during manifest construction and redaction-label normalization, source-record label constructor copies, nested redaction-label normalizer closures, plus comprehension-frame allocation during label normalization. |
| Keep diagnostic bundle source preparation on streaming validation. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/assembler.py::assemble_diagnostic_bundle()` now passes source iterables directly into `app/diagnostics/source_preparation.py::prepare_sources()`, which validates and normalizes descriptors while consuming them instead of first tuple-materializing every source or bouncing through an assembler-private preparation wrapper. Archive entries now append the manifest first and explicitly extend with sorted payload entries instead of using a starred list-unpack around `sorted(payload_entries)`, and returned archive paths are accumulated directly instead of using a tuple generator expression over the archive entries. `app/diagnostics/bundle_layout.py::append_archive_entry()` and `append_archive_entry_path()` now own long-path archive entry and path projection append mutation while short archive-entry/path projections stay direct. Single-source diagnostic bundles now skip prepared-source and payload-entry sorting entirely while multi-source bundles keep deterministic source/archive ordering. `app/diagnostics/bundle_layout.py` now owns prepared-source ordering, payload-entry ordering, manifest-first archive entry layout, archive path projection, and summary aggregation while keeping `DiagnosticSourceRecord` as a type-only contract import, and `app/diagnostics/assembler.py::assemble_diagnostic_bundle()` now calls its layout helpers directly instead of keeping private ordering/archive-path delegators. `app/diagnostics/archive_writer.py::write_stable_zip()` now owns deterministic ZIP writing, including timestamps, stored compression, Unix owner-only permissions, and entry writes, leaving the assembler free of low-level `zipfile` mechanics. `DiagnosticBundle.summary` now delegates to `bundle_layout.bundle_summary()`, which reuses `app/diagnostics/manifest_payloads.py::source_counts_payload()` and its shared accumulator against the cached manifest source order instead of calling `DiagnosticManifest.to_dict()` and serializing every source record just to return route/test summary fields. `app/diagnostics/json_safe.py` now owns recursive JSON-safe diagnostic payload normalization for mappings, tuples, and lists, using direct loops instead of dict/list comprehension frames before JSON encoding; `safe_json_mapping()` owns mapping key traversal directly instead of allocating an `items()` view, while `safe_json_payload()` delegates mapping and sequence recursion to owner helpers. `safe_json_sequence()` now handles empty, single, pair, three-item, and four-item diagnostic sequences by direct index access before falling back to long-path accumulation through `app/diagnostics/json_safe.py::append_safe_json_item()`. `app/diagnostics/assembler.py` and `app/diagnostics/payload_encoding.py` no longer keep private JSON-safe wrapper aliases. `app/diagnostics/payload_encoding.py` now owns source payload collection, bytes/string/object redaction, shared redacted-text UTF-8 encoding, dedicated JSON-object redaction/JSON-safe encoding, direct owner-module JSON-safe normalization calls, and deterministic JSON encoding while keeping descriptor/redaction type imports type-only, and `app/diagnostics/assembler.py::assemble_diagnostic_bundle()` now calls those helpers directly instead of keeping private payload compatibility wrappers. `app/diagnostics/source_results.py::source_collection_result()` now owns per-source omitted/error/included outcome selection plus optional payload-entry construction, and `collect_source_results()` owns record/payload-entry accumulation while keeping `DiagnosticSourceRecord`, `ConfigSecretStore`, and private `_PreparedSource` references as type-only imports, leaving `assemble_diagnostic_bundle()` focused on source ordering, manifest creation, and final archive assembly. `app/diagnostics/source_results.py::append_collected_source_result()` now owns collect-plus-append mutation for long prepared-source paths while short source-count paths keep direct indexed collection. `app/diagnostics/archive_paths.py` now owns archive path validation, generated source filenames, default source prefixes, manifest archive path, and path-safety constants, leaving `app/diagnostics/assembler.py` focused on source preparation, redaction/encoding, manifest records, and ZIP assembly without re-exporting archive-path internals or compatibility constants. `app/diagnostics/records.py` now owns included/truncated, omitted, and error manifest record construction plus exception summary shaping while importing source status/default reason vocabulary directly from `app/diagnostics/source_record_fields.py`, keeping redaction config/metadata types as type-only imports, and `app/diagnostics/assembler.py::assemble_diagnostic_bundle()` now calls those record builders directly instead of keeping private manifest-record compatibility wrappers or inline record construction. `app/diagnostics/source_preparation.py` now owns diagnostic source descriptors, prepared-source records, descriptor validation, and duplicate source/archive-path checks while importing source descriptor byte caps, category vocabulary, content-type default, text normalization, and integer normalization directly from `app/diagnostics/source_record_fields.py` instead of the contract wrapper, `app/diagnostics/__init__.py` re-exports `DEFAULT_SOURCE_MAX_BYTES` from that same owner module, and `app/diagnostics/assembler.py` no longer keeps a private `_prepare_sources()` delegator. `app/diagnostics/assembler.py` imports only the runtime `prepare_sources()` helper from the source-preparation owner, keeps `DiagnosticSource`/`ConfigSecretStore` as type-only imports, and no longer imports the private `_PreparedSource` record type. `app/diagnostics/sources.py` and the package facade now import `DiagnosticSource` from `app/diagnostics/source_preparation.py`, and `app/diagnostics/assembler.py::__all__` no longer advertises that owner-module descriptor as assembler-owned API. `app/diagnostics/source_preparation.py::_prepared_source()` now owns normalized prepared-source record construction, leaving `prepare_sources()` focused on descriptor-level validation plus duplicate source/archive-path state. `app/diagnostics/sources.py::_metadata_source()` now owns diagnostic export metadata source descriptor construction, and `_metadata_payload()` owns the metadata schema/runtime-object payload shape, leaving `build_default_diagnostic_sources()` focused on runtime dependency selection and source ordering instead of carrying the metadata schema/runtime-object payload literal inline. Diagnostic source descriptor required/optional text normalization now also reuses `app/text_utils.py::stripped_bounded_text()` instead of carrying a duplicate local strip/bound helper. Diagnostic internals now import sibling diagnostic modules through relative imports instead of round-tripping through the `app.diagnostics` package facade while leaving `app/diagnostics/__init__.py` as the public compatibility facade. Focused proof lives in `tests/test_diagnostic_export_assembler.py::test_diagnostic_modules_use_relative_sibling_imports`, which keeps non-facade diagnostic modules off package-facade sibling imports, `test_validation_stops_consuming_source_iterable_at_first_duplicate`, where a generator raises if source preparation reads past the first duplicate source ID, `test_assembler_uses_shared_diagnostic_sanitization_policy_bounds`, which preserves policy-owned archive-path bounds while proving assembler no longer exposes private archive-path constants, public archive-path compatibility constants, or `DiagnosticSource` through `__all__`, `test_archive_entry_order_uses_explicit_extension`, which guards the archive-entry construction shape, `test_archive_writer_owns_stable_zip_metadata`, which preserves stable ZIP timestamps, stored compression, owner-only file mode, and entry writes outside the assembler, `test_single_source_bundle_skips_sorting`, which patches `builtins.sorted` during one-source/one-payload assembly, `test_bundle_summary_does_not_serialize_sources`, which patches source-record serialization to fail while summary counts still work, `test_bundle_layout_owns_summary_and_ordering_helpers`, which keeps pure ordering/summary logic and the old private ordering wrappers out of assembler while proving bundle summaries reuse shared aggregate counts without taking on accumulator internals or importing the contract at runtime for annotations, `test_bundle_layout_owns_archive_entries_and_paths`, which preserves manifest-first archive ordering and archive path projection in the layout owner module, `test_assembler_delegates_manifest_record_builders`, which keeps included/omitted/error record helper bodies, private record wrappers, and runtime redaction config/metadata type imports out of assembler/records runtime paths while proving records imports source status vocabulary from the source-record field owner instead of the contract wrapper, `test_included_record_helper_owns_truncation_and_metadata`, which preserves included-byte truncation, status selection, and redaction metadata transfer in the record owner module, `test_source_collection_result_owns_per_source_outcomes`, which preserves omitted and included source outcomes while keeping payload-entry construction in the source-result helper, `test_source_collection_result_captures_errors_without_payload_entry`, which preserves fail-open error records without archive payload entries, `test_collect_source_results_owns_record_and_payload_accumulation`, which preserves ordered record and payload-entry accumulation outside the assembler loop while proving source-result type annotations do not import the contract, config-store protocol, or private prepared-source record at runtime, `test_json_safe_uses_direct_recursive_loops`, which preserves JSON-safe output while failing if JSON-safe normalization reintroduces comprehension frames, `test_json_safe_mapping_owns_direct_key_iteration`, which keeps mapping traversal out of the dispatcher and fails if mapping normalization calls `.items()` or reintroduces comprehension frames, `test_json_safe_skips_iteration_for_exact_empty_single_pair_three_or_four_containers`, which makes short diagnostic sequence normalization fail on iteration or slicing while preserving output for up to four values, `test_json_safe_sequence_types_share_owner_recursive_helper` and `test_assembler_delegates_json_safe_payload_normalization`, which keep recursive JSON-safe payload normalization in the owner module without payload-encoding wrapper aliases, `test_assembler_delegates_payload_collection_and_encoding`, which keeps collection, redaction, JSON encoding decisions, result accumulation, private payload wrappers, and runtime descriptor/redaction type imports out of assembler/payload encoding runtime paths, `test_text_payload_encoding_helper_owns_redacted_utf8_encoding`, which proves bytes and strings share the redacted-text encoding helper, `test_json_payload_encoding_skips_text_encoding_helper`, which preserves JSON object payload routing through JSON-safe normalization, `test_json_payload_encoding_helper_owns_safe_json_encoding`, which preserves JSON-safe object encoding while proving `redact_and_encode_payload()` delegates JSON-object work, `test_diagnostic_metadata_source_owns_payload_shape`, which preserves metadata payload semantics while keeping schema/runtime-object literals out of both the default source-list builder and metadata descriptor helper, `test_diagnostic_source_text_normalization_uses_shared_helper`, which proves source descriptor text fields are routed through the shared stripped-text helper and imports the descriptor from its owner module, `test_diagnostic_sanitization_policy_centralizes_source_bounds`, which keeps the package facade and policy module pointed at the source-record field owner for default source byte caps, `test_assembler_delegates_source_preparation`, which keeps descriptor validation state, the old private preparation wrapper, runtime private prepared-source imports, and runtime source descriptor/config-store annotation imports out of the assembler module while proving preparation constants come from the source-record field owner instead of the contract wrapper, and `test_prepared_source_helper_owns_normalized_record_construction`, which preserves normalized prepared-source fields while proving construction is delegated out of the preparation loop. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_export_assembler.py tests/test_diagnostic_export_contract.py tests/test_diagnostic_export_bundle_integration.py`; `make verify-fast` | Preserve diagnostic bundle deterministic archive ordering, duplicate source/path rejection before collection, source descriptor text validation, source descriptor byte/category/content-type semantics, public package default-source-byte export compatibility, normalized prepared-source field construction, metadata source payload shape, unsafe-path validation, redaction, truncation, JSON-safe payload output, manifest output, summary counts, included/omitted/error record semantics, exception summaries, fail-open source error behavior, stable ZIP metadata, archive_paths ordering, and the public diagnostics package facade while avoiding whole-source iterable materialization before validation, starred archive-entry unpacking, unnecessary single-source/payload sorting, assembler-owned ordering/summary/archive-entry helper bodies, assembler-owned stable ZIP writer mechanics, private ordering compatibility wrappers, assembler-owned manifest record helper bodies, inline included record construction in the assembler loop, assembler-owned per-source outcome branching, assembler-owned source result accumulation, source-result runtime imports of contract dataclasses, config-store protocol, and private prepared-source record for annotations only, bundle-layout runtime imports of contract dataclasses for annotations only, payload-encoding runtime imports of descriptor/redaction types for annotations only, records runtime imports of redaction config/metadata types for annotations only, assembler runtime imports of source descriptor/config-store types for annotations only, assembler runtime imports of private prepared-source record type, private record compatibility wrappers, diagnostics-internal package-facade sibling imports, records imports routed through the contract wrapper for source-record status/default vocabulary, package facade default-source-byte imports routed through the contract wrapper, duplicated source-count branches across manifest and bundle summaries, assembler-owned recursive JSON-safe payload normalization, private JSON-safe compatibility wrappers, payload-encoding JSON-safe wrapper aliases, duplicated bytes/string redacted-text encoding, dispatcher-owned JSON-object redaction/encoding, assembler-owned collection/redaction/encoding decisions, private payload compatibility wrappers, assembler-owned descriptor validation state, source-preparation imports routed through the contract wrapper for owner-module constants, private source-preparation compatibility wrappers, assembler-private archive-path re-exports, assembler-facade archive-path compatibility constants, assembler-facade re-exports of the source descriptor owner type, inline prepared-source record construction in the preparation loop, inline diagnostic metadata source payload construction, metadata descriptor-owned payload schema literals, archive-path generator frames, summary-time source serialization, recursive mapping items-view allocation, recursive comprehension frames during payload encoding, and duplicate local stripped-text helper logic. |
| Keep diagnostic archive path validation on single-pass segment scanning. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/archive_paths.py::_validate_archive_path()` now scans archive path segments with `_iter_archive_path_segments()` instead of allocating a `split()` parts list and running separate segment scans for dot and forbidden directory checks. Focused proof lives in `tests/test_diagnostic_export_assembler.py::test_archive_path_validation_scans_segments_without_split_list`, where a `str` subclass raises if segment iteration falls back to `split()`. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_export_assembler.py tests/test_diagnostic_export_contract.py tests/test_diagnostic_export_bundle_integration.py`; `make verify-fast` | Preserve unsafe archive path rejection, manifest collision rejection, deterministic archive ordering, duplicate path rejection before collection, redaction, truncation, and manifest output while avoiding repeated path-segment scans. |
| Keep diagnostic exact-secret redaction on preordered candidates. | request/status | code-path reasoning + focused regression proof | `app/diagnostics/secret_inventory.py` now owns ConfigStore secret inventory, provider-label normalization, stable key/label ordering, and exact-secret candidate ordering behind an explicit config-store factory so `app/diagnostics/redaction.py` can focus on applying redaction to text and payloads. `app/diagnostics/secret_inventory.py::_collect_configured_secret_candidates()` now orders exact configured-secret candidates by length once, and `app/diagnostics/text_rules.py::apply_exact_secret_redaction()` reuses that order instead of sorting candidates for every redacted string. Provider keys are sorted directly during candidate collection instead of sorting `(key, value)` item pairs through a lambda, and `_candidate_label_tuple()` now owns candidate-to-secret-label projection instead of keeping label-list accumulation in the collector body or using a generator-expression frame. Configured-secret collection now skips `sorted()` for empty or single-provider and empty or single-label inventory paths, and skips candidate list sorting for zero or one configured secret candidate, while preserving sorted output for multi-provider snapshots and longest-secret-first redaction for overlapping values. Configured provider-label inventory now deduplicates normalized labels during the provider scan instead of collecting a label list and copying it through `set()` before sorting. Configured-secret values are now stripped once through `_usable_configured_secret()` and reused for candidate storage instead of stripping once for validation and again for the retained secret value. `app/diagnostics/secret_inventory.py::_append_configured_secret_candidate()` now owns usable-secret validation plus `_SecretCandidate` construction, so VirusTotal and provider candidate collection share one append path while provider-label inventory still updates only for retained provider secrets. `app/diagnostics/payload_redaction.py` now owns recursive payload traversal, safe key coercion, scalar field redaction, cycle handling, depth limits, mapping traversal, and sequence accumulation, and `app/diagnostics/redaction.py::redact_diagnostic_payload()` now calls the traversal engine directly instead of keeping private payload traversal delegators. `app/diagnostics/redaction.py` also no longer re-exports payload traversal constants, secret-inventory public APIs, secret-inventory internals, or private accumulator/secret-length names through its public facade, so tests and callers that exercise those helpers import them from their owner modules or access private helpers explicitly. Recursive payload redaction still scans dict keys directly and accumulates redacted sequence items in a direct loop instead of allocating an `items()` view or list-comprehension frame while walking nested diagnostic payloads. `app/diagnostics/payload_redaction.py::redact_payload_mapping()` now owns direct dict key iteration, safe key coercion, payload key-label lookup, credential-like scalar redaction, and child recursion so `redact_payload_value()` stays focused on scalar/container dispatch plus cycle/depth policy. `app/diagnostics/payload_redaction.py::redact_payload_sequence()` now handles empty, single, pair, three-item, and four-item diagnostic sequences by direct index access before falling back to the long-path accumulation loop, avoiding iterator setup on the common short nested-payload paths. `app/diagnostics/payload_rules.py` now owns payload key-to-redaction-label classification for credential-like fields and auth headers, leaving `payload_redaction.redact_payload_mapping()` focused on applying that policy during mapping traversal. `app/diagnostics/text_rules.py::apply_exact_secret_redaction()` now owns exact configured-secret replacement, `apply_text_pattern_redaction()` owns common credential regex rule coordination, `_apply_text_rule()` owns per-rule callback/count mechanics, and `redact_text_with_candidates()` owns the combined exact-then-pattern text flow so `app/diagnostics/redaction.py` no longer keeps a private text-rule wrapper. `_normalize_label_part()` also reuses a compiled label-cleanup regex instead of calling module-level `re.sub()` for every provider label. `_RedactionAccumulator.metadata()` now reuses a cached sorted label tuple until a new label is added instead of sorting the same label set for repeated metadata reads, and skips `sorted()` entirely for empty or single-label metadata. `app/diagnostics/runtime_payloads.py::config_secret_inventory_payload_from_inventory()` now owns the public secret-inventory diagnostic payload shape and copies the preordered secret/provider label tuples with direct accumulation instead of constructor-copying them with `list(...)`; long label-copy append mutation delegates to `append_label_copy()`, leaving `config_secret_inventory_payload()` focused on inventory collection and config-error handling. Focused proof lives in `tests/test_diagnostic_redaction.py::test_exact_secret_redaction_reuses_preordered_candidates` and `test_configured_secret_candidates_keep_longest_first_redaction`, which preserve overlapping-secret safety while failing if exact redaction calls `sorted()`, `test_exact_secret_redaction_delegates_text_rule_helper`, which keeps exact-replacement loop/count mechanics in `text_rules.py` and proves the private redaction-module text wrapper is gone, `test_configured_secret_candidate_order_skips_sort_for_zero_or_one_candidate`, which fails if zero/single candidate ordering calls list sort, `test_configured_secret_candidate_append_owns_validation_and_construction`, which preserves trimming/min-length rejection while proving one helper owns candidate append construction, `test_candidate_label_tuple_owns_secret_label_projection`, which preserves secret-label projection and zero/single fast paths in the projection helper, `test_configured_secret_collection_avoids_item_pairs_and_generator_frames`, which makes provider `.items()` fail, guards the collector against `<genexpr>` bytecode, and proves collection delegates candidate construction and label projection to owner helpers while preserving provider/secret label order, `test_configured_secret_collection_strips_each_secret_once`, which fails if usable configured secrets are stripped once for validation and again for storage, `test_configured_secret_collection_skips_sort_for_single_provider`, which patches `builtins.sorted` to fail for the single-provider inventory path, `test_configured_secret_collection_accepts_explicit_store_factory`, which proves the default ConfigStore path is injectable without monkeypatching the module global, `test_payload_redaction_uses_direct_recursive_loops`, which makes payload `.items()` fail, proves mapping traversal is delegated, and guards the mapping helper against `<listcomp>` bytecode, `test_payload_sequence_redaction_skips_iteration_for_empty_single_pair_three_or_four_sequence`, which makes short diagnostic payload sequences fail on iteration or slicing while preserving redacted output for up to four values, `test_payload_mapping_redaction_owns_key_policy_and_child_traversal`, which keeps key policy lookup, scalar redaction, and recursive child traversal in the mapping helper, `test_payload_key_redaction_rules_live_outside_recursive_traversal`, which keeps field/header key classification out of recursive traversal, `test_redaction_delegates_payload_traversal_engine`, which keeps payload traversal mechanics and private payload traversal wrappers out of `redaction.py`, `test_redaction_facade_does_not_reexport_owner_module_internals`, which keeps payload traversal constants, text constants, secret-inventory public APIs and internals, private accumulator names, and secret-length policy constants out of the redaction facade, `test_text_pattern_redaction_delegates_rule_engine`, which keeps credential regex rule coordination in `apply_text_pattern_redaction()` while proving regex substitution now lives in `_apply_text_rule()`, `test_apply_text_rule_owns_callback_replacement_counting`, which preserves already-redacted token skips and replacement counting in the per-rule helper, `test_configured_secret_inventory_deduplicates_provider_labels_directly`, which patches `builtins.set` to fail while preserving provider-label dedupe, `test_redaction_metadata_reuses_sorted_label_snapshot`, which fails if unchanged metadata labels sort again, `test_redaction_metadata_skips_sort_for_zero_or_one_label`, which fails if common empty/single-label metadata paths call `sorted()`, `test_label_part_normalization_uses_compiled_regex`, which fails if label normalization returns to module-level `re.sub()`, plus `tests/test_diagnostic_export_sources.py::test_config_secret_inventory_payload_accumulates_labels_without_list_constructor`, `test_config_secret_inventory_payload_helper_owns_public_shape`, and `test_copy_label_tuple_delegates_long_path_append`, which guard the diagnostic source payload against label `list(...)` constructor calls and keep public secret-inventory payload shape and long label-copy mutation in runtime payload helpers. | R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_diagnostic_redaction.py tests/test_diagnostic_export_assembler.py tests/test_diagnostic_export_bundle_integration.py`; `make verify-fast` | Preserve configured-secret inventory labels, provider label normalization/dedupe, longest-secret-first exact replacement, pattern redaction, nested payload traversal, mapping key policy application, payload key classification, cycle safety, and secret-free diagnostic exports while avoiding repeated candidate sorting per string, provider item-pair sorting, repeated configured-secret stripping, repeated configured-secret candidate construction, collector-owned candidate-label projection, runtime source collection functions owning public payload shape, unnecessary zero/single candidate sorting, unnecessary single-item provider/label sorting, hidden ConfigStore construction in tests/callers that need an explicit secret store, secret-label generator frames, provider label list-to-set copies, recursive payload items-view/list-comprehension allocation, recursive traversal-owned mapping mechanics, recursive traversal-owned payload key classification, redaction-module-owned payload traversal mechanics, private payload traversal wrappers, redaction-facade re-exports of owner-module public or private internals, redaction-module-owned exact replacement loops, mixed text-rule coordination and callback mechanics, redaction-module-owned text regex replacement callbacks, private text-rule wrappers, redaction-module private text wrapper aliases, unnecessary metadata label sorting, repeated label-regex dispatch through module-level `re.sub()`, and diagnostic-source label constructor copies. |
| Keep normalized duplicate IOC candidates on the single-classification path in `run_pipeline()`. | IOC extraction pipeline | measurement + focused regression proof | The `pipeline-duplicate-candidates` capture exercises representative URL defang variants that all normalize to `http://evil.com`: the current `app/pipeline/extractor.py::run_pipeline()` canonical-value gate returns one IOC and calls `classify()` once instead of repeating classification for every raw variant. `app/pipeline/normalizer.py::normalize()` now returns already-clean values before running the defang regex substitution loop when no known defang sentinel is present. `app/pipeline/normalizer.py::_DEFANG_PATTERNS` is now an immutable tuple of compiled pattern/replacement pairs instead of a mutable list for the static defang table. The final type/value dedup path now appends directly to the output list with a `seen_keys` set instead of storing IOCs in a dict and returning `list(values())`. `run_pipeline()` now shares raw-candidate access, candidate normalization/classification, and IOC type/value identity through `_candidate_raw()`, `_classify_candidate_raw()`, and `_ioc_identity()` instead of repeating those rules across the single-candidate, pair-candidate, and long-list paths. `app/pipeline/classifier.py::classify()` also lowercases domain candidates once and reuses that value for blacklist lookup and IOC output. `app/pipeline/classifier.py::_DOMAIN_BLACKLIST` now uses a tuple-backed frozenset for static blacklist membership instead of a set literal at module import. IP candidate classification now parses with `ipaddress.ip_address()` once via `_classify_ip_type()` instead of probing IPv6 and IPv4 separately. Pipeline internals now import sibling pipeline modules through relative imports instead of round-tripping through the `app.pipeline` package facade. Focused proof lives in `tests/test_pipeline.py::test_pipeline_modules_use_relative_sibling_imports`, which keeps pipeline internals off package-facade sibling imports, `tests/test_pipeline.py::TestRunPipelineDeduplication::test_normalized_duplicate_variants_classified_once`, which preserves first-match `raw_match` semantics while proving duplicate normalized variants skip repeated classification, `test_type_value_duplicates_keep_first_output_order`, which preserves first-result order when distinct normalized candidates classify to the same IOC, `tests/test_pipeline.py::TestRunPipelineTypes::test_pipeline_dedup_rules_are_shared_helpers`, which guards the shared helper placement for candidate raw reads, candidate classification, and type/value identity checks, `tests/test_normalizer.py::TestEdgeCases::test_clean_input_skips_defang_pattern_loop`, which fails if clean input runs regex substitutions, `test_defang_patterns_are_static_tuple`, which guards the defang pattern table shape, `tests/test_classifier.py::TestClassifyDomain::test_domain_lowercase_value_is_reused`, which fails if the domain path lowercases twice, `test_domain_blacklist_uses_static_frozenset`, which guards the blacklist membership table shape, and `tests/test_classifier.py::TestClassifyIPv4::test_ipv4_classification_parses_ip_once`, which fails if an IPv4 candidate is parsed twice. | R085, R087, R040 | `python3 -m pytest -q tests/test_pipeline.py tests/test_extractor.py tests/test_normalizer.py tests/test_classifier.py`; `make verify-fast` | Preserve exact raw extraction behavior, all documented defang replacements, clean-input pass-through behavior, first-observed raw_match output, canonical IOC deduplication by type/value, output order, URL-before-IP precedence, IPv4/IPv6 classification semantics, domain blacklist semantics, lowercased domain output, and silent discard of unclassifiable candidates while avoiding regex substitution passes for clean values, mutable static defang pattern tables, repeated classify work for the same normalized value, duplicated raw-candidate/classification/IOC-identity logic across pipeline branches, the final dict-values copy, duplicate domain lowercasing, static blacklist set-literal allocation, duplicate IP parsing, and package-facade sibling imports in pipeline internals. |
| Keep raw IOC extraction deduplication on direct output-list accumulation. | IOC extraction pipeline | code-path reasoning + focused regression proof | `app/pipeline/extractor.py::extract_iocs()` now appends first-seen raw candidates directly to the returned list while tracking a `seen_raw` set, instead of storing candidates in a raw-keyed dict and returning `list(values())`. `app/pipeline/extractor.py::_EXPECTED_EXTRACTION_ERRORS` now centralizes the expected library failure tuple used by each extractor source instead of repeating it across every `except` block. Focused proof lives in `tests/test_extractor.py::TestDeduplicationInExtract::test_dedup_appends_first_seen_candidates_directly`, which patches all extractor sources and verifies duplicates keep the first type hint and first-seen output order, plus `tests/test_extractor.py::TestExtractEdgeCases::test_expected_extraction_errors_share_one_policy` and `test_expected_extraction_errors_fail_closed_without_warning`, which preserve fail-closed expected-error behavior while guarding the shared exception policy. | R085, R087, R040 | `python3 -m pytest -q tests/test_extractor.py tests/test_pipeline.py tests/test_normalizer.py tests/test_classifier.py`; `make verify-fast` | Preserve iocextract/iocsearcher merge order, raw-value deduplication, first type-hint semantics, exception tolerance, and downstream pipeline input shape while avoiding the intermediate candidate dict, final values-list copy, and duplicated expected-exception tuples. |
| Keep SSH auth.log parsing on streaming lines, direct BSD timestamps, and cached source classification. | IOC extraction pipeline | code-path reasoning + focused regression proof | `app/ssh/line_streams.py` now owns decoded bytes/text stream iteration and CR/LF line-ending cleanup, so `app/ssh/parser.py` can focus on SSH accepted-login matching, timestamps, source classification, warnings, and summary counts. `parse_auth_log()` calls `line_streams.iter_lines()` directly and increments `total_lines` as it parses, instead of reading the entire upload and materializing a line list before the parsing loop. `_parse_bsd_timestamp()` now uses a precomputed month map and direct `HH:MM:SS` integer extraction instead of `datetime.strptime()` for every BSD accepted-login line. `_classify_source()` now uses a bounded `lru_cache`, avoiding repeated `ipaddress.ip_address()` parsing and hostname exception paths for repeated source tokens. SSH internals now import sibling SSH modules through relative imports instead of round-tripping through the `app.ssh` package facade. Focused proof lives in `tests/test_ssh_parser.py::test_ssh_modules_use_relative_sibling_imports`, which keeps SSH internals off package-facade sibling imports, `TestStreamTypes::test_text_stream_is_not_read_all_at_once`, `TestStreamTypes::test_parser_delegates_line_stream_decoding_helpers`, `TestTimestampBSD::test_bsd_timestamp_parsing_does_not_use_strptime`, and `TestSourceExtraction::test_repeated_source_classification_is_cached`, which fail if text streams are read wholesale, line-stream helper implementations drift back into the parser module, BSD parsing falls back to `strptime()`, or repeated source tokens are reparsed. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_ssh_parser.py`; `make verify-fast` | Preserve BSD/RFC3339 accepted-login parsing, year rollover, IPv4/IPv6/hostname classification, partial-match warnings, malformed UTF-8 replacement, and summary invariants while avoiding full-file line-list allocation, parser-owned stream decoding/line cleanup, general-purpose BSD timestamp parsing, repeated source classification, and package-facade sibling imports for SSH auth.log uploads. |
| Keep `CacheStore.stats()` on one aggregate SQLite read. | local cache/history | query-count proof + focused regression proof | The `cache-stats-query-count` capture shows `CacheStore.stats()` now returns total entries and oldest timestamp through one `SELECT COUNT(*), MIN(cached_at)` aggregate query. Focused proof lives in `tests/test_cache_store.py::TestStats::test_stats_uses_single_aggregate_query`, while the existing stats tests preserve empty-cache, populated-cache, and concurrent-write behavior. | R085, R087, R022, R040 | `python3 -m pytest -q tests/test_cache_store.py`; `make verify-fast` | Preserve WAL-backed persistent connection behavior, total entry count, oldest cached timestamp semantics, clear/purge behavior, and thread-safe access while avoiding duplicate stats reads. |
| Keep nonpositive cache TTL reads on the pre-SQLite fast path. | local cache/history | query-count proof + focused regression proof | `app/cache/store.py::CacheStore.get()` now returns `None` before touching SQLite when `ttl_seconds <= 0`, matching the existing expired-entry contract without reading or decoding a row that cannot be fresh. Focused proof lives in `tests/test_cache_store.py::TestTTL::test_nonpositive_ttl_skips_cache_lookup`, which traces the connection and asserts no `SELECT` is issued. | R087, R022, R040 | `python3 -m pytest -q tests/test_cache_store.py`; `make verify-fast` | Preserve cache-miss behavior, positive-TTL hits, expiry semantics, WAL-backed store behavior, and no-error-caching assumptions while avoiding pointless reads when caching is effectively disabled. |
| Keep empty cache read/write payloads on JSON literals. | local cache/history | code-path reasoning + focused regression proof | `app/json_utils.py::EMPTY_JSON_OBJECT` centralizes the empty JSON object literal, and `CacheStore.put()` stores that literal for empty cache payloads through `encode_json_object()` instead of invoking `json.dumps({})` or retaining a cache-local literal alias. `CacheStore.get()` and `get_all_for_ioc()` mirror that fast path by returning empty dicts directly when stored payload columns are the literal `{}`, avoiding `json.loads()` for empty cache payloads. Focused proof lives in `tests/test_cache_store.py::TestPutAndGet::test_empty_payload_skips_json_encoding`, which patches the cache-store JSON encoder to fail, `test_empty_payload_skips_json_decoding`, which patches the decoder to fail while preserving `get()` and `get_all_for_ioc()` roundtrip behavior for empty payloads, and `test_empty_payload_uses_shared_json_literal_constant`, which guards the shared literal path. | R087, R022, R040 | `python3 -m pytest -q tests/test_cache_store.py::TestPutAndGet::test_empty_payload_skips_json_encoding tests/test_cache_store.py::TestPutAndGet::test_empty_payload_skips_json_decoding tests/test_cache_store.py::TestPutAndGet::test_empty_payload_uses_shared_json_literal_constant tests/test_cache_store.py::TestPutAndGet::test_roundtrip tests/test_cache_store.py::TestGetAllForIoc::test_get_all_for_ioc_returns_all_providers`; `make verify-fast` | Preserve cache put/get JSON shape, detail-page cache result loading, provider/cached_at metadata injection, and non-empty payload encoding/decoding while avoiding unnecessary JSON encoder and decoder calls for empty cache payloads, duplicated JSON literal strings, and cache-local JSON literal aliases. |
| Keep cache/history SQLite PRAGMA setup behind the shared `app.sqlite.configure_connection()` helper. | local cache/history | refactor proof + focused regression proof | `app/cache/store.py` and `app/enrichment/history_store.py` now share one SQLite connection-configuration helper instead of duplicating WAL, synchronous, busy-timeout, cache-size, and temp-store PRAGMA setup. Focused proof lives in `tests/test_sqlite.py`, while the existing cache/history store tests still verify WAL mode and busy-timeout on live store connections. | R022, R040 | `python3 -m pytest -q tests/test_sqlite.py tests/test_cache_store.py tests/test_history_store.py`; `make verify-fast` | Preserve WAL-backed persistent local-store behavior and keep future SQLite tuning centralized so cache and history stores do not drift. |
| Keep recent-history summaries on the SQL-side input preview projection. | local cache/history | code-path reasoning + focused regression proof | `app/enrichment/history_records.py` now owns pure history row shaping, JSON payload fast paths, retention input validation, and top-verdict derivation, leaving `app/enrichment/history_store.py` focused on SQLite connection/query lifecycle. `app/enrichment/history_records.py::_analysis_insert_record()` now owns persisted row ID/value tuple construction, including IOC/result JSON payload encoding, total_count, and top-verdict selection, so `HistoryStore.save_analysis()` stays focused on timestamping, locking, insert execution, pruning, and commit. `app/enrichment/history_store.py::HistoryStore.list_recent()` now selects `substr(input_text, 1, 120)` for summary rows instead of returning full pasted analyst input and slicing it in Python. It now accumulates returned summary dicts with a direct loop instead of allocating a list-comprehension frame around the already SQL-bounded rows. `app/enrichment/history_store.py` also no longer imports or exports unused private history-record constants, JSON helpers, verdict helpers, or row-shaping types through its store facade; those helpers stay directly importable from their owner module for focused tests. Focused proof lives in `tests/test_history_store.py::TestSaveAndLoad::test_save_analysis_delegates_insert_record_shaping`, which keeps insert row shaping out of `HistoryStore.save_analysis()` while preserving SQL value order, `tests/test_history_store.py::TestPublicExports::test_history_store_public_exports_exclude_record_private_helpers`, which keeps private history-record helpers out of the store facade attributes and public exports, `tests/test_history_store.py::TestListRecent::test_truncates_input_text`, which now also verifies `load_analysis()` still returns the complete saved input text, `test_list_recent_empty_single_pair_and_three_paths_use_row_count`, which keeps short recent-history summaries on direct row paths through four rows, and `test_list_recent_accumulates_summaries_without_list_comprehension`, which guards `list_recent()` against `<listcomp>` bytecode. | R087, R022, R040 | `python3 -m pytest -q tests/test_history_store.py`; `make verify-fast` | Preserve persisted analysis input fidelity, save-analysis SQL value order, total_count/top_verdict semantics, recent-history summary shape, and focused access to private history-record helpers while avoiding HistoryStore-owned insert row shaping, history-store facade imports or re-exports of private record helpers, full input-text transfer into Python for list views, and comprehension-frame allocation around bounded summary rows. |
| Keep empty history save/load payloads on JSON literals. | local cache/history | code-path reasoning + focused regression proof | `app/json_utils.py::EMPTY_JSON_ARRAY` centralizes the empty JSON array literal, and `HistoryStore.save_analysis()` stores that literal for empty IOC and result payloads through `encode_json_array()` instead of invoking `json.dumps([])` for each empty list or retaining a history-record local literal alias. `HistoryStore.load_analysis()` delegates row shaping to `app/enrichment/history_records.py::_analysis_from_row()`, which mirrors that fast path by returning empty lists directly when stored payload columns are the literal `[]`, avoiding `json.loads()` for empty history payloads. Focused proof lives in `tests/test_history_store.py::TestSaveAndLoad::test_empty_payloads_skip_json_encoding`, which patches the history-store JSON encoder to fail, `test_empty_payloads_skip_json_decoding`, which patches the decoder to fail, and `test_empty_payloads_use_shared_json_literal_constant`, which guards the shared literal path, while preserving save/load roundtrip behavior for empty payloads. | R087, R022, R040 | `python3 -m pytest -q tests/test_history_store.py::TestSaveAndLoad::test_empty_payloads_skip_json_encoding tests/test_history_store.py::TestSaveAndLoad::test_empty_payloads_skip_json_decoding tests/test_history_store.py::TestSaveAndLoad::test_empty_payloads_use_shared_json_literal_constant tests/test_history_store.py::TestSaveAndLoad::test_roundtrip`; `make verify-fast` | Preserve history save/load JSON shape, empty IOC/result roundtrips, non-empty payload encoding/decoding, and top-verdict fallback while avoiding unnecessary JSON encoder and decoder calls for empty saved payloads, duplicated JSON literal strings, and history-record local JSON literal aliases. |
| Keep history top-verdict computation on the malicious short-circuit path. | local cache/history | code-path reasoning + focused regression proof | `app/enrichment/history_records.py::_compute_top_verdict()` now returns immediately when it sees `_MAX_VERDICT`, the maximum verdict severity, instead of scanning the remaining saved results. `_FALLBACK_VERDICT` also centralizes the no-verdict/error-only fallback. Focused proof lives in `tests/test_history_store.py::TestComputeTopVerdictUnit::test_malicious_verdict_short_circuits_scan`, while `test_top_verdict_terminal_constants_are_precomputed` guards the terminal verdict constants and `test_short_top_verdict_paths_skip_iteration` preserves no-iteration fast paths up to four saved results alongside the existing priority-order tests for malicious/suspicious/no_data/clean/error semantics. | R087, R022, R040 | `python3 -m pytest -q tests/test_history_store.py`; `make verify-fast` | Preserve saved-analysis top_verdict semantics while avoiding unnecessary result-row inspection once the maximum severity has been found and avoiding duplicated terminal verdict literals. |
| Keep history verdict priority on a precomputed map. | local cache/history | code-path reasoning + focused regression proof | `app/enrichment/history_records.py::_VERDICT_PRIORITY` now stores the top-verdict severity map once at module load, and `_compute_top_verdict()` reuses it instead of rebuilding the same dict for every saved analysis. `_MAX_VERDICT` and `_FALLBACK_VERDICT` keep the scan's terminal verdict constants next to that precomputed priority map. Focused proof lives in `tests/test_history_store.py::TestComputeTopVerdictUnit::test_priority_map_is_precomputed` and `test_top_verdict_terminal_constants_are_precomputed`, while existing priority and malicious short-circuit tests preserve verdict semantics. | R087, R022, R040 | `python3 -m pytest -q tests/test_history_store.py`; `make verify-fast` | Preserve saved-analysis top_verdict priority order, error-only fallback, malicious short-circuiting, and history save/load behavior while avoiding repeated priority-map allocation. |
| Keep `ProviderRegistry` filters on direct provider scans. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/registry.py::all()` now keeps its public mutable-list return contract while accumulating providers from registry keys directly through `append_registered_provider()` instead of allocating a dict-values view and then copying it. `app/enrichment/registry.py::configured()` and `providers_for_type()` now keep their public list-returning contracts while accumulating matching providers from registry keys directly during the registry scan through `append_configured_provider()` and `append_provider_for_type()` instead of allocating values views or list-comprehension frames. `provider_count_for_type()` also counts matching configured providers from registry keys in a direct loop instead of calling `providers_for_type()` and allocating a provider list just to take its length, routing the scan through a `sum()` generator, or allocating a values view. `_provider_supports_configured_type()` centralizes the shared configured/type eligibility predicate used by both list and count scans. Focused proof lives in `tests/test_provider_registry.py::TestRegistryRegister::test_all_accumulates_without_values_view`, which makes the all-provider path fail if it calls `values()`, `test_all_delegates_registered_provider_append`, which keeps the registry-map append mutation delegated to `append_registered_provider()`, `TestRegistryListReturningFilters::test_list_filters_do_not_allocate_comprehension_frames`, which guards the list-returning filters against `<listcomp>` bytecode, `test_list_filters_scan_without_values_view`, which makes configured/type filters fail on `values()`, `test_list_filters_delegate_append_mutation`, which keeps configured/type append mutation delegated to `append_configured_provider()` and `append_provider_for_type()`, `TestRegistryProvidersForType::test_providers_for_type_uses_shared_eligibility_predicate`, which proves the list path uses the shared predicate, `TestRegistryProviderCountForType::test_count_does_not_allocate_provider_list`, which makes the count fail if it falls back to `providers_for_type()`, `test_count_does_not_use_sum_generator`, which patches `builtins.sum` to fail, `test_count_scans_without_values_view`, which makes the count path fail on `values()`, and `test_count_uses_shared_eligibility_predicate`, which proves the count path uses the same predicate while preserving count semantics. | R085, R087, D080 | `python3 -m pytest -q tests/test_provider_registry.py tests/test_registry_setup.py`; `make verify-fast` | Preserve all-provider reads, configured-provider filtering, returned list mutability, insertion order, and IOC type support semantics while keeping registry scans free of unnecessary values-view, provider-list, list-comprehension, generator allocation, and duplicated configured/type eligibility logic. |
| Keep static adapter frozensets on tuple inputs. | provider registration/config diagnostics | code-path reasoning + focused regression proof | Literal provider adapter `supported_types` constants now call `frozenset((...))` instead of `frozenset({...})`, avoiding temporary set-literal allocation during adapter module import while preserving the public immutable set contract. `app/enrichment/adapters/shodan.py::_MALICIOUS_TAGS` uses the same tuple-input shape for its static membership table. Adapter modules now import sibling adapters and enrichment owner modules through relative imports instead of round-tripping through the `app.enrichment` package facade. Focused proof lives in `tests/test_adapter_contract.py::test_adapter_static_frozensets_avoid_temporary_set_literals`, which scans adapter source files and fails if static `frozenset({` literals return in inline or whitespace-separated form, and `tests/test_adapter_contract.py::test_adapter_modules_use_relative_enrichment_imports`, which keeps adapter imports off the package facade while the existing adapter contract suite preserves supported-type behavior for every adapter. | R008, R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve adapter supported_types frozenset contracts, registry setup, unsupported-type guards, and Shodan malicious-tag matching while avoiding temporary set-literal allocation for static adapter membership tables and package-facade sibling imports in adapter modules. |
| Keep ASN TXT parsing on first-record and direct field extraction. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/asn_cymru.py::CymruASNAdapter.lookup()` now delegates IPv4/IPv6 Cymru DNS query-name construction to `_cymru_query_name()` and DNS resolver setup/lifetime policy to `_configured_resolver()`, keeping reverse-pointer zone replacement and resolver construction rules out of the error-handling flow. `CymruASNAdapter.lookup()` also reads the first DNS TXT answer with `next(iter(answers))` instead of materializing all answer records with `list(answers)[0]`. `app/enrichment/adapters/dns_txt.py::decode_txt_chunks()` now owns empty, single, pair, three, and four-chunk TXT decoding for both ASN Intel and DNS Records, so `_decode_txt_strings()` keeps provider parsing local while sharing bounded chunk mechanics and only joining long segmented TXT records. `_parse_txt_fields()` also extracts the pipe-delimited ASN/prefix/RIR/allocation fields directly instead of building a stripped `split()` parts list or accumulating an intermediate field list. `_parse_response()` now delegates stable ASN metadata shaping to `_asn_raw_stats()`, keeping field unpacking and raw_stats literals out of the parser body. `_no_data_result()` now owns the shared informational Cymru result shape for DNS misses and parsed ASN context instead of repeating the same constructor arguments across lookup branches. Focused proof lives in `tests/test_asn_cymru.py::TestQueryConstruction::test_lookup_delegates_dns_query_name_helper`, `test_configured_resolver_helper_preserves_lifetime_policy`, `test_query_name_helper_preserves_ipv4_and_ipv6_zones`, `tests/test_asn_cymru.py::TestSuccessfulLookup::test_txt_answer_uses_first_record_without_materializing_all_answers`, where a generator raises if ASN parsing reads past the first record, `test_short_chunk_txt_answers_skip_join_iteration` and `test_multi_chunk_txt_answer_still_concatenates_segments`, which preserve TXT decoding semantics while proving ASN delegates to the shared TXT chunk decoder, `test_txt_parse_does_not_allocate_split_parts`, where a string subclass raises if parsing calls `split()`, `test_parse_response_delegates_raw_stats_helper`, which keeps ASN raw_stats mechanics out of `_parse_response()`, `test_raw_stats_helper_preserves_key_order_and_values`, which preserves the public raw_stats envelope, `test_txt_field_parser_does_not_build_intermediate_field_list`, which fails if the field parser emits list-building bytecode, and `test_no_data_result_helper_preserves_informational_shape`, which preserves the centralized no-data result contract. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_asn_cymru.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve Team Cymru query construction, resolver lifetime, TXT field semantics including empty and segmented TXT concatenation, ASN raw_stats fields/order, no-data verdict behavior, DNS error handling, fresh resolver behavior, and no-HTTP/no-requests boundaries while avoiding lookup-owned reverse-pointer zone replacement rules, lookup-owned resolver setup, unnecessary DNS answer list materialization, adapter-local TXT chunk mechanics, TXT field-list materialization, parser-owned ASN raw_stats mechanics, and duplicated no-data result construction. |
| Keep DNS record extraction on table-driven dispatch. | provider registration/config diagnostics | refactor proof + focused regression proof | `app/enrichment/adapters/dns_lookup.py::DnsAdapter.lookup()` now carries each DNS record type's raw_stats key and extractor in `_RECORD_TYPES`, so the lookup loop calls the table extractor directly instead of running an `if`/`elif` chain for A/MX/NS/TXT after each resolver call. `_configured_resolver()` now owns DNS resolver construction and the shared lifetime policy, `_empty_raw_stats()` owns the default DNS raw_stats envelope with fresh per-call lists, and `_resolve_record_type()` owns per-record resolver exception mapping so `DnsAdapter.lookup()` no longer carries resolver setup, raw_stats initialization, or DNS exception policy inline. The A/NS, MX, and TXT extractors now use direct loops instead of allocating list-comprehension frames per resolved record set. `app/enrichment/adapters/dns_txt.py::decode_txt_chunks()` now owns empty, single, pair, three, and four-chunk TXT decoding for both DNS Records and ASN Intel, so `_decode_txt_record()` keeps DNS record extraction local while sharing bounded chunk mechanics and only joining long segmented TXT records. `app/enrichment/adapters/dns_lookup.py::_dns_result()` now owns the shared informational no-data result envelope for DNS record responses instead of keeping verdict, counts, scan-date, and raw_stats constructor wiring in the resolver loop. DNS result construction now uses one informational provider envelope helper. Focused proof lives in `tests/test_dns_lookup.py::TestSuccessfulLookup::test_lookup_uses_record_table_extractors`, `test_lookup_delegates_raw_stats_and_record_resolution_helpers`, `test_configured_resolver_helper_preserves_lifetime_policy`, `tests/test_dns_lookup.py::TestRawStatsStructure::test_empty_raw_stats_helper_preserves_key_order_and_fresh_lists`, `tests/test_dns_lookup.py::TestPartialFailure::test_record_resolution_helper_preserves_error_mapping`, `test_record_resolution_helper_preserves_expected_empty_outcomes`, `tests/test_dns_lookup.py::TestTXTRecords::test_short_chunk_txt_records_skip_join_iteration`, which proves DNS delegates TXT chunk mechanics to the shared decoder, `tests/test_dns_lookup.py::TestRecordExtractorImplementation::test_record_extractors_do_not_allocate_list_comprehension_frames`, `test_record_extractors_skip_iteration_for_empty_single_pair_three_or_four_answers`, which keeps A/NS text, MX, and TXT extraction on direct paths for up to four answers, and `tests/test_dns_lookup.py::TestSuccessfulLookup::test_result_helper_preserves_provider_envelope`, while the existing A/MX/NS/TXT extraction tests preserve exact raw_stats formatting. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_dns_lookup.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve DNS-only/no-HTTP behavior, resolver lifetime, record order, A/MX/NS/TXT raw_stats formats including empty and multi-chunk TXT concatenation, NXDOMAIN/NoAnswer no-data semantics, NoNameservers/Timeout lookup error strings, parsed-response total_engines, and per-record lookup error collection while removing repeated record-type branch dispatch inside DNS lookups, lookup-owned resolver setup, lookup-owned raw_stats initialization, lookup-owned DNS exception mapping, list-comprehension extractor frames, adapter-local TXT chunk mechanics, and duplicated DNS result-envelope construction. |
| Keep VirusTotal engine total computation in the stats scan. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/virustotal.py::_parse_response()` now delegates analysis-map extraction to `_analysis_map()`, engine count scanning to `_engine_counts()`, verdict selection to `_virustotal_verdict()`, timestamp conversion to `_scan_date()`, and raw_stats assembly to `_virustotal_raw_stats()`, leaving the parser focused on wiring the shared result envelope. `_engine_counts()` computes `malicious` and total engine count while scanning `last_analysis_stats` keys once instead of calling `sum(stats.values())`, allocating an `items()` view, and then subtracting excluded buckets. `_EXCLUDED_ENGINE_STATUSES` now owns the timeout/type-unsupported exclusion table as a static frozenset instead of rebuilding that membership set inside the stats loop. `_top_detections()` now scans `last_analysis_results` keys directly instead of allocating a `values()` view while preserving first-five unique malicious detection names. `VTAdapter` now inherits the shared `BaseHTTPAdapter.lookup()` pipeline and delegates generic 404 no-data and 429 rate-limit handling to the shared base-adapter helpers, keeping only VirusTotal-specific auth handling in `_make_pre_raise_hook()` instead of repeating safe_request dispatch and parse/error propagation locally. `VTAdapter.supported_types` now derives directly from `ENDPOINT_MAP`, and `app/enrichment/adapters/virustotal.py::_virustotal_result()` now owns the shared provider envelope for 404 no-data hooks and parsed API responses instead of repeating provider, scan-date, total_engines, and raw_stats constructor wiring across branches. Focused proof lives in `tests/test_vt_adapter.py::TestLookupSuccess::test_total_engine_count_does_not_use_sum_helper`, where `builtins.sum` is patched to fail and stats raise if parsing calls `.items()` while detection and total engine counts are preserved, `test_engine_status_exclusions_use_static_frozenset`, which fails if the parser rebuilds excluded-status membership sets, `test_top_detections_do_not_allocate_values_view`, where analysis results raise if parsing calls `values()` while preserving top-detection dedupe, `test_parse_response_delegates_stats_verdict_and_raw_stats_helpers`, `test_adapter_uses_base_lookup_with_status_hook`, `tests/test_base_adapter.py::TestPreRaiseHook::test_shared_429_helper_returns_rate_limit_error`, `test_engine_counts_and_verdict_helper_preserve_semantics`, `test_top_detections_helper_preserves_unique_cap`, `test_raw_stats_helper_preserves_stats_and_reputation`, plus `tests/test_vt_adapter.py::test_supported_types_derive_from_endpoint_map` and `tests/test_vt_adapter.py::TestLookupSuccess::test_result_helper_preserves_provider_envelope`, which fail if supported IOC types drift from endpoint routing, parser helpers drift, the shared lookup pipeline is bypassed, generic status helpers drift, or the centralized VirusTotal result-envelope contract changes. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_vt_adapter.py tests/test_vt_context_fields.py tests/test_adapter_contract.py`; `make verify-fast` | Preserve VirusTotal endpoint mapping, 404/no-data behavior, rate-limit/auth errors, verdict semantics, excluded timeout/type-unsupported buckets, top detections, reputation, parsed-response total_engines, and HTTP safety controls while avoiding parser-owned stats/verdict/raw_stats mechanics, provider-local generic 404/429 hook logic, duplicated lookup/safe_request dispatch, an extra stats values/sum pass, stats items-view allocation, per-parse excluded-status set construction, analysis-result values-view allocation, duplicated supported-type declarations, and duplicated VirusTotal result-envelope construction. |
| Keep ThreatMiner capped result extraction bounded. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/threatminer.py` now applies passive-DNS and related-sample caps inside extraction loops instead of building full result arrays and slicing after the fact. Those extractors also return immediately for zero or negative caps, so disabled extraction paths do not touch result iterables. `_threatminer_request_url()` now owns ThreatMiner query-string construction so `_call()` only dispatches through `safe_request()` with session, allowlist, IOC, and provider context. `_has_results()` now owns ThreatMiner's body-level 404/empty-results gate, while `_passive_dns_raw_stats()`, `_samples_raw_stats()`, and `_domain_raw_stats()` own IP/domain/hash raw_stats assembly so lookup paths no longer repeat result-body checks or inline raw_stats dictionaries. `_no_data_result()` now owns the shared informational ThreatMiner result shape used by IP, domain, and hash lookup paths instead of repeating the same constructor arguments in each branch. Defensive dict sample rows now scan keys directly instead of allocating a `values()` view while extracting the first string value. Focused proof lives in `tests/test_threatminer.py::TestIPLookup::test_ip_lookup_passive_dns_stops_at_cap`, `TestDomainLookup::test_domain_lookup_samples_stop_at_cap`, and `TestHashLookup::test_hash_lookup_samples_stop_at_cap`, where bounded iterables raise if extraction reads past the documented caps, `TestIPLookup::test_zero_cap_passive_dns_skips_result_iteration`, `test_empty_single_pair_three_and_four_passive_dns_lists_skip_accumulator_loop`, `TestDomainLookup::test_zero_cap_samples_skip_result_iteration`, and `test_empty_single_pair_three_and_four_sample_lists_skip_accumulator_loop`, where exploding/no-iteration iterables prove zero caps and short result lists up to four rows avoid fallback iteration, `TestDomainLookup::test_dict_sample_rows_do_not_allocate_values_view`, where dict rows raise if sample extraction calls `values()`, `TestNoDataHandling::test_call_delegates_request_url_construction`, `test_request_url_helper_preserves_query_shape`, `test_lookup_paths_delegate_result_gates_and_raw_stats_helpers`, `test_result_body_gate_preserves_404_and_empty_semantics`, `test_raw_stats_helpers_preserve_public_shapes`, and `test_domain_raw_stats_omits_empty_extracted_lists`, plus `TestNoDataHandling::test_no_data_result_helper_preserves_informational_shape`, which preserves the centralized no-data result contract. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_threatminer.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve ThreatMiner IP/domain/hash routing, query-string shape, two-call domain lookup, passive DNS and sample raw_stats shapes, domain-only omission of empty extracted lists, no-data semantics, defensive dict sample handling, and HTTP safety controls while avoiding full oversized result scans beyond the published caps, dispatch-owned request URL construction, repeated lookup-path body gates, lookup-owned raw_stats dictionary assembly, zero-cap result iteration, dict values-view allocation in sample fallback rows, and duplicated no-data result construction. |
| Keep crt.sh certificate parsing on one body scan. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/base.py::BaseHTTPAdapter.lookup()` now treats only `EnrichmentResult`/`EnrichmentError` as terminal safe_request outputs, so successful JSON arrays flow through `_parse_response()` instead of forcing list-backed providers to repeat the lookup pipeline. `app/enrichment/adapters/crtsh.py::CrtShAdapter` now inherits that shared lookup pipeline for crt.sh JSON-list responses instead of repeating unsupported-type checks, safe_request dispatch, and parse/error propagation locally. `app/enrichment/adapters/crtsh.py::_parse_response()` now delegates certificate metadata shaping to `_crtsh_raw_stats()`, which computes earliest/latest certificate dates while scanning certificate rows for SAN subdomains, avoiding a separate date-list build plus `min()`/`max()` pass while keeping raw_stats literals out of the parser body. crt.sh certificate parsing now delegates raw_stats shaping while still using one body scan. `_iter_name_values()` streams newline-delimited SAN names from each `name_value` string instead of allocating a per-certificate split list. `_capped_sorted_subdomains()` also skips sorting empty, single-subdomain, and short multi-subdomain sets up to four entries, and selects the first 50 alphabetical subdomains with `heapq.nsmallest()` when the deduplicated set exceeds the raw_stats cap instead of sorting the full set and slicing. `app/enrichment/adapters/crtsh.py::_crtsh_result()` now owns the shared informational no-data result envelope for empty and populated certificate responses instead of repeating verdict, counts, scan-date, and raw_stats constructor wiring across parse branches. Focused proof lives in `tests/test_base_adapter.py::TestLookupDispatch::test_successful_json_list_body_is_parsed`, `tests/test_crtsh.py::TestCertDataExtraction::test_adapter_uses_base_lookup_for_json_list_response`, `test_date_range_and_subdomains_computed_in_one_body_scan`, where a single-pass body raises if parsing iterates certificate rows more than once, `test_parse_response_delegates_raw_stats_helper`, which keeps certificate raw_stats mechanics out of `_parse_response()`, `test_raw_stats_helper_preserves_key_order_and_values`, which preserves the public raw_stats envelope, `test_name_value_parsing_does_not_allocate_split_list`, where a string subclass raises if SAN parsing calls `split()`, `test_empty_or_single_subdomain_sets_skip_sorting`, where `builtins.sorted` is patched to fail for empty/single sets, `test_four_subdomain_sets_skip_sorting`, which proves direct comparison ordering for four subdomains, `test_subdomain_cap_avoids_full_sorted_list`, where `builtins.sorted` is patched to fail for oversized sets, and `test_result_helper_preserves_provider_envelope`, which preserves the centralized crt.sh result-envelope contract. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_crtsh.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve crt.sh domain-only support, cert counts, earliest/latest date semantics, raw_stats fields/order, wildcard stripping, lowercase/deduplicated/sorted subdomains, first-50 alphabetical cap, no-data verdict behavior, parsed-response total_engines, and HTTP safety controls while avoiding duplicated crt.sh lookup/safe_request dispatch, parser-owned certificate raw_stats mechanics, a second scan for date range calculation, per-certificate SAN split-list allocation, unnecessary empty/single subdomain sorting, full oversized subdomain sorting, and duplicated crt.sh result-envelope construction. |
| Keep Shodan malicious-tag detection on a direct count. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/shodan.py::_parse_response()` now delegates malicious-tag counting to `_malicious_tag_count()`, keeping the existing short-list no-iteration paths while still counting duplicate bad tags directly instead of building a temporary filtered `bad_tags` list and taking its length. `_shodan_signals()` now owns InternetDB list-field extraction and missing-list defaults, while `_shodan_verdict()` owns no-data/suspicious/malicious priority so `_parse_response()` no longer carries Shodan signal extraction or verdict branching. `_raw_stats()` now owns the stable InternetDB raw_stats envelope for ports, vulnerabilities, tags, hostnames, and CPEs instead of rebuilding the same dictionary in every verdict branch. `app/enrichment/adapters/base.py::BaseHTTPAdapter._make_pre_raise_hook()` now applies Shodan's generic 404 no-data hook policy from the `_no_data_on_404` class flag instead of keeping a provider-local hook method in the adapter. `_shodan_result()` now owns the shared provider envelope for both InternetDB 404 no-data hooks and parsed malicious/suspicious/no-data responses instead of repeating provider, scan-date, and raw_stats constructor wiring across branches. Focused proof lives in `tests/test_shodan.py::TestLookupFound::test_malicious_tag_count_preserves_duplicate_bad_tags`, which preserves duplicate bad-tag detection counts and raw tag stats, `test_parse_response_delegates_raw_stats_and_tag_count_helpers`, which keeps signal extraction, verdict branching, raw_stats, and tag-count mechanics out of `_parse_response()`, `test_shodan_signal_helper_preserves_list_identity_and_defaults`, which preserves list identity and missing-field defaults in the shared signal helper, `test_verdict_helper_preserves_priority_and_counts`, which preserves Shodan signal priority and counts, `test_raw_stats_helper_preserves_list_identity_and_key_order`, which preserves the raw_stats shape without copying response lists, `test_malicious_tag_count_preserves_short_no_iteration_paths`, which preserves the short-list tag-count paths up to four tags, `tests/test_base_adapter.py::TestPreRaiseHook::test_shared_404_hook_returns_no_data_result`, `test_generic_status_policy_flags_build_shared_hook`, and `test_result_helper_preserves_provider_envelope`, which preserves the centralized result-envelope contract. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_shodan.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve Shodan IPv4/IPv6 support, malicious-tag priority over vulnerability suspicion, duplicate bad-tag counting, missing-list defaults, 404 no-data behavior, parsed-response total_engines, raw_stats passthrough, raw_stats list identity, and HTTP safety controls while avoiding provider-local generic 404 hook methods, parser-owned signal extraction, parser-owned verdict branching, intermediate bad-tag list allocation, duplicated verdict-branch raw_stats construction, and duplicated result-envelope construction. |
| Keep EmailRep malicious verdict selection on the risk-flag scan. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/emailrep.py::_risk_flags()` now returns both the ordered risk flag list and whether any high-confidence malicious flag was present, so `_parse_response()` does not rescan malicious flag fields with `any()` after building raw_stats. `_NO_REPUTATION_VALUES` and `_DETECTION_VERDICTS` now hold the static verdict membership tables instead of rebuilding small sets during every parse. `app/enrichment/adapters/emailrep.py::_emailrep_signals()` now owns response details, reputation, suspicious, and reference extraction with provider-specific defaults, while `_parse_response()` delegates verdict classification to `_emailrep_verdict()`, detection-count selection to `_emailrep_detection_count()`, and stable raw_stats assembly to `_emailrep_raw_stats()`, keeping EmailRep response-field reads, reputation branching, verdict-count membership checks, profile normalization, and raw_stats literals out of the parser body. `app/enrichment/adapters/emailrep.py::_emailrep_result()` now owns the shared provider envelope for parsed EmailRep reputation responses instead of keeping provider, scan-date, total_engines, and raw_stats constructor wiring inside the verdict parser. Focused proof lives in `tests/test_emailrep.py::TestEmailRepLookup::test_verdict_uses_risk_flag_scan_without_second_malicious_pass`, where `builtins.any` is patched to fail while malicious verdict and risk flags are preserved, `test_verdict_membership_tables_are_static_frozensets`, which fails if the parser rebuilds verdict membership sets, `test_parse_response_delegates_verdict_count_and_raw_stats_helpers`, which keeps signal extraction plus verdict/count/raw_stats mechanics out of `_parse_response()`, `test_signal_helper_preserves_defaults_and_details_reuse`, which preserves response-field defaults and details identity, `test_verdict_and_detection_helpers_preserve_semantics`, `test_raw_stats_helper_preserves_key_order_and_list_identity`, and `test_profiles_helper_reuses_lists_and_normalizes_missing_values`, plus `tests/test_emailrep.py::TestEmailRepLookup::test_result_helper_preserves_provider_envelope`, which fails if the centralized EmailRep result-envelope contract changes. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_emailrep.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve EmailRep email-only support, auth headers, response-field defaults, details identity, high-confidence malicious verdict mapping, suspicious/clean/no-data behavior, ordered risk_flags raw_stats, raw_stats fields/order, profile passthrough/list identity, parsed-response total_engines, scan-date passthrough, and HTTP safety controls while avoiding a second malicious-flag scan, per-parse verdict membership set construction, parser-owned response-field extraction, parser-owned verdict/count/raw_stats mechanics, and duplicated EmailRep result-envelope construction. |
| Keep AbuseIPDB parsed result construction behind one provider envelope helper. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/abuseipdb.py::_abuseipdb_signals()` now owns AbuseIPDB `data` envelope extraction plus score/report/user/date defaults, while `_parse_response()` delegates threshold verdict selection to `_abuseipdb_verdict()` and stable raw_stats assembly to `_abuseipdb_raw_stats()`, leaving the parser focused on coordinating signals with the shared result helper. `app/enrichment/adapters/base.py::BaseHTTPAdapter._make_pre_raise_hook()` now applies AbuseIPDB's generic 429 rate-limit error policy from the `_rate_limit_on_429` class flag instead of keeping a provider-local hook method in the adapter. `app/enrichment/adapters/abuseipdb.py::_abuseipdb_result()` now owns the shared provider envelope for parsed AbuseIPDB reputation responses instead of keeping provider, scan-date, total_engines, and raw_stats constructor wiring inside the score-threshold parser. Focused proof lives in `tests/test_abuseipdb.py::TestAbuseIPDBLookup::test_parse_response_delegates_verdict_and_raw_stats_helpers`, which keeps data-envelope extraction, score branching, and raw_stats literals out of `_parse_response()`, `test_signal_helper_preserves_data_defaults_and_identity`, which preserves `data` identity and missing-envelope defaults, `test_verdict_helper_preserves_thresholds`, which preserves malicious/suspicious/clean/no_data threshold semantics, `test_raw_stats_helper_preserves_key_order_and_values`, `tests/test_base_adapter.py::TestPreRaiseHook::test_shared_429_helper_returns_rate_limit_error`, `test_generic_status_policy_flags_build_shared_hook`, which preserves the shared rate-limit string and declarative hook path, and `tests/test_abuseipdb.py::TestAbuseIPDBLookup::test_result_helper_preserves_provider_envelope`, which fail if generic 429 handling or the centralized AbuseIPDB result-envelope contract changes. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_abuseipdb.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve AbuseIPDB IPv4/IPv6 support, capital Key auth header, Accept header, data-envelope defaults and identity, score threshold verdict mapping, totalReports/numDistinctUsers count mapping, lastReportedAt scan-date passthrough, raw_stats fields/order, rate-limit hook, and HTTP safety controls while avoiding provider-local generic 429 hook methods, parser-owned data-envelope extraction, parser-owned score branching, parser-owned raw_stats envelope construction, and duplicated AbuseIPDB result-envelope construction. |
| Keep ThreatFox best-record selection on a short-circuiting confidence scan. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/threatfox.py::_select_best_record()` now scans records directly and returns as soon as a perfect confidence record is seen instead of using `max(..., key=lambda ...)` over every returned record. `app/enrichment/adapters/abusech.py::abusech_data_records()` now owns the shared abuse.ch query-status and data-list normalization gate used by ThreatFox and MalwareBazaar, so their parsers do not duplicate no-result status checks or missing-data list coercion. `_parse_response()` now delegates confidence threshold mapping to `_threatfox_verdict()` and selected-record metadata shaping to `_threatfox_raw_stats()`, keeping threshold decisions and raw_stats literals out of the parser body after best-record selection. `app/enrichment/adapters/threatfox.py::_threatfox_result()` now owns the shared provider envelope for no-result and selected-record responses instead of repeating provider, scan-date, total_engines, and raw_stats constructor wiring across parse branches. Focused proof lives in `tests/test_threatfox.py::TestMultipleResults::test_best_record_selection_short_circuits_on_perfect_confidence`, `test_best_record_selection_skips_iteration_for_zero_to_four_records`, which keeps short returned record sets on direct indexed comparison paths, `tests/test_threatfox.py::TestEdgeCases::test_parse_response_delegates_verdict_and_raw_stats_helpers`, which guards `_parse_response()` against regrowing threshold/raw_stats mechanics or abuse.ch data-gate logic, `tests/test_abusech.py::test_data_records_preserve_no_result_and_missing_data_contract`, which preserves no-result and missing-data behavior through the shared helper, `test_raw_stats_helper_preserves_key_order_and_values`, which preserves the public raw malware metadata envelope, `tests/test_threatfox.py::TestConfidenceThreshold::test_verdict_helper_preserves_confidence_threshold`, and `tests/test_threatfox.py::TestEdgeCases::test_result_helper_preserves_provider_envelope`, which preserve best-record selection, confidence thresholding, and the centralized ThreatFox result-envelope contract. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_threatfox.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve ThreatFox hash and IOC lookup routing, confidence threshold semantics, highest-confidence record selection, raw malware metadata fields/order, no-result semantics, missing-data no-data behavior, parsed-response total_engines, and HTTP safety controls while avoiding callback-based full scans when the best possible confidence appears early, parser-owned abuse.ch query-status/data-list normalization, parser-owned confidence verdicting/raw_stats construction, and duplicated ThreatFox result-envelope construction. |
| Keep IP Context geo and ASN/ISP formatting on direct string construction. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/ip_api.py::_ip_context_signals()` now owns ipinfo response-field extraction, provider defaults, fresh flags creation, and missing-country validation, while `app/enrichment/adapters/ip_api.py::_parse_response()` delegates ASN/ISP parsing to `_asn_context()`, geo context formatting to `_geo_context()`, and stable raw_stats assembly to `_raw_stats()`, leaving the parser focused on coordinating signal output with result construction. `_geo_context()` still builds the context string directly instead of allocating a temporary `geo_parts` list solely for `join()`, and `_asn_context()` parses the `org` text with `partition()` instead of `split()` list allocation. `_no_data_result()` now owns the shared informational IP Context result shape for private-IP 404s, malformed responses, and successful geo context instead of repeating the same constructor arguments across branches. Focused proof lives in `tests/test_ip_api.py::TestGeoFormatting::test_parser_delegates_geo_and_raw_stats_helpers`, which keeps signal extraction, formatting, and raw_stats mechanics out of `_parse_response()`, `test_signal_helper_preserves_defaults_and_flags_identity`, which preserves response-field defaults and fresh flags output, `test_geo_format_exact_full_context`, `test_geo_format_exact_minimal_context`, `test_org_parsing_does_not_allocate_split_parts`, `test_asn_context_uses_partition_without_split`, `test_geo_context_builds_without_parts_list`, `test_raw_stats_helper_preserves_flags_identity_and_key_order`, and `test_no_data_result_helper_preserves_informational_shape`, which preserve full CC/city/ASN formatting, missing-field separator behavior, ASN/ISP display text without `split()`, raw_stats key order/flags identity, and the centralized no-data result contract. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_ip_api.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve IP Context public/private-IP behavior, missing-country no-data behavior, no-data verdicts, response-field defaults, raw_stats fields/order, flags list identity, ASN/org parsing, exact geo display text, and HTTP safety controls while avoiding parser-owned response-field extraction, parser-owned formatting/raw_stats mechanics, temporary geo-part and ASN/ISP split-list allocation, and duplicated no-data result construction. |
| Keep WHOIS name-server normalization on list reuse. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/whois_lookup.py::_normalise_name_servers()` now reuses already-materialized WHOIS name-server lists instead of copying them with `list()`, while still materializing tuple/iterable values for the raw_stats contract. `_whois_raw_stats()` now owns successful WHOIS field extraction and raw_stats assembly, using `_safe_whois_field()` to preserve graceful per-field parse-error recording without keeping repeated try/except blocks in `WhoisAdapter.lookup()`. `app/enrichment/adapters/whois_lookup.py::_whois_result()` now owns the shared informational no-data result envelope for successful lookups, domain-not-found misses, and graceful parse/TLD degrade paths instead of repeating verdict, counts, scan-date, and raw_stats constructor wiring across branches. Focused proof lives in `tests/test_whois_lookup.py::TestSuccessfulLookup::test_lookup_delegates_successful_raw_stats_extraction`, which keeps field parsing out of the public lookup method, `test_whois_raw_stats_helper_preserves_shape_and_list_identity`, which preserves raw_stats key order, datetime normalization, and name-server list reuse, `test_whois_raw_stats_helper_records_parse_errors`, which preserves graceful per-field parse-error defaults, `tests/test_whois_lookup.py::TestRawStatsExtraction::test_name_server_lists_are_reused_without_copying`, `test_name_server_non_list_iterables_are_materialized`, `TestNormaliseNameServers::test_normalise_name_servers_reuses_exact_lists_without_iterating`, `test_normalise_name_servers_skips_iteration_for_empty_single_pair_three_or_four_tuple`, which proves short tuple name-server values up to four entries avoid fallback iteration, and `tests/test_whois_lookup.py::TestSuccessfulLookup::test_result_helper_preserves_provider_envelope`. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_whois_lookup.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve WHOIS no-data verdicts, registrar/org/date extraction, raw_stats key order, name_servers raw_stats list behavior, domain-not-found defaults, parse-error recording, parsed-response total_engines, and non-HTTP protocol boundaries while avoiding lookup-owned field extraction try/except blocks, unnecessary name-server list copies, and duplicated WHOIS result-envelope construction. |
| Keep shared HTTP response reads on one byte accumulator. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/http_safety.py::read_limited()` now extends one `bytearray` while enforcing the 1 MB response cap instead of appending chunks to a list and joining them after the stream is read. `RESPONSE_CHUNK_SIZE` now owns the streaming read chunk size alongside the other HTTP safety constants instead of leaving an inline magic number in the response loop. Focused proof lives in `tests/test_http_safety.py::TestReadLimited::test_read_limited_parses_chunked_json`, which preserves chunked JSON parsing and the documented 8192-byte chunk size, plus `test_read_limited_uses_shared_chunk_size_constant`, which fails if `read_limited()` stops reading the shared chunk-size constant. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_http_safety.py tests/test_adapter_contract.py`; `make verify-fast` | Preserve SSRF validation, stream=True/no-redirect request flags, timeout behavior, pre-raise hook semantics, response byte cap enforcement, JSON parsing, and exception mapping while avoiding per-response chunk-list allocation and final bytes join. |
| Keep HTTP adapter allowlist membership on a construction-time set. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/base.py::BaseHTTPAdapter.__init__()` now materializes `allowed_hosts` as a `frozenset` once, so every `safe_request()` SSRF validation checks hostname membership against the cached set instead of rescanning the original list. The same constructor now snapshots auth headers through `_auth_headers_snapshot()`, which skips the no-op `Session.headers.update({})` call for adapters whose default auth headers are empty and copies non-empty header mappings with direct key iteration instead of `dict(...)`, while preserving keyed adapter header setup. `app/enrichment/http_safety.py::validate_endpoint()` and `safe_request()` accept collection-style allowlists, preserving the existing error path for rejected hosts. Focused proof lives in `tests/test_base_adapter.py::TestLookupDispatch::test_allowed_hosts_are_cached_as_membership_set`, which verifies `lookup()` passes the cached set to `safe_request()` and ignores later mutations to the caller's list, `test_single_pair_three_or_four_allowed_hosts_skip_general_iteration`, which proves short allowlists up to four hosts avoid general iteration or slicing, `tests/test_base_adapter.py::TestAuthHeaders::test_default_auth_headers_skip_empty_session_update`, which fails if a public adapter updates the session with an empty header mapping, `test_auth_headers_snapshot_accumulates_without_constructor_copy`, which guards the header snapshot path against `dict(...)`, `.items()`, and mapping `.copy()`, `test_auth_headers_snapshot_short_paths_skip_fallback_loop`, which proves short auth header mappings up to four keys avoid the fallback copy loop, and `test_adapter_auth_header_cache_uses_immutable_snapshot`, which preserves immutable cached headers even if the source mapping mutates later. | R008, R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_base_adapter.py tests/test_http_safety.py tests/test_adapter_contract.py`; `make verify-fast` | Preserve SSRF allowlist enforcement, unsupported-type rejection, pre-raise hook behavior, POST body dispatch, keyed adapter auth headers, immutable header snapshots, and rejected-host error reporting while avoiding repeated linear allowlist membership scans on HTTP-backed provider lookups, auth-header constructor copies, and no-op empty header updates during public adapter construction. |
| Keep route-mapped adapter support declarations derived from endpoint maps. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `HashlookupAdapter.supported_types`, `OTXAdapter.supported_types`, and `URLhausAdapter.supported_types` now derive directly from their private IOC-type route maps instead of repeating equivalent frozenset literals. `app/enrichment/adapters/hashlookup.py::_parse_response()` now delegates stable NSRL response-field extraction to `_hashlookup_signals()` and metadata shaping to `_hashlookup_raw_stats()`, keeping body reads and raw_stats literals out of the parser body. `app/enrichment/adapters/otx.py::_otx_signals()` now owns OTX pulse-info, reputation, and type-title extraction with provider-specific defaults, while `_parse_response()` delegates pulse threshold mapping to `_otx_verdict()` and stable raw_stats assembly to `_otx_raw_stats()`, keeping response-field reads, threshold branches, and raw_stats literals out of the parser body. `app/enrichment/adapters/abusech.py::abusech_query_status()` owns the shared abuse.ch query-status fallback used by URLhaus and the MalwareBazaar/ThreatFox data-record gate. `app/enrichment/adapters/urlhaus.py::_urlhaus_signals()` now owns abuse.ch query-status extraction plus URLhaus count, tag, blacklist, and signature metadata defaults, while `_parse_response()` delegates query-status/count verdict mapping to `_urlhaus_verdict()` and stable raw_stats assembly to `_urlhaus_raw_stats()`, keeping listed/host/payload branching, provider status fallback, response-field reads, and raw_stats literals out of the parser body. `app/enrichment/adapters/base.py::BaseHTTPAdapter._make_pre_raise_hook()` now applies the generic Hashlookup/OTX 404 no-data hook policy from `_no_data_on_404` class flags instead of keeping equivalent provider-local hook methods in each adapter. `app/enrichment/adapters/hashlookup.py::_hashlookup_result()` now owns the shared provider envelope for CIRCL 404 no-data hooks and known-good NSRL responses instead of repeating provider, scan-date, total_engines, and raw_stats constructor wiring across branches. `app/enrichment/adapters/otx.py::_otx_result()` now owns the shared provider envelope for both OTX 404 no-data hooks and parsed malicious/suspicious/no-data pulse responses instead of repeating provider, scan-date, total_engines, and raw_stats constructor wiring across branches. `app/enrichment/adapters/urlhaus.py::_urlhaus_result()` now owns the shared provider envelope for parsed listed, host, payload, and no-result responses instead of keeping provider, scan-date, total_engines, and raw_stats constructor wiring inside the verdict branch parser. Focused proof lives in `tests/test_hashlookup.py::TestURLPattern::test_supported_types_derive_from_hash_route_map`, `tests/test_hashlookup.py::TestLookupFound::test_parse_response_delegates_raw_stats_helper`, `test_signal_helper_preserves_defaults`, `test_raw_stats_helper_preserves_key_order_and_defaults`, `tests/test_hashlookup.py::TestLookupFound::test_result_helper_preserves_provider_envelope`, `tests/test_otx.py::TestOTXTypeMapping::test_supported_types_derive_from_otx_route_map`, `tests/test_otx.py::TestOTXLookup::test_parse_response_delegates_verdict_and_raw_stats_helpers`, `test_signal_helper_preserves_defaults_and_pulse_mapping`, `test_verdict_helper_preserves_thresholds`, `test_raw_stats_helper_preserves_key_order_and_values`, `tests/test_base_adapter.py::TestPreRaiseHook::test_shared_404_hook_returns_no_data_result`, `test_shared_404_hook_ignores_other_statuses`, `test_generic_status_policy_flags_build_shared_hook`, `tests/test_urlhaus.py::test_supported_types_derive_from_endpoint_map`, `tests/test_urlhaus.py::TestURLhausLookup::test_parse_response_delegates_verdict_and_raw_stats_helpers`, `test_signal_helper_preserves_defaults_and_metadata_identity`, `tests/test_abusech.py::test_query_status_helper_preserves_provider_fallback`, `test_verdict_helper_preserves_status_and_count_semantics`, `test_raw_stats_helper_preserves_key_order_and_blacklist_identity`, `tests/test_urlhaus.py::TestURLhausLookup::test_result_helper_preserves_provider_envelope`, and `tests/test_otx.py::TestOTXLookup::test_result_helper_preserves_provider_envelope`, which fail if supported IOC types drift from endpoint routing, generic 404 no-data hook policy drifts, Hashlookup/OTX/URLhaus parser helpers drift, Hashlookup response-field defaults drift, OTX response-field defaults drift, URLhaus response-field defaults or metadata identity drift, abuse.ch query-status fallback drifts, or centralized Hashlookup/OTX/URLhaus result-envelope contracts change. | R008, R010, R020, R085 | `python3 -m pytest -q tests/test_hashlookup.py tests/test_otx.py tests/test_urlhaus.py tests/test_adapter_contract.py`; `make verify-fast` | Preserve provider support coverage, endpoint path/body routing, auth headers, no-data hooks, Hashlookup known-good semantics, Hashlookup response-field defaults, Hashlookup raw_stats fields/order/defaults, OTX pulse verdict thresholds, OTX response-field defaults, OTX raw_stats fields/order, URLhaus listed/host/payload verdict semantics, URLhaus response-field defaults, URLhaus metadata identity, URLhaus raw_stats fields/order, abuse.ch query-status fallback semantics, parsed-response total_engines, and adapter contract behavior while removing duplicated import-time type-set declarations that can drift from routing maps, provider-local generic 404 hook closures, parser-owned Hashlookup response-field extraction/raw_stats mechanics, parser-owned OTX response-field extraction, parser-owned OTX threshold/raw_stats mechanics, parser-owned URLhaus response-field extraction, parser-owned URLhaus query-status/verdict/raw_stats mechanics, and duplicated Hashlookup/OTX/URLhaus result-envelope construction. |
| Keep MalwareBazaar result construction behind one provider envelope helper. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/abusech.py::abusech_data_records()` now owns the shared abuse.ch query-status and data-list normalization gate used by MalwareBazaar and ThreatFox, so their parsers do not duplicate no-result status checks or missing-data list coercion. `app/enrichment/adapters/malwarebazaar.py::_parse_response()` now delegates stable malware metadata shaping to `_malwarebazaar_raw_stats()`, keeping raw_stats literals and tag normalization out of the parser body after sample selection. `app/enrichment/adapters/malwarebazaar.py::_malwarebazaar_result()` now owns the shared provider envelope for both `hash_not_found` no-data results and found-sample malicious results instead of repeating provider, verdict metadata, scan-date, and raw_stats constructor wiring in each branch. Focused proof lives in `tests/test_malwarebazaar.py::TestLookupFound::test_parse_response_delegates_raw_stats_helper`, which keeps raw_stats literals and abuse.ch data-gate logic out of `_parse_response()`, `tests/test_abusech.py::test_data_records_preserve_not_found_and_missing_data_contract`, which preserves hash-not-found and missing-data behavior through the shared helper, `test_raw_stats_helper_preserves_key_order_and_tag_identity`, which preserves the public raw malware metadata envelope without copying tag lists, and `test_result_helper_preserves_provider_envelope`, while the existing found/not-found tests preserve MalwareBazaar verdict and metadata behavior. | R008, R010, R020, R085 | `python3 -m pytest -q tests/test_malwarebazaar.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve MalwareBazaar hash-only support, form-encoded request body, auth headers, hash-not-found no-data behavior, missing-data no-data behavior, found-sample malicious semantics, first_seen scan_date, raw_stats fields/order, and tag-list identity while removing parser-owned abuse.ch query-status/data-list normalization, parser-owned raw_stats envelope construction, and duplicated result-envelope construction. |
| Keep GreyNoise result construction behind one provider envelope helper. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/adapters/greynoise.py::_greynoise_signals()` now owns Community API field extraction and provider-specific defaults, while `_parse_response()` delegates verdict priority to `_greynoise_verdict()` and stable raw_stats assembly to `_greynoise_raw_stats()`, keeping GreyNoise response-field reads, RIOT/malicious/noise ordering, and raw_stats literals out of the parser body. `app/enrichment/adapters/base.py::BaseHTTPAdapter._make_pre_raise_hook()` now applies GreyNoise's generic 404 no-data hook policy from the `_no_data_on_404` class flag instead of keeping a provider-local hook method in the adapter. `app/enrichment/adapters/greynoise.py::_greynoise_result()` now owns the shared provider envelope for both Community API 404 no-data hooks and parsed clean/malicious/suspicious/no-data responses instead of repeating provider, total_engines, scan_date, and raw_stats constructor wiring across branches. Focused proof lives in `tests/test_greynoise.py::TestGreyNoiseLookup::test_parse_response_delegates_verdict_and_raw_stats_helpers`, which guards the parser against regrowing signal extraction, verdict branches, raw_stats literals, or a provider-local 404 hook, `test_signal_helper_preserves_defaults_and_key_fallbacks`, which preserves GreyNoise field defaults, `test_verdict_helper_preserves_priority`, which preserves RIOT clean priority, malicious classification, suspicious noise, and no-data fallback, `test_raw_stats_helper_preserves_key_order_and_values`, which preserves the public raw_stats envelope, and `test_result_helper_preserves_provider_envelope`, while the existing 404 tests preserve GreyNoise response behavior. | R008, R010, R020, R085 | `python3 -m pytest -q tests/test_greynoise.py tests/test_adapter_contract.py tests/test_registry_setup.py`; `make verify-fast` | Preserve GreyNoise IPv4/IPv6 support, lowercase auth header, field defaults, 404 no-data behavior, RIOT clean priority, malicious classification priority, suspicious noise handling, last_seen scan_date, and raw_stats fields/order while removing provider-local generic 404 hook methods, parser-owned signal extraction, parser-owned verdict branching, parser-owned raw_stats envelope construction, and duplicated result-envelope construction. |
| Keep ConfigStore read-after-write on the cached parser path. | provider registration/config diagnostics | mocked parser proof + focused regression proof | `app/enrichment/config_store.py::_save_config()` now keeps the just-written `ConfigParser` instance cached instead of clearing `_cached_cfg`, so immediate reads after settings writes do not reparse the same config file. `app/enrichment/config_files.py` now owns path-scoped locks, mutable parser copying, and atomic owner-only writes, leaving `app/enrichment/config_store.py` focused on VirusTotal/provider keys, cache TTL, and SSH normal-hours accessors without retaining private file-helper aliases. `app/enrichment/config_files.py::copy_config()` now copies parser sections/options with direct loops instead of `read_dict({section: dict(...)})`, preserving failed-write isolation without constructor-copying section proxies or building a dict-comprehension frame. `app/enrichment/config_values.py` now owns configured-value normalization, case-insensitive provider option normalization, cache TTL parsing/fallback, and provider-section key accumulation, leaving `ConfigStore` as a cached parser persistence facade without retaining private value-helper aliases. `app/enrichment/config_values.py::provider_keys_from_config()` also accumulates provider section keys directly instead of constructor-copying the section proxy, with direct short paths for up to four configured providers and long provider-section key copy mutation delegated to `append_provider_key()`. Focused proof lives in `tests/test_config_store.py::TestConfigStoreSetAndGet::test_save_keeps_written_config_cached`, which patches `ConfigParser.read` and verifies read-after-write returns the saved key without disk parsing, `TestConfigStoreSetAndGet::test_config_store_delegates_file_mechanics_to_config_files`, which keeps lock/copy/atomic-write mechanics out of the domain class, `TestConfigStoreSetAndGet::test_config_copy_accumulates_sections_without_constructor_copies`, which preserves parser copy isolation while guarding against `read_dict`, `dict(...)`, and dict-comprehension regressions, `TestConfigStoreSetAndGet::test_config_store_delegates_value_helpers`, which keeps value normalization, cache TTL parsing, and provider-section accumulation out of `ConfigStore`, `TestConfigStoreMultiProvider::test_all_provider_keys_accumulates_directly_from_section`, which makes constructor-style section copying fail while preserving provider-key output, `TestConfigStoreMultiProvider::test_provider_key_fallback_delegates_append_mutation`, which keeps the fallback accumulator delegated to `append_provider_key()`, and `TestConfigStoreMultiProvider::test_provider_key_get_and_set_share_option_normalization`, which proves provider get/set use the shared option-name helper. | R008, R009, R040, R085, R087 | `python3 -m pytest -q tests/test_config_store.py tests/test_settings.py`; `make verify-fast` | Preserve owner-only config writes, disk persistence for fresh store instances, failed-write cache isolation, provider-key sections, provider-name case-insensitivity, cache TTL, SSH normal-hours settings, and settings-page behavior while avoiding immediate post-save config reparsing, ConfigStore-owned file-lock/tempfile mechanics, ConfigStore-owned value normalization and TTL parsing, parser-copy dict-comprehension frames, provider-section constructor copies, and duplicated provider option-name normalization. |
| Keep registry provider-key loading on one config map read. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/provider_catalog.py` now owns provider metadata and static registration order, while `app/enrichment/setup.py::build_registry()` stays focused on runtime config reads and provider construction. `app/enrichment/provider_catalog.py::PROVIDER_REGISTRATION_PLAN` now owns the full ordered registration plan, so `build_registry()` walks one catalog sequence directly instead of special-casing VirusTotal, Shodan, split keyed-provider groups, trailing zero-auth providers, retaining private setup-module aliases for catalog tables, or keeping parallel split catalog tables. `app/enrichment/provider_catalog.py::ProviderRegistration` and registration-kind constants now give each plan row named fields and shared dispatch labels while preserving tuple unpacking for order checks, so setup dispatch no longer depends on route-local positional tuple unpacking or inline string literals. `app/enrichment/provider_catalog.py` now imports concrete adapter classes under private aliases and its `__all__` does not advertise them, so adapter classes used only to construct the static registration plan do not leak as public catalog facade attributes. `app/enrichment/setup.py::build_registry()` now reads `ConfigStore.all_provider_keys()` once and passes key-requiring adapters values from that map instead of calling `get_provider_key()` once per provider. `app/enrichment/setup.py::_register_keyed_provider()` also centralizes non-VirusTotal keyed adapter construction and registration so those providers share one key-map lookup path. `_register_zero_auth_provider()` centralizes zero-auth HTTP adapter construction and registration so providers that need no API key but still need SSRF allowlist state share one construction path, including Shodan. `_register_direct_provider()` owns zero-auth non-HTTP adapter construction for DNS, ASN, and WHOIS providers so those adapters no longer accept unused HTTP allowlist arguments. `_register_provider_from_plan()` owns the plan dispatch for VirusTotal, keyed providers, zero-auth HTTP providers, and direct non-HTTP providers so `build_registry()` has one registration loop. Focused proof lives in `tests/test_registry_setup.py::TestBuildRegistry::test_config_store_all_provider_keys_called_once_for_key_providers`, `test_key_required_providers_share_registration_helper`, `test_zero_auth_http_providers_share_registration_helper`, `test_non_http_zero_auth_providers_use_direct_registration_helper`, `test_provider_registration_tables_preserve_order_without_slices`, and `tests/test_emailrep_registry_settings.py::test_build_registry_reads_emailrep_key_from_config_store`, which preserve EmailRep configuration, missing-key behavior, all-provider registration, zero-auth provider setup, direct DNS/ASN/WHOIS setup, static provider order, unified registration-plan order, named registration records, adapter-owner public imports, removal of split catalog registration tables, and the single provider-key map load. | R008, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_registry_setup.py tests/test_emailrep_registry_settings.py tests/test_provider_registry.py`; `make verify-fast` | Preserve all 16 registered providers, VirusTotal's separate key path, key-required provider configuration semantics, zero-auth HTTP provider behavior, DNS/ASN/WHOIS non-HTTP provider behavior, EmailRep email-only coverage, local config failure fallback, and catalog-owned registration data while avoiding repeated provider-key lookups, split provider-order registration loops, duplicated keyed-provider construction paths, duplicated zero-auth HTTP construction paths, parallel split catalog registration tables, setup-module metadata re-export aliases, catalog-facade public attributes or re-exports of concrete adapter classes, positional registration tuple dispatch, inline registration-kind string branching, and unused HTTP allowlist constructor arguments on non-HTTP adapters during registry construction. |
| Keep settings provider-key display on one config map read. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/routes/settings.py::settings_get()` now reads `ConfigStore.all_provider_keys()` once and uses that map for provider status display instead of calling `get_provider_key()` once per non-VirusTotal provider. `app/routes/settings_view.py` now owns settings provider status rows, local provider health rows, supported IOC type labels, and the shared VirusTotal provider id, leaving `app/routes/settings.py` focused on request handling and applying route responses without re-exporting settings view compatibility aliases. `app/routes/settings_view.py::settings_page_context()` now owns the full settings GET template context, including provider rows, local provider health, cache stats, cache TTL, and history-save diagnostics. `app/routes/settings_view.py::settings_route_context()` now owns ConfigStore construction from route-supplied cache/registry dependencies for settings GET, `settings_page_result()` wraps that context in the shared `TemplateResult` response boundary, and `settings_page_route_response()` applies that decision so `settings_get()` only wires explicit dependencies. `app/routes/settings_view.py::save_provider_key()` now owns VirusTotal-vs-generic provider key persistence so the POST route no longer carries provider-specific ConfigStore save branches. `app/routes/settings_view.py::save_provider_key_and_rebuild_registry()` now owns the save-then-rebuild operation so `settings_post()` no longer imports or calls `build_registry()` directly after a key save. `app/routes/settings_view.py::provider_key_save_action()` now owns provider-key POST validation, ConfigStore construction, save/rebuild sequencing, and action message/category selection. `app/routes/settings_view.py::provider_key_save_action_from_form()` now owns submitted provider-id/API-key form normalization for key saves, `provider_key_save_route_response()` owns provider-key POST action construction plus flash/redirect response application, and `app/routes/settings_view.py::apply_settings_action()` applies optional registry mutation through an explicit setter instead of accepting the whole Flask app. `app/routes/settings_view.py::positive_cache_ttl_hours()` now owns cache TTL parsing and positive-integer validation so `cache_ttl_set()` no longer carries route-local `int()`/`ValueError` parsing branches. `app/routes/settings_view.py::cache_ttl_update_action()` now owns cache-TTL validation, ConfigStore construction, save execution, and action message/category selection. `app/routes/settings_view.py::cache_ttl_update_action_from_form()` now owns submitted cache-TTL form normalization, and `cache_ttl_update_route_response()` owns cache-TTL action construction plus flash/redirect response application. `app/routes/settings_view.py::cache_clear_action()` now owns cache clear execution and action result selection, and `cache_clear_route_response()` owns cache-clear action construction plus flash/redirect response application. `app/routes/settings_view.py` now owns settings action flash message text, and `app/routes/settings_view.py::apply_settings_action_response()` owns optional registry mutation through the route-supplied setter before delegating Flask flash/redirect response application to `app/routes/browser_responses.py::apply_flash_redirect()` for provider-key saves, cache clears, and TTL updates. `app/routes/settings.py` now imports `settings_view` as the owner module and calls those settings actions directly instead of retaining private action aliases in the route module. `app/routes/settings_view.py::_provider_status_row()` now owns one provider display row, while `provider_status_rows()` stays a direct accumulator that delegates row construction instead of repeating metadata copy, key lookup, mask, and configured-flag mechanics in the loop. `app/routes/settings_view.py::_provider_health_row()` now owns one local provider health row, while `provider_health_rows()` stays a direct accumulator that delegates secret-free readiness/key-status/support text shaping instead of keeping the full row literal in the loop. `app/routes/settings_view.py::_mask_key()` also reuses the configured-key length while building masked display text instead of measuring it twice. Focused proof lives in `tests/test_settings.py::test_get_settings_reads_provider_key_map_once`, which verifies configured display state while failing if the route falls back to per-provider reads, `tests/test_settings.py::test_settings_route_delegates_provider_rows_to_view_helpers`, which keeps provider row shaping, settings page result construction, GET response application, and key-save branching out of the Flask route body, `test_settings_page_context_owns_get_template_shape`, which keeps settings GET template shape, ConfigStore construction, whole-app settings helper inputs, cache/config reads, and settings GET response application out of the route body while proving the route passes cache/registry dependencies explicitly, `test_save_provider_key_helper_preserves_vt_and_provider_paths`, which preserves VirusTotal and generic provider save paths, `test_save_provider_key_and_registry_rebuild_lives_in_view_helper`, which preserves save-then-registry-rebuild ordering while keeping direct registry construction out of the route body, `test_provider_key_save_action_from_form_owns_field_normalization`, which keeps provider-key form normalization out of the route body, `test_provider_key_save_route_response_owns_action_application`, which keeps provider-key action construction and flash/redirect response application out of the route body, `test_provider_key_save_action_owns_post_validation_and_registry_rebuild`, which keeps POST validation, ConfigStore construction, save/rebuild sequencing, and action message/category selection in the settings view helper, `test_apply_settings_action_owns_optional_registry_assignment`, which keeps optional rebuilt-registry assignment behind an explicit setter and out of the Flask route body, `test_apply_settings_action_response_preserves_flash_redirect_and_registry_assignment`, which preserves registry mutation, flash, redirect URL, and response ordering through shared browser response application, `test_settings_action_response_application_lives_in_view_helper`, which keeps public settings action flash text, explicit registry-setter forwarding, and repeated redirect plumbing out of the route module, `test_positive_cache_ttl_helper_owns_ttl_validation`, which keeps TTL parsing out of the route body while preserving accepted/rejected values, `test_cache_ttl_update_action_owns_validation_and_save`, which keeps TTL ConfigStore construction, save execution, and action message/category selection in the settings view helper, `test_cache_ttl_update_route_response_owns_action_application`, which keeps cache-TTL action construction and flash/redirect response application out of the route body, `test_cache_clear_action_owns_cache_clear_and_result`, which keeps cache clear execution and result selection in the settings view helper, `test_cache_clear_route_response_owns_action_application`, which keeps cache-clear action construction and flash/redirect response application out of the route body, `tests/test_settings.py::test_provider_status_rows_accumulates_without_constructor_copy`, which preserves provider status output while guarding against `dict(info)` and `.items()` mapping copies and proving the accumulator delegates to `_provider_status_row()`, `test_provider_health_row_helper_owns_secret_free_shape`, which preserves configured/missing/zero-auth health semantics while keeping API keys, network lookups, and full row literals out of the accumulator, and `tests/test_settings.py::test_mask_key_measures_configured_key_once`, which preserves masked-key output while failing if configured key masking repeats length work. | R008, R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_settings.py tests/test_registry_setup.py`; `make verify-fast` | Preserve settings page provider metadata, masked key display, configured/not-configured badges, local provider health rows, VirusTotal's separate key path, cache stats/TTL display and saves, settings flash messages, registry rebuild behavior after key saves, and history-save diagnostics while avoiding repeated provider-key lookups on settings GET, repeated configured-key length work during mask rendering, provider-info constructor copies, repeated provider status row construction mechanics, repeated provider health row literal construction, route-local provider view-model construction, route-owned settings GET template-context assembly, route-owned settings GET result construction, route-owned settings GET response application, whole-app settings helper inputs, whole-app settings action-response inputs, route-local provider-key action construction, route-local cache-TTL action construction, route-local cache-clear action construction, route-local cache TTL parsing, route-local cache TTL save execution, route-local cache clear execution, route-local provider key save branching, route-local provider save validation, route-local optional registry assignment, route-local settings flash text, route-local settings flash/redirect response application, repeated settings flash/redirect plumbing, route-local ConfigStore construction for key saves, route-local ConfigStore construction for TTL saves, and route-local provider registry rebuild construction. |
| Keep settings provider validation on a precomputed ID set. | provider registration/config diagnostics | code-path reasoning + focused regression proof | `app/enrichment/provider_catalog.py::valid_provider_ids()` now derives valid provider IDs from `PROVIDER_INFO` through one direct helper loop, and `app/routes/settings.py::_VALID_PROVIDER_IDS` precomputes that set once at import time so `settings_post()` passes it into the form action helper instead of rebuilding `{p['id'] for p in PROVIDER_INFO}` on every save attempt or maintaining a duplicate provider-id tuple. `app/routes/form_values.py::stripped_form_value()` now owns route form-field normalization, and `app/routes/settings_view.py::provider_key_save_action_from_form()` plus `cache_ttl_update_action_from_form()` delegate to it for provider-key saves and cache TTL updates instead of open-coding request form access or trimming in the route module. Focused proof lives in `tests/test_settings.py::test_valid_provider_ids_reuse_catalog_helper_without_route_list_builder`, which keeps provider-id ownership in the catalog while guarding the route against duplicate provider-id constants, `test_save_provider_validation_uses_precomputed_id_set`, where `PROVIDER_INFO` is patched to fail if POST validation iterates it and the helper is guarded against generator/set-comprehension frames, `tests/test_settings.py::test_settings_post_and_cache_ttl_share_form_normalization`, which proves both POST paths share the stripped-value helper while preserving saved key and TTL values, `test_provider_key_save_action_from_form_owns_field_normalization`, which keeps submitted provider-id/API-key normalization in the form action helper, plus `test_settings_form_normalization_lives_in_shared_route_helper`, which keeps request form access and text trimming out of the settings route bodies. | R008, R009, R010, R040, R085, R087 | `python3 -m pytest -q tests/test_settings.py tests/test_registry_setup.py`; `make verify-fast` | Preserve settings save validation, unknown-provider rejection, VirusTotal/provider key save routing, registry rebuild behavior, cache TTL validation, flash/redirect flow, and provider metadata rendering while avoiding repeated provider-id set construction, duplicate provider-id metadata tuples, duplicated form-value normalization on settings POST routes, route-local request form access/trimming mechanics, and import-time provider-id generator/comprehension frames. |

### do next

| Finding | Seam | Evidence kind | Evidence summary | Continuity guardrails | Rerun lanes | Continuity notes |
| --- | --- | --- | --- | --- | --- | --- |

### later

| Finding | Seam | Evidence kind | Evidence summary | Continuity guardrails | Rerun lanes | Continuity notes |
| --- | --- | --- | --- | --- | --- | --- |
| Defer broader SQLite cache/history access-shape work until contention evidence appears. | local cache/history | ranked project-map priority | `docs/project-map.md` ranks SQLite access shape third, but the `cache-store-tempdb`, `cache-stats-query-count`, and `history-store-tempdb` captures now separate the shipped stats query-count reduction from the still-healthy WAL-backed put/get/save/list/load paths. Broader persistence work remains deferred until contention or write-amplification evidence appears. | R085, R087, R022, R040 | `make verify-fast` plus targeted cache/history fixtures when promoted | Promote broader changes only with contention or write-amplification evidence showing equivalent cache hit, expiry, clear, history list, and history reload behavior with less work. |

### leave alone

| Finding | Seam | Evidence kind | Evidence summary | Continuity guardrails | Rerun lanes | Continuity notes |
| --- | --- | --- | --- | --- | --- | --- |
| Leave provider registration/config diagnostics clarity alone for this optimization pass unless readiness diagnostics become the blocker. | provider registration/config diagnostics | identity-grounded keep-decision | The S01 project map ranks this fifth: it supports analyst confidence and secret-redaction boundaries, but it is not the best current performance target for the paste → enrich → review loop. | R085, R087, D080 | `make verify-fast` if settings/diagnostics code changes | Do not expose API keys, tokens, or analyst-sensitive IOC data in audit artifacts, settings output, diagnostics, or command captures. |

## M017 audit notes

- This baseline intentionally contains no placeholder rows; each bucket records a current do-now/do-next/later/leave-alone decision.
- Re-run with `make audit-m017` after each optimization slice so S05 can consume current command-capture rows and S03/S04 shipped-proof language.
- Failed optional capture commands are recorded in the measurement table with nonzero exits so unrelated artifact generation remains inspectable.
