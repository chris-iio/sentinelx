# S02: Identity-Grounded Optimization Audit

**Goal:** Refresh the SentinelX optimization audit workflow for M017 so it consumes the S01 project map, produces a milestone-local ranked audit artifact, and identifies the best do-now/do-next/later/leave-alone optimization target with evidence requirements for S03.
**Demo:** The audit runner/artifact is refreshed for M017, grounded in the project map, with ranked current findings and explicit do-now/do-next/later/leave-alone decisions.

## Must-Haves

- `tools/optimization_audit.py` supports M017-specific audit generation without regressing existing M013 audit tests.
- `tests/test_optimization_audit.py` covers the M017 identity-grounded contract without reading `.gsd/` artifacts.
- `.gsd/milestones/M017/M017-AUDIT.md` exists, is generated by the audit runner, references `docs/project-map.md`, includes ranked buckets `do now`, `do next`, `later`, and `leave alone`, and names an explicit S03 optimization target or explicit no-code-justified outcome.
- Verification commands pass: `python3 -m pytest -q tests/test_optimization_audit.py`; `python3 tools/optimization_audit.py --mode baseline --milestone-id M017 --output .gsd/milestones/M017/M017-AUDIT.md`; structural grep assertions over the generated M017 artifact.

## Proof Level

- This slice proves: Contract proof. Real runtime is not required for this planning/audit slice, but the artifact must preserve runtime proof requirements for downstream code changes. Human/UAT is not required.

## Integration Closure

Upstream surfaces consumed: `docs/project-map.md`, `.gsd/PROJECT.md`, `.gsd/milestones/M013/M013-AUDIT.md`, `tools/optimization_audit.py`, `tests/test_optimization_audit.py`, `Makefile`. New wiring introduced: M017 audit runner output/Makefile target and milestone-local generated audit artifact. Remaining before milestone is truly usable end-to-end: S03 must implement the selected best optimization with measurement or code-path proof, S04/S05 must run analyst-flow/final verification.

## Verification

- Inspection surfaces are the audit CLI (`python3 tools/optimization_audit.py --help`), pytest coverage for the audit contract, and the generated `.gsd/milestones/M017/M017-AUDIT.md`. Failure visibility comes from CLI exit codes, pytest assertion messages, and command-capture rows in the artifact. Redaction constraints: audit content may reference provider/config seams but must not expose API keys, tokens, or analyst-sensitive IOC data beyond synthetic examples.

## Tasks

- [x] **T01: Teach the audit runner the M017 identity-grounded contract** `est:1h 30m`
  Why: S02 must not merely copy the old M013 audit; the runner needs a current M017 contract grounded in SentinelX's S01 project map and decisions D078-D080. Executor skills: test, write-docs, verify-before-complete.
  - Files: `tools/optimization_audit.py`, `tests/test_optimization_audit.py`, `Makefile`
  - Verify: python3 -m pytest -q tests/test_optimization_audit.py

- [x] **T02: Generate the M017 ranked optimization audit artifact** `est:1h`
  Why: S03 needs a concrete, current, ranked audit artifact that names the chosen do-now target and proof requirements. Executor skills: write-docs, verify-before-complete.
  - Files: `.gsd/milestones/M017/M017-AUDIT.md`
  - Verify: python3 tools/optimization_audit.py --mode baseline --milestone-id M017 --output .gsd/milestones/M017/M017-AUDIT.md && test -s .gsd/milestones/M017/M017-AUDIT.md && grep -q "docs/project-map.md" .gsd/milestones/M017/M017-AUDIT.md && grep -q "### do now" .gsd/milestones/M017/M017-AUDIT.md && grep -q "### do next" .gsd/milestones/M017/M017-AUDIT.md && grep -q "### later" .gsd/milestones/M017/M017-AUDIT.md && grep -q "### leave alone" .gsd/milestones/M017/M017-AUDIT.md && grep -qi "S03" .gsd/milestones/M017/M017-AUDIT.md && ! grep -Eq "TBD|TODO|_Fill during" .gsd/milestones/M017/M017-AUDIT.md

- [x] **T03: Prove the audit workflow and S03 handoff are mechanically usable** `est:45m`
  Why: S02 closes only when the refreshed workflow and generated artifact are executable, structurally valid, and usable by downstream S03 without reinterpreting project identity. Executor skills: test, verify-before-complete.
  - Files: `tests/test_optimization_audit.py`, `.gsd/milestones/M017/M017-AUDIT.md`
  - Verify: python3 -m pytest -q tests/test_optimization_audit.py && python3 tools/optimization_audit.py --mode baseline --milestone-id M017 --output .gsd/milestones/M017/M017-AUDIT.md && python3 - <<'PY'
from pathlib import Path
p = Path('.gsd/milestones/M017/M017-AUDIT.md')
text = p.read_text(encoding='utf-8')
required = ['docs/project-map.md', '### do now', '### do next', '### later', '### leave alone', 'S03', 'app/enrichment', 'app/routes', 'app/pipeline']
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit(f'missing required audit markers: {missing}')
if any(token in text for token in ['TBD', 'TODO', '_Fill during']):
    raise SystemExit('unresolved placeholder present')
print('PASS M017 audit workflow')
PY

## Files Likely Touched

- tools/optimization_audit.py
- tests/test_optimization_audit.py
- Makefile
- .gsd/milestones/M017/M017-AUDIT.md
