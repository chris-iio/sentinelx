---
estimated_steps: 48
estimated_files: 3
skills_used: []
---

# T03: Refresh M017 audit evidence for the shipped optimization

---
estimated_steps: 5
estimated_files: 3
skills_used:
  - test
  - write-docs
---

# T03: Refresh M017 audit evidence for the shipped optimization

**Slice:** S03 — Best Optimization Implementation
**Milestone:** M017

## Description

Regenerate the M017 optimization audit after the status snapshot optimization is in place, and update the runner/tests only if needed so the artifact records shipped S03 proof rather than stale target language. The artifact may cite the existing internal status-snapshot scaling capture or a refreshed capture, but it must remain generated by `tools/optimization_audit.py`, not manually patched.

## Load Profile

- **Shared resources**: audit runner measurement fixtures and generated artifact.
- **Per-operation cost**: one local benchmark/regeneration pass plus focused pytest coverage.
- **10x breakpoint**: not runtime-critical; failure risk is stale artifact claims or missing proof.

## Negative Tests

- **Malformed inputs**: missing project map and optional capture failures should remain covered by `tests/test_optimization_audit.py`.
- **Error paths**: failed optional captures remain durable rows instead of aborting artifact generation.
- **Boundary conditions**: generated artifact includes all ranked buckets and concrete seam markers without placeholders.

## Steps

1. Inspect `tools/optimization_audit.py` and `tests/test_optimization_audit.py` for existing M017 capture and ranked finding assertions.
2. If current generated language still says only “S03 should target” after the optimization ships, update the generator contract to record that S03 shipped the tail-only status snapshot optimization with measurement/code-path proof.
3. Keep the artifact grounded in `docs/project-map.md`, R085/R087, D078-D080, and concrete seams `app/enrichment/orchestrator.py`, `app/routes/_helpers.py`, and `app/pipeline` where the runner already references downstream seam inventory.
4. Run focused audit tests and regenerate `.gsd/milestones/M017/M017-AUDIT.md` through the runner.
5. Use executable assertions to confirm the artifact includes S03, the concrete seams, ranked buckets, proof language, and no placeholders.

## Must-Haves

- [ ] Artifact is generated by `tools/optimization_audit.py`, not hand-edited.
- [ ] Artifact records measurement or explicit code-path reasoning for the shipped optimization.
- [ ] Existing M017 audit tests still protect project-map grounding and ranked buckets.

## Verification

- `python3 -m pytest -q tests/test_optimization_audit.py`
- `python3 tools/optimization_audit.py --mode baseline --milestone-id M017 --output .gsd/milestones/M017/M017-AUDIT.md`
- `python3 - <<'PY'\nfrom pathlib import Path\np=Path('.gsd/milestones/M017/M017-AUDIT.md'); t=p.read_text();\nfor s in ['docs/project-map.md','S03','do now','do next','later','leave alone','app/enrichment','app/routes','app/pipeline']:\n    assert s in t, s\nassert 'TBD' not in t and 'TODO' not in t and '_Fill during' not in t\nprint('PASS audit artifact structure')\nPY`

## Observability Impact

- Signals added/changed: generated measurement/proof rows and audit wording become the durable inspection surface.
- How a future agent inspects this: `make audit-m017` or the explicit runner command, then read `.gsd/milestones/M017/M017-AUDIT.md`.
- Failure state exposed: optional capture failures remain nonzero rows with stderr summaries.

## Inputs

- `tools/optimization_audit.py` — generated audit contract and measurement capture source.
- `tests/test_optimization_audit.py` — runner contract tests.
- `docs/project-map.md` — identity-grounding input for the generated audit.
- `app/enrichment/orchestrator.py` — optimized seam cited by proof.
- `app/routes/_helpers.py` — optimized route seam cited by proof.

## Expected Output

- `tools/optimization_audit.py` — updated only if generated S03 shipped-proof wording or capture labels need to change.
- `tests/test_optimization_audit.py` — updated only if runner contract assertions need to cover shipped S03 proof.
- `.gsd/milestones/M017/M017-AUDIT.md` — regenerated M017 audit artifact with current proof.

## Inputs

- `tools/optimization_audit.py`
- `tests/test_optimization_audit.py`
- `docs/project-map.md`
- `app/enrichment/orchestrator.py`
- `app/routes/_helpers.py`

## Expected Output

- `tools/optimization_audit.py`
- `tests/test_optimization_audit.py`
- `.gsd/milestones/M017/M017-AUDIT.md`

## Verification

python3 -m pytest -q tests/test_optimization_audit.py && python3 tools/optimization_audit.py --mode baseline --milestone-id M017 --output .gsd/milestones/M017/M017-AUDIT.md

## Observability Impact

Refreshes the generated audit as the durable proof surface for future agents; optional capture failures stay visible instead of being hidden.
