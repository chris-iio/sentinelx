# S01: M020 Audit Expansion

**Goal:** Produce a generated M020 optimization audit artifact from the source audit runner that ranks aggressive rewrite candidates across SentinelX seams into do-now, do-next, later, and leave-alone outcomes, with proof requirements suitable for downstream rewrite slices.
**Demo:** A generated M020 audit artifact exists, ranks aggressive rewrite candidates across SentinelX seams, and distinguishes do-now, do-next, later, and leave-alone outcomes with proof requirements.

## Must-Haves

- `tools/optimization_audit.py` supports `--milestone-id M020` in both template and baseline modes without hand-editing the generated artifact.
- `tests/test_optimization_audit.py` covers the M020 contract using temporary output paths, including M020 identity/proof language, ranked buckets, candidate seams from `docs/project-map.md`, and downstream proof requirements.
- `Makefile` exposes repo-native `audit-m020-template` and `audit-m020` targets.
- `.gsd/milestones/M020/M020-AUDIT.md` is generated by the runner and contains current do-now/do-next/later/leave-alone outcomes rather than placeholder prose.

## Proof Level

- This slice proves: contract + operational proof. Real runtime is not required; this slice proves the source-generated audit contract, Make entrypoint, and generated artifact. Human/UAT is not required.

## Integration Closure

Upstream surfaces consumed: `docs/project-map.md`, `tools/optimization_audit.py`, `tests/test_optimization_audit.py`, `Makefile`, M020 decisions/requirements from the planning context, and prior M017 audit patterns already encoded in the runner. New wiring introduced: M020 milestone-specific CLI/default-output handling plus Make targets. Remaining milestone work: S02-S04 must consume the ranked candidates to ship or reject specific rewrites, and S05 must refresh the artifact with final outcomes and full `make verify` proof.

## Verification

- The audit runner remains the inspection surface: generated command-surface rows, measurement-capture rows, ranked finding tables, rerun checklist, and guardrail/proof language let future agents inspect candidate selection and failure-proof requirements. Capture-command failures should continue to be recorded as nonzero capture rows without blocking artifact generation. No secrets or analyst IOC payloads should be introduced into generated text.

## Tasks

- [x] **T01: Add M020 audit contract tests** `est:45m`
  Why: S01 is only useful if the M020 audit shape is locked before the runner is changed; tests must prove generated output rather than hand-edited prose. Do: extend `tests/test_optimization_audit.py` with temp-path tests for `--milestone-id M020 --mode template` and `--mode baseline`. Assert the M020 artifact title, decisions D081-D083, requirements R094/R095/R099/R100, `docs/project-map.md` grounding, all four buckets, aggressive rewrite candidate seams, proof requirements, rerun lanes, and no unresolved placeholder rows in baseline output. Keep tests self-contained and do not read `.gsd/` files. Include negative/robustness assertions that default-output selection for M020 is milestone-local and that the audit still records capture-command failures instead of hiding them. Done when the new tests fail against the current runner for missing M020 support and pass after T02.
  - Files: `tests/test_optimization_audit.py`
  - Verify: python3 -m pytest -q tests/test_optimization_audit.py

- [x] **T02: Implement M020 audit generation in the runner and Makefile** `est:1h 30m`
  Why: R094/R095 require the M020 artifact to be source-generated and ranked; the existing runner only has generic/M013 and M017 milestone-specific paths. Do: update `tools/optimization_audit.py` with M020 constants, default output/template paths, M020-specific ranked findings and stance sections, an M020 rerun checklist, command-surface/default-output selection, and baseline language that distinguishes do-now, do-next, later, and leave-alone candidates with proof requirements. Ground the candidate list in `docs/project-map.md` seams and M020 decisions/requirements, while avoiding secrets or analyst IOC data. Update `Makefile` variables, `.PHONY`, and targets for `audit-m020-template` and `audit-m020`. Preserve existing M013/M017 behavior and keep capture-command failure rows visible. Done when the focused audit tests pass and `make audit-m020` writes the milestone artifact.
  - Files: `tools/optimization_audit.py`, `Makefile`, `tests/test_optimization_audit.py`
  - Verify: python3 -m pytest -q tests/test_optimization_audit.py

- [ ] **T03: Generate and verify the M020 audit artifact** `est:30m`
  Why: The slice demo is not true until the DB-independent generated artifact exists for downstream slices to consume. Do: run the new M020 Make target to generate `.gsd/milestones/M020/M020-AUDIT.md`; inspect it enough to ensure it is generated by the runner, has the M020 command surface, contains ranked candidates in all four buckets, and includes proof requirements and verification lanes for downstream S02-S05. Do not hand-edit the artifact. Then run the focused audit tests and the repo's fast verification lane if time permits; at minimum the focused test command is required for task completion. Done when the generated artifact exists, focused tests pass, and the artifact can serve as S02 input.
  - Files: `.gsd/milestones/M020/M020-AUDIT.md`
  - Verify: make audit-m020

## Files Likely Touched

- tests/test_optimization_audit.py
- tools/optimization_audit.py
- Makefile
- .gsd/milestones/M020/M020-AUDIT.md
