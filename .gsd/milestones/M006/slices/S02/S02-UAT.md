# S02: WHOIS Domain Enrichment — UAT

**Milestone:** M006
**Written:** 2026-03-25T11:47:36.226Z

# S02 WHOIS Domain Enrichment — UAT Script\n\n## Preconditions\n- SentinelX running locally with `python3 -m flask run`\n- python-whois==0.9.6 installed in environment\n- No WHOIS API key required (zero-auth provider)\n\n---\n\n## Test 1: WhoisAdapter Provider Protocol Conformance\n**Steps:**\n1. Open Python shell: `python3 -c \"from app.enrichment.adapters.whois_lookup import WhoisAdapter; from app.enrichment.provider import Provider; a = WhoisAdapter(allowed_hosts=[]); print(a.name, a.requires_api_key, a.is_configured(), a.supported_types)\"`\n\n**Expected:**\n- name = \"WHOIS\"\n- requires_api_key = False\n- is_configured() = True\n- supported_types = frozenset({IOCType.DOMAIN})\n\n---\n\n## Test 2: Registry Contains 15 Providers Including WHOIS\n**Steps:**\n1. Run: `python3 -m pytest tests/test_registry_setup.py::TestBuildRegistry::test_registry_has_fourteen_providers -v`\n2. Run: `python3 -m pytest tests/test_registry_setup.py::TestBuildRegistry::test_registry_contains_whois -v`\n3. Run: `python3 -m pytest tests/test_registry_setup.py::TestBuildRegistry::test_whois_is_always_configured -v`\n\n**Expected:**\n- All 3 tests pass\n- Registry reports 15 providers total\n- WHOIS adapter present and always configured\n\n---\n\n## Test 3: WHOIS Lookup Returns Correct raw_stats Fields\n**Steps:**\n1. Run: `python3 -m pytest tests/test_whois_lookup.py::TestRawStatsExtraction -v`\n\n**Expected:**\n- raw_stats contains keys: registrar, creation_date, expiration_date, name_servers, org, lookup_errors\n- registrar is a string from WHOIS response\n- creation_date/expiration_date are ISO format strings (normalized from datetime)\n- name_servers is a list of strings\n- org is a string or None\n- lookup_errors is empty list on success\n\n---\n\n## Test 4: Datetime Polymorphism Handling\n**Steps:**\n1. Run: `python3 -m pytest tests/test_whois_lookup.py::TestDatetimePolymorphism -v`\n\n**Expected:**\n- Single datetime → ISO format string\n- List of datetimes → first element ISO format string\n- Empty list → None\n- None → None\n- String → passed through as-is\n\n---\n\n## Test 5: Domain Not Found Graceful Handling\n**Steps:**\n1. Run: `python3 -m pytest tests/test_whois_lookup.py::TestDomainNotFound -v`\n\n**Expected:**\n- Returns EnrichmentResult (not EnrichmentError)\n- verdict = \"no_data\"\n- raw_stats has empty defaults (registrar=None, name_servers=[], etc.)\n\n---\n\n## Test 6: Error Cases Return Correct Types\n**Steps:**\n1. Run: `python3 -m pytest tests/test_whois_lookup.py::TestQuotaExceeded tests/test_whois_lookup.py::TestCommandFailed tests/test_whois_lookup.py::TestGracefulDegrade tests/test_whois_lookup.py::TestUnexpectedException -v`\n\n**Expected:**\n- WhoisQuotaExceededError → EnrichmentError with \"WHOIS quota exceeded\" message\n- WhoisCommandFailedError → EnrichmentError with \"WHOIS command failed\" message\n- FailedParsingWhoisOutputError → EnrichmentResult with lookup_errors populated\n- UnknownTldError → EnrichmentResult with lookup_errors populated\n- Unexpected exception → EnrichmentError + logger.exception() called\n\n---\n\n## Test 7: No HTTP Safety Imports in WhoisAdapter\n**Steps:**\n1. Run: `grep -c 'http_safety\\|validate_endpoint\\|safe_request' app/enrichment/adapters/whois_lookup.py`\n2. Run: `python3 -m pytest tests/test_whois_lookup.py::TestNoHTTPSafety -v`\n\n**Expected:**\n- grep returns 0 (no matches)\n- Both no-HTTP tests pass\n- WhoisAdapter does not import requests library\n\n---\n\n## Test 8: Frontend WHOIS Context Field Rendering\n**Steps:**\n1. Verify WHOIS in CONTEXT_PROVIDERS: `grep 'WHOIS' app/static/src/ts/modules/row-factory.ts`\n2. Verify PROVIDER_CONTEXT_FIELDS has WHOIS entry with registrar, creation_date (Created), expiration_date (Expires), name_servers (NS/tags), org (Org)\n3. Run: `make typecheck`\n\n**Expected:**\n- WHOIS present in CONTEXT_PROVIDERS set\n- WHOIS entry in PROVIDER_CONTEXT_FIELDS with 5 fields\n- TypeScript typecheck passes clean\n\n---\n\n## Test 9: Full Test Suite Regression Check\n**Steps:**\n1. Run: `python3 -m pytest --tb=short -q`\n\n**Expected:**\n- 1035+ tests pass\n- Zero failures\n- No regressions from existing providers\n\n---\n\n## Edge Cases Covered by Unit Tests\n- Unsupported IOC type (e.g., IPv4) returns EnrichmentError without calling whois\n- name_servers=None in WHOIS response defaults to empty list\n- Empty datetime list returns None (not IndexError)\n- String dates passed through without parsing attempt"
