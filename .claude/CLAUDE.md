# Claude Code Adapter

Read root `AGENTS.md` first. It is canonical for repository policy; do not mirror
those rules here.

Repository skills are canonical under `.agents/skills/` and exposed to Claude by
tracked symlinks under `.claude/skills/`.
