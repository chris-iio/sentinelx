---
phase: 22-enrichment-module-and-entry-point
plan: "02"
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/templates/base.html
  - app/static/main.js
  - app/static/dist/main.js
autonomous: false
requirements: [SAFE-04, SAFE-03]

must_haves:
  truths:
    - "base.html has exactly one script tag referencing dist/main.js (not two script tags)"
    - "app/static/main.js no longer exists in the repository"
    - "The app loads in the browser with all features working from the TypeScript bundle alone"
    - "Enrichment polling, card rendering, verdict updates, and dashboard counts function identically to pre-migration"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Single script tag for dist/main.js"
      contains: "dist/main.js"
    - path: "app/static/dist/main.js"
      provides: "Compiled TypeScript bundle (IIFE, minified)"
  key_links:
    - from: "app/templates/base.html"
      to: "app/static/dist/main.js"
      via: "script tag with defer"
      pattern: "script src.*dist/main\\.js.*defer"
---

<objective>
Remove the original main.js safety net and verify the TypeScript bundle works in isolation — the migration's "point of no return" step.

Purpose: SAFE-04 removes the duplicate `main.js` script tag from base.html (the `dist/main.js` tag already exists and stays). SAFE-03 deletes the original `app/static/main.js` from the repository. These are the final cleanup steps after Plan 01 confirmed the TypeScript bundle compiles correctly.

Output: `base.html` with a single script tag, `main.js` deleted from git, and human verification that the app works.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-enrichment-module-and-entry-point/22-RESEARCH.md
@.planning/phases/22-enrichment-module-and-entry-point/22-01-SUMMARY.md

<interfaces>
<!-- base.html current state (line 35-36): -->
<!-- TWO script tags — dist/main.js AND main.js -->
<!-- SAFE-04: remove line 36 (main.js), keep line 35 (dist/main.js) -->

From app/templates/base.html (lines 35-36):
```html
<script src="{{ url_for('static', filename='dist/main.js') }}" defer></script>
<script src="{{ url_for('static', filename='main.js') }}" defer></script>
```

After cleanup (only line 35 remains):
```html
<script src="{{ url_for('static', filename='dist/main.js') }}" defer></script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove main.js script tag and delete original file</name>
  <files>app/templates/base.html, app/static/main.js</files>
  <action>
Two operations in sequence:

**Step 1 — SAFE-04: Update base.html**
Read `app/templates/base.html` and remove the second script tag on line 36 that references `main.js`. Keep the first script tag on line 35 that references `dist/main.js`. The result should have exactly ONE script tag loading the TypeScript bundle.

Before:
```html
<script src="{{ url_for('static', filename='dist/main.js') }}" defer></script>
<script src="{{ url_for('static', filename='main.js') }}" defer></script>
```

After:
```html
<script src="{{ url_for('static', filename='dist/main.js') }}" defer></script>
```

Do NOT modify any other part of base.html. Only remove the single `main.js` script line.

**Step 2 — SAFE-03: Delete original main.js**
Run `git rm app/static/main.js` to remove the original 835-line vanilla JS file from the repository. This is safe because:
- Plan 01 already created `enrichment.ts` with all the enrichment logic
- Plan 01 already replaced the `main.ts` placeholder with real module imports
- `make js` produces `dist/main.js` from the TypeScript source
- The `dist/main.js` script tag in base.html is already in place

**Step 3 — Rebuild and verify**
Run `make js` to rebuild the bundle (in case any changes in Plan 01 were not yet compiled). Then run `make typecheck` to confirm types are clean. Then verify:
- `test ! -f app/static/main.js` — original file is gone
- `test -f app/static/dist/main.js` — compiled bundle exists
- `grep -c "main.js" app/templates/base.html` should return 1 (only the dist/main.js reference)
  </action>
  <verify>
    <automated>cd /home/chris/projects/sentinelx && test ! -f app/static/main.js && echo "main.js deleted" && test -f app/static/dist/main.js && echo "dist/main.js exists" && grep -c "main.js" app/templates/base.html | grep -q "1" && echo "single script tag" && make typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>base.html has exactly one script tag (dist/main.js), app/static/main.js is deleted from git, make typecheck passes, dist/main.js exists</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify TypeScript migration works end-to-end</name>
  <files>app/templates/base.html, app/static/dist/main.js</files>
  <action>
Present the verification checklist to the user. The TypeScript migration is structurally complete:
- enrichment.ts: typed polling loop, progress bar, result rendering, warning banner, export button
- main.ts: entry point importing all 7 modules (form, clipboard, cards, filter, enrichment, settings, ui)
- base.html: single script tag loading dist/main.js (original main.js removed)
- Original main.js: deleted from repository

The compiled bundle at dist/main.js now contains ALL app JavaScript, built from TypeScript source.
  </action>
  <verify>
User confirms all checks pass:

1. Start the app: `cd /home/chris/projects/sentinelx && python -m flask run`
2. Open http://127.0.0.1:5000 in browser

**Input page checks:**
3. Verify textarea auto-grows when pasting text
4. Verify mode toggle switches between Online/Offline and button text updates
5. Verify submit button is disabled when textarea is empty, enabled when text is present
6. Paste some IOCs (IPs, domains, hashes) and verify "X characters pasted" feedback appears and fades

**Results page checks (offline mode):**
7. Submit in Offline mode — verify IOC cards render with correct types and verdicts
8. Verify filter bar works: click verdict buttons, type pills, and search input
9. Verify copy button copies IOC value to clipboard

**Results page checks (online mode — requires VT API key):**
10. If VT API key is configured: submit in Online mode with a known IP (e.g., 8.8.8.8)
11. Verify enrichment progress bar animates and shows "X/Y providers complete"
12. Verify provider results appear in cards with verdict badges
13. Verify dashboard counts update as results arrive
14. Verify cards reorder by severity
15. Verify export button copies all IOCs with enrichment summaries

**Settings page:**
16. Visit /settings — verify API key show/hide toggle works

**Browser console:**
17. Open DevTools Console — verify NO JavaScript errors appear during any of the above operations
  </verify>
  <done>User types "approved" confirming all features work from the TypeScript bundle, or describes issues for remediation</done>
</task>

</tasks>

<verification>
1. `app/static/main.js` does not exist: `test ! -f app/static/main.js`
2. `app/static/dist/main.js` exists and is a valid IIFE bundle
3. `base.html` contains exactly one `main.js` reference (the dist/ version)
4. `make typecheck` exits 0
5. `make js` exits 0
6. Human verified: app loads and all features work from the TypeScript bundle
</verification>

<success_criteria>
- Original main.js deleted from repository (SAFE-03)
- base.html has single script tag for dist/main.js (SAFE-04)
- App loads correctly with all features working from TypeScript bundle alone
- No JavaScript errors in browser console
- Human has verified the migration is behaviorally equivalent
</success_criteria>

<output>
After completion, create `.planning/phases/22-enrichment-module-and-entry-point/22-02-SUMMARY.md`
</output>
