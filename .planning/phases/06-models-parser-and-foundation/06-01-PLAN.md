---
phase: 06-models-parser-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ssh/__init__.py
  - app/ssh/models.py
  - tests/test_ssh_models.py
autonomous: true
requirements:
  - PARSE-01
  - PARSE-04

must_haves:
  truths:
    - "LoginEvent is a frozen dataclass with all 7 fields from D-01"
    - "source_ip and hostname are mutually exclusive optionals (D-02 invariant)"
    - "ParseSummary is a frozen dataclass with total_lines, parsed_count, skipped_count, warning_count"
    - "SSH models are in app/ssh/models.py — separate package from enrichment"
    - "All model tests pass including immutability and invariant checks"
  artifacts:
    - path: "app/ssh/__init__.py"
      provides: "SSH package initialization"
    - path: "app/ssh/models.py"
      provides: "LoginEvent and ParseSummary frozen dataclasses"
      contains: "@dataclass(frozen=True)"
      exports: ["LoginEvent", "ParseSummary"]
    - path: "tests/test_ssh_models.py"
      provides: "Model unit tests"
      min_lines: 80
  key_links:
    - from: "app/ssh/models.py"
      to: "dataclasses"
      via: "@dataclass(frozen=True)"
      pattern: "dataclass.*frozen.*True"
    - from: "tests/test_ssh_models.py"
      to: "app/ssh/models.py"
      via: "import LoginEvent, ParseSummary"
      pattern: "from app.ssh.models import"
---

<objective>
Create the `app/ssh/` package with LoginEvent and ParseSummary frozen dataclasses, plus comprehensive unit tests.

Purpose: Establishes the SSH domain data models that the parser (Plan 03), GeoIP wrapper (Phase 7), and detector (Phase 8) all depend on. Getting the model right first prevents downstream rework.

Output: `app/ssh/__init__.py`, `app/ssh/models.py`, `tests/test_ssh_models.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-models-parser-and-foundation/06-CONTEXT.md
@.planning/phases/06-models-parser-and-foundation/06-RESEARCH.md

<interfaces>
<!-- Existing frozen dataclass pattern to follow -->
From app/enrichment/models.py:
```python
from __future__ import annotations
from dataclasses import dataclass

@dataclass(frozen=True)
class EnrichmentResult:
    ioc: IOC
    provider: str
    verdict: str
    detection_count: int
    total_engines: int
    scan_date: str | None
    raw_stats: dict
```

From app/pipeline/models.py (IOC pattern):
```python
@dataclass(frozen=True)
class IOC:
    type: IOCType
    value: str
    raw_match: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/ssh/ package with LoginEvent and ParseSummary models</name>
  <files>app/ssh/__init__.py, app/ssh/models.py, tests/test_ssh_models.py</files>
  <read_first>
    - app/enrichment/models.py (frozen dataclass pattern — follow this exactly)
    - app/pipeline/models.py (IOC frozen dataclass — same from __future__ import annotations pattern)
    - .planning/phases/06-models-parser-and-foundation/06-CONTEXT.md (D-01 through D-05, D-10, D-11 — locked field definitions)
  </read_first>
  <behavior>
    - Test: LoginEvent can be instantiated with all 7 fields (username="alice", source_ip="1.2.3.4", hostname=None, timestamp=datetime(2025,1,15,14,30,0), auth_method="password", line_number=42, raw_line="Jan 15 14:30:00 server sshd[1234]: Accepted password for alice from 1.2.3.4 port 54321 ssh2")
    - Test: LoginEvent with hostname set and source_ip=None is valid (username="bob", source_ip=None, hostname="vpn.corp.example.com", timestamp=datetime(2025,1,15,14,30,0), auth_method="publickey", line_number=100, raw_line="...")
    - Test: LoginEvent is frozen — assigning event.username = "mallory" raises FrozenInstanceError
    - Test: ParseSummary can be instantiated with (total_lines=100, parsed_count=50, skipped_count=45, warning_count=5)
    - Test: ParseSummary is frozen — assigning summary.total_lines = 999 raises FrozenInstanceError
    - Test: ParseSummary invariant — parsed_count + skipped_count + warning_count == total_lines holds for a valid instance
    - Test: LoginEvent fields have correct types — source_ip is str|None, hostname is str|None, timestamp is datetime, auth_method is str, line_number is int, raw_line is str
    - Test: LoginEvent with source_ip="2001:db8::1" (IPv6) is valid
    - Test: LoginEvent __eq__ works correctly for two events with identical fields (frozen dataclass provides this)
    - Test: LoginEvent with auth_method="keyboard-interactive/pam" is valid (generic method storage per discretion)
  </behavior>
  <action>
1. Create `app/ssh/__init__.py`:
   - Module docstring: `"""SSH login anomaly detection package.\n\nContains models, parser, and (in later phases) detector and routes\nfor analyzing SSH auth.log files.\n"""`
   - Empty body (package marker only)

2. Create `app/ssh/models.py` with exactly these contents:
   - `from __future__ import annotations` (first line after docstring, per convention)
   - `from dataclasses import dataclass`
   - `from datetime import datetime`
   - Module docstring: `"""SSH login event data models.\n\nProvides immutable data structures for parsed SSH login events.\nAll models are frozen dataclasses matching the project convention\n(see app/enrichment/models.py).\n\nSecurity note: raw_line contains attacker-controlled content from log files.\nDownstream renderers (Phase 9) MUST use createElement + textContent,\nnever innerHTML, when displaying raw_line values (SEC-08).\n"""`
   - `@dataclass(frozen=True)` class `LoginEvent` with fields in this exact order:
     - `username: str`
     - `source_ip: str | None`
     - `hostname: str | None`
     - `timestamp: datetime`
     - `auth_method: str`
     - `line_number: int`
     - `raw_line: str`
   - Docstring for LoginEvent explaining D-02 invariant: "Exactly one of source_ip and hostname is non-None per event. When hostname is present (UseDNS enabled), downstream GeoIP lookup should be skipped."
   - `@dataclass(frozen=True)` class `ParseSummary` with fields:
     - `total_lines: int`
     - `parsed_count: int`
     - `skipped_count: int`
     - `warning_count: int`
   - Docstring for ParseSummary explaining invariant: "parsed_count + skipped_count + warning_count == total_lines"

3. Create `tests/test_ssh_models.py` with TDD tests:
   - `from __future__ import annotations`
   - `from dataclasses import FrozenInstanceError`
   - `from datetime import datetime`
   - `from app.ssh.models import LoginEvent, ParseSummary`
   - Module docstring explaining what is tested
   - Class `TestLoginEvent` with tests for:
     - Instantiation with source_ip set, hostname=None
     - Instantiation with hostname set, source_ip=None
     - IPv6 source_ip ("2001:db8::1")
     - auth_method="keyboard-interactive/pam" accepted
     - Frozen enforcement (FrozenInstanceError on assignment)
     - Equality of two identical instances
     - All field values accessible after creation
   - Class `TestParseSummary` with tests for:
     - Instantiation with all 4 fields
     - Frozen enforcement
     - Invariant assertion: parsed_count + skipped_count + warning_count == total_lines
   - Class `TestLoginEventInvariant` with test for:
     - D-02: Document that both source_ip and hostname being None or both being set is logically invalid (this is a parser responsibility, not a model enforcement — the model allows it but the parser must never produce it)

4. Run tests RED first (before creating models.py), then GREEN.
  </action>
  <verify>
    <automated>cd /home/chris/projects/sentinelx && python -m pytest tests/test_ssh_models.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - app/ssh/__init__.py exists and contains "SSH login anomaly detection package"
    - app/ssh/models.py contains "from __future__ import annotations"
    - app/ssh/models.py contains "@dataclass(frozen=True)" (appears twice — once for LoginEvent, once for ParseSummary)
    - app/ssh/models.py contains "class LoginEvent:" with fields username, source_ip, hostname, timestamp, auth_method, line_number, raw_line
    - app/ssh/models.py contains "class ParseSummary:" with fields total_lines, parsed_count, skipped_count, warning_count
    - app/ssh/models.py contains "Security note: raw_line contains attacker-controlled content"
    - app/ssh/models.py does NOT import from app.enrichment (D-11: clean domain boundary)
    - tests/test_ssh_models.py contains "from app.ssh.models import LoginEvent, ParseSummary"
    - tests/test_ssh_models.py contains "FrozenInstanceError"
    - tests/test_ssh_models.py contains at least 8 test methods (test_ functions)
    - `python -m pytest tests/test_ssh_models.py -x` exits 0
  </acceptance_criteria>
  <done>
    - LoginEvent frozen dataclass exists at app/ssh/models.py with all 7 fields per D-01
    - ParseSummary frozen dataclass exists with 4 counter fields per D-05
    - Both classes have comprehensive docstrings including security note for raw_line
    - All model tests pass including frozen enforcement and field type checks
    - No imports from app.enrichment (D-11 clean boundary)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log file content -> LoginEvent fields | raw_line and username contain attacker-controlled strings from auth.log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | LoginEvent.raw_line | accept | Model stores raw content as-is; rendering is Phase 9 responsibility (SEC-08 textContent enforcement). Docstring documents the obligation. |
| T-06-02 | Information Disclosure | LoginEvent.username | accept | Usernames from log files are not secrets; they are already in the auth.log that the analyst uploaded. No additional exposure. |
</threat_model>

<verification>
- `python -m pytest tests/test_ssh_models.py -x -v` — all tests pass
- `python -c "from app.ssh.models import LoginEvent, ParseSummary; print('imports OK')"` — models importable
- `grep -c "@dataclass(frozen=True)" app/ssh/models.py` — returns 2
</verification>

<success_criteria>
- LoginEvent and ParseSummary are importable frozen dataclasses
- All field names and types match D-01 and D-05 exactly
- At least 8 unit tests pass covering construction, immutability, and field access
- app/ssh/ is a proper Python package with __init__.py
</success_criteria>

<output>
After completion, create `.planning/phases/06-models-parser-and-foundation/06-01-SUMMARY.md`
</output>
