# Optional but recommended for stable CSRF/session behavior across restarts.
# If omitted, SentinelX auto-generates a development key at startup.
SECRET_KEY=

# Provider API keys are configured in the app at /settings and stored in
# ~/.sentinelx/config.ini, not in this repo-local env file.
